---
phase: 02-fake-device-harness-protocol-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [turtle/turtle-helper/bridge/bridge.py, turtle/turtle-helper/bridge/settings.py]
autonomous: true
requirements: [RESIL-03, RESIL-04, RESIL-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Token comparison in the hello handshake is exact, case-sensitive Python string equality (hello.get('token') != settings.bridge_token) with no normalization or case-folding — unchanged by this plan, but now reachable only after settings.bridge_token is guaranteed non-empty (CR-01)"
    - "The bridge's pending-future cleanup on a device disconnect is device-scoped: only the futures belonging to the device that dropped are resolved with an error; pending commands to other connected devices are untouched, proven by a direct in-process check, not a live model call"
    - { statement: "RESIL-03 (a device disconnecting mid-command produces a clean error reply and no leaked pending future) is proven by a direct, zero-spend in-process check against bridge.send_cmd with a synthetic websocket, since the ONLY live way to make the bridge call send_cmd is through the paid agent loop, and this phase budgets exactly two paid calls for the devices-question scenario (D-05), neither of which is available for this check.", verification: backstop }
  artifacts:
    - turtle/turtle-helper/bridge/bridge.py
    - turtle/turtle-helper/bridge/settings.py
  key_links:
    - "bridge.py's handler() hello path is what Plan 02-03's wrong-token, disallowed-player, and drop-and-reconnect scenarios exercise live"
    - "bridge.py's send_cmd()/pending/pending_by_device fix is what Plan 02-06's Python sort_chest composition and the real agent tool calls rely on to never hang or leak"
---

<objective>
Close the four Phase 1 review gaps (CR-01, WR-01, WR-02) folded into this phase by CONTEXT.md D-13, plus
the RESIL-03 disconnect-cleanup fix (D-10), before the harness (Plan 02-03) exists to exercise any of
them. This plan is the "make the bridge trustworthy" half of D-05's sequencing: everything here lands
against the current hand-rolled agent loop and stays correct straight through the Pydantic AI swap
(Plan 02-05/02-06) since none of it touches `agent.py`.

Purpose: RESIL-03/04/05 and the CR-01/WR-01/WR-02 review fold-ins all live in `bridge.py`'s connection
handler and `send_cmd`, which the harness (next plan, next wave) needs already-correct to prove them
live without discovering them broken mid-scenario.

Output: `bridge.py`'s `handler()` (hello validation, reconnect replacement, malformed-frame handling)
and `send_cmd()` (disconnect-safe, device-scoped pending tracking) fixed in place; `settings.py`'s
`bridge_token` field gets the same `min_length=1` guard `allowed_players` already has.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hello validation rewrite — distinct rejection reasons, stale-socket replacement, CR-01</name>
  <files>turtle/turtle-helper/bridge/bridge.py, turtle/turtle-helper/bridge/settings.py</files>
  <read_first>turtle/turtle-helper/bridge/bridge.py, turtle/turtle-helper/bridge/settings.py</read_first>
  <behavior>
    - A hello whose `type` is not `"hello"`: connection closes with code 4000 and reason "expected
      hello"; bridge logs one WARNING naming the remote address and the type it got, never the payload.
    - A hello with no `id` field: connection closes 4000 "hello missing id"; bridge logs one WARNING
      naming the remote address.
    - A hello whose `token` does not equal `settings.bridge_token`: connection closes 4001 "bad token";
      bridge logs one WARNING naming the device id the hello claimed and the remote address — the
      submitted token value itself never appears in any log line, per D-16.
    - A hello with a valid, non-empty token whose `id` already has a live entry in `devices` pointing
      at a *different* websocket: the bridge logs one INFO line naming the device id and closes the OLD
      websocket with code 4000 "replaced", then registers the NEW connection under that id (D-11,
      closes WR-01).
    - `Settings()` constructed with `BRIDGE_TOKEN=` (present, empty) raises `ValidationError` instead
      of validating to an empty string (CR-01, mirrors the existing `allowed_players` guard).
  </behavior>
  <action>
    In turtle/turtle-helper/bridge/settings.py, add `min_length=1` to the `bridge_token` Field alongside
    its existing `description=`, matching the pattern already used on `allowed_players`.

    In turtle/turtle-helper/bridge/bridge.py's `handler()`, replace the single combined
    `if hello.get("type") != "hello" or hello.get("token") != settings.bridge_token:` check with four
    sequential checks in this order: (1) type mismatch -> log + close 4000 "expected hello"; (2) missing
    id (`hello.get("id")` falsy) -> log + close 4000 "hello missing id"; (3) token mismatch -> log
    (device id + remote address only, never the token) + close 4001 "bad token"; (4) stale reconnect —
    look up the existing `devices.get(dev_id)` entry: if present and its `"ws"` is not this `websocket`,
    log an INFO line naming `dev_id` and close that OLD websocket with code 4000 "replaced" (best-effort;
    swallow any exception from closing an already-dead socket) before proceeding to register the new
    one. After all four checks pass, register `devices[dev_id] = {...}` exactly as today.
  </action>
  <acceptance_criteria>
    - settings.py's `bridge_token` Field call includes `min_length=1`
    - bridge.py's handler() has four distinguishable log call sites for: non-hello type, missing id,
      bad token, and stale-device replacement
    - Region-scoped: within 3 lines of the bad-token log call, `hello.get("token")` and `hello["token"]`
      do not appear as a logged argument
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'min_length=1' turtle/turtle-helper/bridge/settings.py && grep -c 'min_length=1' turtle/turtle-helper/bridge/settings.py | grep -q '^2$'</automated>
    <fails_when>non-zero exit, or the count is not exactly 2 (one on allowed_players from Phase 1, one newly added to bridge_token)</fails_when>
  </verify>
  <verify>
    <automated>F=turtle/turtle-helper/bridge/bridge.py; grep -q 'hello missing id' "$F" && grep -q '"replaced"' "$F" && grep -A2 'bad token' "$F" | grep -qv 'hello.get(\"token\")'</automated>
    <fails_when>non-zero exit (a required log/close site is missing, or the token value is being logged near the bad-token rejection)</fails_when>
  </verify>
  <done>All four hello-path outcomes have distinct, correctly-worded log lines and close codes; CR-01's empty-token hole is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: send_cmd — catch ConnectionClosed, track pending futures per device</name>
  <files>turtle/turtle-helper/bridge/bridge.py</files>
  <read_first>turtle/turtle-helper/bridge/bridge.py</read_first>
  <behavior>
    - `send_cmd()` never raises `ConnectionClosed` out to its caller: if the device's socket closes
      while the command is being sent, `send_cmd` returns `{"ok": False, "error": "<device_id>
      disconnected during command"}` (same shape as the existing timeout-error return).
    - After any `send_cmd` call completes (success, timeout, or disconnect), the module-level `pending`
      dict has no leftover entry for that command's `cid`.
    - A device-scoped index exists mapping each connected device id to the set of `cid`s currently
      pending for it, so a disconnect handler (Task 3) can resolve exactly that device's futures without
      touching any other device's pending commands.
  </behavior>
  <action>
    In turtle/turtle-helper/bridge/bridge.py, add a new module-level dict `pending_by_device:
    dict[str, set[str]] = {}` beside the existing `pending` dict declaration. In `send_cmd()`: after
    generating `cid` and creating the future, register the cid in `pending_by_device.setdefault(
    device_id, set())`. Wrap both the `await websocket.send(...)` call and the existing `await
    asyncio.wait_for(fut, settings.cmd_timeout)` in one try/except that also catches
    `websockets.exceptions.ConnectionClosed` (already imported), returning `{"ok": False, "error":
    f"{device_id} disconnected during command"}` on that branch, alongside the existing `TimeoutError`
    branch unchanged. In the existing `finally` block, keep `pending.pop(cid, None)` and also discard
    `cid` from `pending_by_device.get(device_id, set())` so the reverse index never accumulates stale
    entries either.
  </action>
  <acceptance_criteria>
    - `pending_by_device: dict[str, set[str]]` declared at module level in bridge.py
    - `send_cmd`'s try/except catches `ConnectionClosed` in addition to `TimeoutError`
    - `pending_by_device`'s per-device set is updated both on registration and in `finally`
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - <<'PYEOF'
import asyncio
from bridge.settings import Settings
from bridge import bridge as b
from websockets.exceptions import ConnectionClosed

b.settings = Settings(bridge_token="t", allowed_players=["X"], anthropic_api_key="sk-ant-test")

class FakeWs:
    async def send(self, *a, **k):
        raise ConnectionClosed(None, None)

b.devices["dev-1"] = {"ws": FakeWs(), "role": "computer", "caps": []}
r = asyncio.run(b.send_cmd("dev-1", "status", {}))
assert r.get("ok") is False and "disconnect" in r.get("error", ""), r
assert not b.pending, b.pending
assert not b.pending_by_device.get("dev-1"), b.pending_by_device
print("D10_OK")
PYEOF
    </automated>
    <fails_when>non-zero exit, or "D10_OK" absent from stdout (an AssertionError before the print, or an import error if bridge.bridge does not resolve from this cwd)</fails_when>
  </verify>
  <done>send_cmd catches ConnectionClosed, never leaks a pending entry, and tracks pending cids per device.</done>
</task>

<task type="auto">
  <name>Task 3: Malformed-frame handling in the message loop, disconnect-triggered pending cleanup</name>
  <files>turtle/turtle-helper/bridge/bridge.py</files>
  <read_first>turtle/turtle-helper/bridge/bridge.py</read_first>
  <action>
    In turtle/turtle-helper/bridge/bridge.py's `handler()`, wrap the body of the `async for raw in
    websocket:` loop in a try/except so no single malformed frame can propagate out of the loop
    (closing WR-02): catch `json.JSONDecodeError` and log one WARNING naming the device id and the raw
    payload truncated to 200 characters via slicing, then `continue`; after a successful `json.loads`,
    check `isinstance(msg, dict)` and treat a non-dict result the same as a decode failure (log +
    continue); for a `"result"` message whose `cid` is missing or not present in `pending`, log one
    WARNING naming the device id and the cid, then `continue` instead of touching `pending`; for any
    other unrecognized `type` value, log one WARNING naming the device id and the type, then `continue`.
    Wrap any other unexpected exception raised while handling one message in the same try/except (log +
    continue) so the loop itself never exits except via `ConnectionClosed` or the socket ending.

    In the same `handler()`'s existing `finally` block (where `devices.pop(dev_id, None)` runs today),
    add the disconnect half of D-10: pop `pending_by_device.pop(dev_id, set())` (the set added in Task
    2) and for each `cid` in it, pop the matching future from `pending` and, if it exists and is not
    already done, resolve it with `{"ok": False, "error": f"{dev_id} disconnected"}`. This must run
    only for cids that belong to THIS device — do not touch any other device's entries in `pending`.
    Keep the existing D-11-aware guard from Task 1 (`finally` only pops `devices[dev_id]` when it still
    points at this websocket) unchanged.
  </action>
  <acceptance_criteria>
    - The message-processing body inside `async for raw in websocket:` is wrapped in try/except that
      catches `json.JSONDecodeError` and a general `Exception`, both followed by `continue`, never a
      bare `raise` or loop exit
    - A `result` message with an unknown or missing cid is logged and skipped, not passed to
      `pending.get(...)` blindly
    - The `finally` block resolves each of the disconnecting device's own pending futures via
      `pending_by_device`, and does not touch entries belonging to other devices
  </acceptance_criteria>
  <verify>
    <automated>F=turtle/turtle-helper/bridge/bridge.py; grep -q 'JSONDecodeError' "$F" && grep -q 'isinstance(msg, dict)' "$F" && grep -q 'pending_by_device.pop' "$F"</automated>
    <fails_when>non-zero exit (malformed-JSON handling, the dict-shape guard, or the disconnect cleanup using pending_by_device is missing)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run ruff check bridge/ && uv run mypy bridge/</automated>
    <fails_when>non-zero exit from either command, or any error line in the output</fails_when>
  </verify>
  <done>A garbage frame, a result with an unknown cid, or an unrecognized type all log and continue rather than crashing the handler loop; disconnect cleanup is device-scoped via pending_by_device.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `pending_by_device: dict[str, set[str]]` — new module-level dict in `bridge/bridge.py`, maps a
  connected device id to the set of `cid`s currently pending for it.
- Four distinct hello-rejection log lines in `bridge/bridge.py`'s `handler()`: non-hello type, missing
  id, bad token, and stale-device replacement — none of them ever logs the submitted token value.
- `min_length=1` constraint added to `bridge_token` in `bridge/settings.py`'s `Settings` model.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device (any role) -> bridge WebSocket handler | Untrusted input crosses here: hello payloads, event/result frames, and raw bytes over an authenticated-by-shared-token connection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02a | Tampering | `handler()` message loop (malformed/garbage JSON frames) | high | mitigate | D-12 fix: try/except around per-message handling, dict-shape validation, log+continue instead of an unhandled exception terminating the connection (WR-02). |
| T-02-02b | Denial of Service | `pending`/`pending_by_device` growth across many reconnects | medium | mitigate | D-10 fix: every `send_cmd` exit path (success, timeout, ConnectionClosed) and every device disconnect cleans up both dicts; no path leaves an orphaned future. |
| T-02-02c | Information Disclosure | Rejection logging naming the reason | high | mitigate | D-16: log lines name the device id and remote address for every rejection reason; the submitted token value is never interpolated into any log line (region-scoped grep in Task 1's verify). |
| T-02-02d | Spoofing | Same shared `BRIDGE_TOKEN` across all devices; a token holder can claim any device id, including hijacking a live id via D-11's reconnect-replacement | medium | accept | This is the pre-existing trust boundary established in Phase 1 (PROJECT.md: shared-secret + `ALLOWED_PLAYERS` model for a small private server) — D-11 makes reconnect *behavior* correct, it does not change who is trusted to hold the token. Per-device secrets are out of scope for v1.0. |
| T-02-02e | Denial of Service | An empty `BRIDGE_TOKEN=` silently accepting any/no token (CR-01) | critical (pre-fix) | mitigate | `min_length=1` on `bridge_token` closes this at startup validation, mirroring the existing `allowed_players` guard — a misconfigured deploy fails fast instead of accepting every device. |
</threat_model>

<verification>
`uv run ruff check bridge/` and `uv run mypy bridge/` both pass clean. The direct in-process D-10 check
prints `D10_OK`. All four hello-path outcomes have distinct log lines with no token-value leakage.
</verification>

<success_criteria>
`bridge.py`'s handler is resilient to malformed frames, correctly replaces stale same-id reconnections,
logs rejection reasons without leaking the token, and its `send_cmd`/`pending`/`pending_by_device`
machinery never leaks a future or hangs past `settings.cmd_timeout` when a device drops mid-command.
`settings.py`'s CR-01 gap is closed.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-02-SUMMARY.md` when done
</output>
