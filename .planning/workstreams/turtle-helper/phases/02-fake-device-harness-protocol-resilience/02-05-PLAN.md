---
phase: 02-fake-device-harness-protocol-resilience
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - turtle/turtle-helper/bridge/agent.py
  - turtle/turtle-helper/pyproject.toml
  - turtle/turtle-helper/uv.lock
autonomous: true
requirements: [HARN-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every device-forwarding tool the model can call has a Pydantic argument model, so pydantic-ai validates the model's call arguments before the tool function ever runs — a hallucinated or malformed call is rejected with a retry prompt, not a Python KeyError"
    - "The per-run toolset is rebuilt from the live device registry on every request (D-09): with no turtle connected the model never sees move/turn/dig/inspect/refuel; with no worker connected at all it never sees status/list_chest/push_one_slot either"
    - "bridge.py's own source is untouched by this plan — agent.configure(settings, client, devices, send_cmd, say, default_worker) keeps its Phase 1 signature; the Pydantic AI Agent is constructed inside agent.py's configure(), not in bridge.py's main()"
  artifacts:
    - turtle/turtle-helper/bridge/agent.py
  key_links:
    - "agent.py's typed tool functions call the same send_cmd/say/default_worker callables Plan 02-02 hardened — never raising, always returning a dict"
    - "Plan 02-06 adds the composition tools (sort_chest, rule management) on top of this plan's toolset-building function"

user_setup: []
---

<objective>
D-05's final plan begins: replace `agent.py`'s hand-rolled `DEVICE_TOOLS`/`LOCAL_TOOLS` JSON dicts and
`run_tool` dispatcher with typed Pydantic AI tool functions (D-06), and build the per-run toolset from
the live device registry (D-09) so the model never sees a tool it cannot use. This plan covers the
mechanical device-tool conversion and the Agent's construction; Plan 02-06 adds the Python-composed
`sort_chest` and rule-management tools on top.

Purpose: this is the largest, most decision-dense part of the phase (D-06, D-09) — splitting it from
composition (Plan 02-06) keeps each plan's context budget sane and lets `bridge.py` stay completely
untouched, since the swap happens entirely behind `agent.configure()`'s existing signature.

Output: `bridge/agent.py` rewritten onto `pydantic-ai-slim[anthropic]` — typed tool functions replacing
every `DEVICE_TOOLS` entry, `list_devices`/`say` as local tools, a `build_toolset()` implementing D-09's
per-run filtering, and an `Agent` constructed once inside `configure()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pydantic-ai, typed device-forwarding tool functions</name>
  <files>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/pyproject.toml, turtle/turtle-helper/uv.lock</files>
  <read_first>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/pyproject.toml, turtle/turtle-helper/turtle/client.lua</read_first>
  <behavior>
    - A model call to a device tool with a missing required field (e.g. `list_chest` with no `name`) is
      rejected by pydantic-ai's own argument validation before the tool function body ever runs — the
      model gets a retry prompt, not a Python exception surfacing as a 500-shaped internal error.
    - Every device-forwarding tool function returns whatever `send_cmd()` returns (always a dict, never
      raises, per Plan 02-02) — the tool itself adds no new exception path.
  </behavior>
  <action>
    Add `pydantic-ai-slim[anthropic]==2.46.0` to `turtle/turtle-helper/pyproject.toml`'s `dependencies`
    list (the Package Legitimacy Audit in RESEARCH.md already verified this package as OK/Approved — no
    blocking checkpoint needed), then run `uv lock` and `uv sync` to update `uv.lock`.

    In turtle/turtle-helper/bridge/agent.py, delete the `DEVICE_TOOLS` and `LOCAL_TOOLS` list-of-dicts
    and the `run_tool()` dispatcher entirely (they are replaced by this task and Task 2). Add one
    Pydantic `BaseModel` argument class and one `async def` tool function per surviving client.lua
    primitive from Plan 02-01, each taking `ctx: RunContext[None]` and the model instance as its
    parameters (matching pydantic-ai's single-object-parameter schema simplification): `StatusArgs`
    (device: str | None); `ListChestArgs` (device: str | None, name: str, required, peripheral name);
    `PushOneSlotArgs` (device: str | None, a source peripheral field, a source slot int field, a
    destination peripheral field, an optional limit int field — name the source-peripheral field
    something other than the Python keyword `from`, e.g. `from_name`, since the function builds the wire
    args dict itself and can put it under `"from"` there); `MoveArgs` (device: str | None, dir:
    Literal["forward","back","up","down"], steps: int | None); `TurnArgs` (device: str | None, dir:
    Literal["left","right"]); `DigArgs` (device: str | None, dir: Literal["forward","up","down"] =
    "forward"); `InspectArgs` (device: str | None only); `RefuelArgs` (device: str | None, count: int |
    None). Each tool function resolves `device_id = args.device or default_worker()`, returns `{"ok":
    False, "error": "no turtle or computer connected"}` immediately when that is falsy (no exception),
    and otherwise returns `await send_cmd(device_id, "<tool name>", {<wire args>})` directly — the wire
    arg dict keys must match client.lua's primitive signatures exactly (e.g. push_one_slot's wire dict
    uses `from`/`slot`/`dest`/`limit`). Carry over each old `DEVICE_TOOLS` entry's `description` string
    as the new function's docstring — pydantic-ai derives the tool description from the docstring.
  </action>
  <acceptance_criteria>
    - `pydantic-ai-slim[anthropic]==2.46.0` present in pyproject.toml's dependencies and in uv.lock
    - agent.py contains no `DEVICE_TOOLS`, `LOCAL_TOOLS`, or `run_tool` identifiers
    - agent.py defines exactly 8 device-forwarding tool functions: status, list_chest, push_one_slot,
      move, turn, dig, inspect, refuel — each with its own Pydantic argument model
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'pydantic-ai-slim\[anthropic\]==2.46.0' turtle/turtle-helper/pyproject.toml && grep -q 'pydantic-ai' turtle/turtle-helper/uv.lock</automated>
    <fails_when>non-zero exit (dependency not pinned in pyproject.toml or not resolved into uv.lock)</fails_when>
  </verify>
  <verify>
    <automated>F=turtle/turtle-helper/bridge/agent.py; ! grep -q 'DEVICE_TOOLS' "$F" && ! grep -q 'def run_tool' "$F" && test "$(grep -c '^async def ' "$F")" -ge 8</automated>
    <fails_when>non-zero exit, or fewer than 8 async device-tool functions found (a DEVICE_TOOLS/run_tool remnant, or a missing primitive)</fails_when>
  </verify>
  <done>All 8 device-forwarding primitives are typed pydantic-ai tool functions; the old JSON-schema dicts and dispatcher are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list_devices/say local tools, Agent construction, per-run toolset (D-09)</name>
  <files>turtle/turtle-helper/bridge/agent.py</files>
  <read_first>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/bridge/bridge.py</read_first>
  <behavior>
    - With zero devices connected, the built toolset contains only the always-available local tools
      (list_devices, say) — none of the 8 device-forwarding tools appear.
    - With one computer (non-turtle) worker connected advertising status/list_chest/push_one_slot, the
      toolset gains exactly those three device tools — move/turn/dig/inspect/refuel stay absent.
    - With a turtle connected (all 8 caps advertised), the toolset gains all 8 device-forwarding tools.
  </behavior>
  <action>
    In turtle/turtle-helper/bridge/agent.py, add `list_devices` and `say` as always-available local tool
    functions (no Pydantic args model needed for `list_devices`; `say`'s args model mirrors the old
    schema: `text: str` required, `to: str | None` for a private whisper) — carry over their exact
    behavior from the old `run_tool`'s `"list_devices"`/`"say"` branches (list_devices returns `{d:
    {"role": v["role"], "caps": v["caps"]} for d, v in devices.items()}`; say calls the injected `say()`
    callable and returns `{"ok": True}`).

    Add `def build_toolset() -> FunctionToolset`: construct a `FunctionToolset` seeded with
    `list_devices` and `say` unconditionally, then for each of the 8 device tools from Task 1, add it
    only if at least one entry in the injected `devices` dict has that tool's name in its `"caps"` list
    (D-09) — this is a plain membership check per tool, not a single all-or-nothing gate, so e.g.
    `status`/`list_chest`/`push_one_slot` can be present while `move`/`turn`/`dig`/`inspect`/`refuel`
    stay absent when no connected device is a turtle.

    Construct the pydantic-ai `Agent` once, inside `configure()` (called once from bridge.py's `main()`,
    signature unchanged): `AnthropicModel(new_settings.model, provider=AnthropicProvider(anthropic_client=
    new_client))`, with `instructions=system` (the existing formatted `SYSTEM_TEMPLATE`). Do not register
    any tools directly on the `Agent` — every tool reaches the model exclusively through
    `build_toolset()`'s per-run `toolsets=` argument (D-09's whole point is that the set is rebuilt fresh
    on every request from the live registry, not fixed at construction time). Store the constructed
    `Agent` in a module-level `agent: Agent[None, str]` variable `configure()` assigns.
  </action>
  <acceptance_criteria>
    - `build_toolset()` returns a `FunctionToolset` whose registered tool names, called with zero
      connected devices, are exactly `{"list_devices", "say"}`
    - `configure()` constructs `AnthropicModel(...)` via `AnthropicProvider(anthropic_client=...)`, not
      by constructing a second, separate Anthropic client
    - No tool is passed to the `Agent(...)` constructor's own `tools=` — only via `build_toolset()`
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - <<'PYEOF'
from bridge import agent as a
a.devices = {}
ts = a.build_toolset()
names = sorted(t.name for t in ts.tools.values()) if hasattr(ts, "tools") else sorted(getattr(ts, "_tools", {}).keys())
assert set(names) == {"list_devices", "say"}, names
print("TOOLSET_EMPTY_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or "TOOLSET_EMPTY_OK" absent (with no devices connected, a device-forwarding tool leaked into the toolset, or the FunctionToolset's internal attribute name differs from what this check assumes — inspect pydantic-ai's actual FunctionToolset shape if this attribute lookup fails and adjust)</fails_when>
  </verify>
  <done>list_devices/say are local tools; build_toolset() filters device tools by live caps; the Agent is constructed once inside configure() with no bridge.py changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: New handle_request() on agent.run(), per-player message_history</name>
  <files>turtle/turtle-helper/bridge/agent.py</files>
  <read_first>turtle/turtle-helper/bridge/agent.py</read_first>
  <behavior>
    - `handle_request(user, text)` keeps the exact same call signature `bridge.py`'s `on_event` already
      uses — no bridge.py change is needed for this plan.
    - A second request from the same player includes their prior turns in `message_history=`; a request
      from a different player never sees another player's history (per-player isolation, matching the
      old `histories` dict's per-user keying).
    - An exception raised anywhere inside `agent.run()` (a genuinely unexpected bug, not a normal tool
      failure) is caught by `bridge.py`'s existing `on_event` try/except and never corrupts the stored
      history for that player — the fix is structural: history is captured from `result.all_messages()`
      only AFTER a successful `agent.run()` returns, so a mid-run failure simply leaves that player's
      history exactly as it was before the failed request (closes WR-03; pydantic-ai's own tool-argument
      validation and internal tool_use/tool_result pairing removes the failure mode WR-03 originally
      named).
  </behavior>
  <action>
    Replace `agent.py`'s `histories`/`MAX_TURNS`/old `handle_request` with: a module-level
    `histories: dict[str, list[ModelMessage]] = {}` (now storing pydantic-ai's `ModelMessage` objects,
    not plain dicts); a new `async def handle_request(user: str, text: str) -> None` that builds
    `toolset = build_toolset()`, calls `result = await agent.run(text, message_history=histories.get(
    user, []), toolsets=[toolset])`, then trims and stores `histories[user] = result.all_messages()[
    -40:]` (or an equivalent bounded trim — exact cap and whether to trim by message count vs. a
    pydantic-ai history processor is Claude's discretion per CONTEXT.md, but it must never leave the
    history in a state pydantic-ai itself would reject on the next `agent.run()` call, so prefer trimming
    only on message-list boundaries pydantic-ai's own history format returns, not by re-slicing inside a
    request/response pair). `handle_request` does not directly call `say()` on the final result — the
    model calls the `say` tool itself as part of `agent.run()`'s tool loop, per the unchanged
    `SYSTEM_TEMPLATE` instruction ("Finish every task with exactly one say() ...").

    Run `uv run ruff check bridge/` and `uv run mypy bridge/` and fix anything either flags.
  </action>
  <acceptance_criteria>
    - `handle_request(user: str, text: str) -> None` signature unchanged from Phase 1
    - `histories` is per-player keyed and only updated after `agent.run()` returns successfully (no
      update inside an except branch or before the await resolves)
    - `uv run ruff check bridge/` and `uv run mypy bridge/` both pass clean
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run ruff check bridge/ && uv run mypy bridge/</automated>
    <fails_when>non-zero exit from either command, or any error line in the output</fails_when>
  </verify>
  <verify>
    <automated>grep -q 'async def handle_request(user: str, text: str) -> None' turtle/turtle-helper/bridge/agent.py</automated>
    <fails_when>non-zero exit (handle_request's signature changed, which would require a bridge.py edit this plan does not make)</fails_when>
  </verify>
  <done>handle_request runs the pydantic-ai Agent with per-player message history; ruff and mypy are clean.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- 8 typed device-forwarding tool functions in `bridge/agent.py`: `status`, `list_chest`,
  `push_one_slot`, `move`, `turn`, `dig`, `inspect`, `refuel` — each with its own Pydantic argument
  model (`StatusArgs`, `ListChestArgs`, `PushOneSlotArgs`, `MoveArgs`, `TurnArgs`, `DigArgs`,
  `InspectArgs`, `RefuelArgs`).
- `list_devices`, `say` local tool functions (`SayArgs` model for `say`).
- `build_toolset() -> FunctionToolset` — D-09's per-run device-cap filtering function.
- Module-level `agent: Agent[None, str]` constructed inside `configure()`.
- New `handle_request(user: str, text: str) -> None` on `agent.run()`; `histories:
  dict[str, list[ModelMessage]]`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model tool-call arguments -> device commands | pydantic-ai validates every tool call's arguments against its Pydantic model before the function body runs, closing a class of malformed-call bugs the hand-rolled loop had no equivalent guard against. |
| New third-party dependency: pydantic-ai-slim[anthropic] | Supply-chain surface added this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05a | Tampering | `pydantic-ai-slim[anthropic]` package install | high | mitigate | RESEARCH.md's Package Legitimacy Audit already verified this package OK/Approved (official Pydantic product, healthy download rate, no ASSUMED/SUS flag) — no blocking-human checkpoint required per the package-legitimacy gate's own rule. |
| T-02-05b | Denial of Service | An unbounded per-player `histories` dict | medium | mitigate | Task 3's trim keeps each player's stored history bounded; unchanged risk profile from Phase 1's `MAX_TURNS` cap. |
| T-02-05c | Elevation of Privilege | The model calling a device tool that requires caps no connected device advertises | medium | mitigate | D-09: `build_toolset()` never exposes a tool unless a connected device's caps list names it — the model cannot even attempt the call. |
</threat_model>

<verification>
`uv run ruff check bridge/` and `uv run mypy bridge/` both pass clean. `build_toolset()` with an empty
device registry exposes only `list_devices`/`say`. `handle_request`'s call signature is unchanged from
Phase 1, so `bridge.py` needs no edits.
</verification>

<success_criteria>
`agent.py` runs entirely on typed pydantic-ai tools with per-run, cap-filtered toolset building; no
`DEVICE_TOOLS`/`LOCAL_TOOLS`/`run_tool` remnants remain; `bridge.py` is untouched.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-05-SUMMARY.md` when done
</output>
