---
phase: 02-fake-device-harness-protocol-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [turtle/turtle-helper/turtle/client.lua]
autonomous: true
requirements: [HARN-03]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "turtle/turtle-helper/turtle/client.lua exposes only CC:Tweaked primitives (status, list_chest, push_one_slot, plus turtle-only move/turn/dig/inspect/refuel and ALLOW_EVAL-gated run_lua) — no composition logic (sort_chest, rule management) remains on the device, per D-07"
    - "push_one_slot(args) wraps peripheral pushItems(toName, fromSlot, limit) and returns {moved=<int>}, giving Python's future sort_chest composition (Plan 02-06) the one primitive it needs beyond list_chest"
    - "The tools dispatch table, readFile/writeFile helpers, capabilities() and session() connection loop are unchanged in shape, so client.lua's wire behavior (hello, cmd/result, reconnect) is identical to Phase 1 except for the capability list content"
  artifacts:
    - turtle/turtle-helper/turtle/client.lua
  key_links:
    - "client.lua's final tools table is the single source of truth Plan 02-03's fake worker (D-03) mirrors and Plan 02-06's Python sort_chest composes against"
---

<objective>
Rewrite `turtle/turtle-helper/turtle/client.lua` per CONTEXT.md D-07: strip the sorting/rule-composition
logic (moving to Python in Plan 02-06) and add the one new primitive Python composition needs
(`push_one_slot`), leaving the device a thin, one-to-one wrapper over CC:Tweaked primitives.

Purpose: this is the foundation the rest of the phase reads from — Plan 02-03's fake worker harness
mirrors this exact tool set and canned-response shapes (D-03), and Plan 02-06's Python `sort_chest`
composition calls `list_chest` then `push_one_slot` the way the old Lua loop did internally. Getting
the primitive set and shapes right here, first, avoids every downstream plan re-deriving them.

Output: a syntax-valid, primitive-only `client.lua` committed to git (previously untracked; this task
performs its first `git add`), with the composition tools and `rules.json`/`loadRules`/`saveRules`/
`destFor` machinery removed entirely.

Execution model note: the author asked this phase's execution to run on the strongest available Claude
model ("throw fable at it" — CONTEXT.md `<specifics>`). GSD has no per-phase model override; if not
already raised, the operator can run `gsd config-set model_profile quality` (or pass a model explicitly)
before `/gsd-execute-phase 2`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove composition tools, add push_one_slot, update setup comment</name>
  <reversibility rating="costly">D-07 amends the "high-level tools live in Lua" principle; undoing it means re-deriving sort_chest/rule-management as Lua and reconnecting rules.json to the device — a coordinated change across this file and agent.py, not a local swap.</reversibility>
  <files>turtle/turtle-helper/turtle/client.lua</files>
  <read_first>turtle/turtle-helper/turtle/client.lua, turtle/turtle-helper/base/chat.lua</read_first>
  <action>
    Edit turtle/turtle-helper/turtle/client.lua per D-07. Remove entirely: the `RULES_FILE` constant,
    the module-level `local rules = {}` and `local overflow = nil` state, the `loadRules()` function,
    the `saveRules()` function, the `destFor(itemName)` helper, the top-level `loadRules()` call, and
    the four composition/rule-management tool functions currently named sort_chest, list_rules,
    add_rule, remove_rule, and set_overflow on the `tools` table. Update the file's header setup
    comment (lines 6-10) to drop the "optional: edit rules.json (created on first run) or use add_rule
    from chat" step — rules no longer live on the device (they move to the bridge in Plan 02-06 per
    D-08).

    Add one new primitive on the `tools` table: a push-one-slot wrapper matching the identifier
    `push_one_slot`, taking `args.from` (source peripheral name), `args.slot` (source slot number),
    `args.dest` (destination peripheral name), and optional `args.limit`. It wraps
    `peripheral.wrap(args.from)` (erroring "no inventory called " .. tostring(args.from) if nil, same
    style as the existing list_chest tool) and calls that peripheral's `pushItems(args.dest, args.slot,
    args.limit)`, returning a table with a single `moved` field set to that call's return value.

    Keep unchanged: the `readFile`/`writeFile` helpers, `TOKEN` loading, `log()`, the existing
    `tools.status` and `tools.list_chest` primitives, the turtle-only block (move/turn/dig/inspect/
    refuel, gated on `if turtle then`), the `ALLOW_EVAL`-gated run_lua tool, the `tools` dispatch table
    pattern itself, `capabilities()` (alphabetical `table.sort` over `pairs(tools)` — unchanged
    mechanism, different resulting list), `session()`, and the outer reconnect while-loop.

    Since `turtle/turtle-helper/turtle/client.lua` is untracked (present on disk, not gitignored —
    confirmed via `git check-ignore`), `git add` it as part of this task's commit so it becomes tracked
    for the first time this phase.
  </action>
  <acceptance_criteria>
    - client.lua contains `function tools.push_one_slot(args)` with a `pushItems` call and a `{moved =`
      return
    - client.lua does NOT contain `function tools.sort_chest`, `function tools.list_rules`,
      `function tools.add_rule`, `function tools.remove_rule`, or `function tools.set_overflow`
    - client.lua does NOT contain `RULES_FILE`, `loadRules`, `saveRules`, or `destFor`
    - `git status` shows client.lua staged/tracked after this task's commit
  </acceptance_criteria>
  <verify>
    <automated>git ls-files --error-unmatch turtle/turtle-helper/turtle/client.lua >/dev/null</automated>
    <fails_when>non-zero exit (file still untracked after this task's commit)</fails_when>
  </verify>
  <done>client.lua committed, tracked, with composition tools removed and push_one_slot added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Syntax-check the rewrite and lock the final primitive set</name>
  <files>turtle/turtle-helper/turtle/client.lua</files>
  <read_first>turtle/turtle-helper/turtle/client.lua</read_first>
  <action>
    No Lua runtime exists on this PC or in WSL (CLAUDE.md, CONTEXT.md gotchas). Verify the rewrite two
    ways: (1) a Lua-5.2-grammar syntax parse via the `luaparse` npm package run from a scratch
    directory (this catches syntax errors only — real in-game behavior is proven in Phase 4, not here);
    (2) structural grep assertions confirming the exact surviving primitive set and the removed
    composition functions are gone. Do not add a package.json or any node_modules to the turtle-helper
    tree — install luaparse in a throwaway `mktemp -d` directory outside the repo.
  </action>
  <acceptance_criteria>
    - luaparse parses client.lua with no thrown error (LUA_PARSE_OK printed)
    - Surviving primitives confirmed present: status, list_chest, push_one_slot, and (inside the
      `if turtle then` block) move, turn, dig, inspect, refuel
    - readFile, writeFile, and the run_lua tool (behind ALLOW_EVAL) are still present
  </acceptance_criteria>
  <verify>
    <automated>cd "$(mktemp -d)" && npm i luaparse --silent >/dev/null 2>&1 && node -e "require('luaparse').parse(require('fs').readFileSync(process.argv[1],'utf8'),{luaVersion:'5.2'}); console.log('LUA_PARSE_OK')" "$(git rev-parse --show-toplevel)/turtle/turtle-helper/turtle/client.lua"</automated>
    <fails_when>non-zero exit, or "LUA_PARSE_OK" absent from the output (luaparse throws on a syntax error before reaching the print)</fails_when>
  </verify>
  <verify>
    <automated>F=turtle/turtle-helper/turtle/client.lua; grep -q 'function tools.status' "$F" && grep -q 'function tools.list_chest' "$F" && grep -q 'function tools.push_one_slot' "$F" && grep -q 'function tools.move' "$F" && grep -q 'function tools.refuel' "$F" && ! grep -q 'function tools.sort_chest' "$F" && ! grep -q 'function tools.add_rule' "$F"</automated>
    <fails_when>non-zero exit (a required primitive is missing or a removed composition function is still present)</fails_when>
  </verify>
  <done>client.lua is syntactically valid and contains exactly the D-07 primitive set — no composition tools remain.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `tools.push_one_slot(args)` — new Lua primitive in `turtle/turtle-helper/turtle/client.lua`, wraps
  `pushItems(toName, fromSlot, limit)`, returns `{moved = <int>}`.
- The post-D-07 capability list itself (alphabetical: `dig, inspect, list_chest, move, push_one_slot,
  refuel, status, turn` on a turtle; `list_chest, push_one_slot, status` on a plain computer) — the
  contract Plan 02-03's fake worker and Plan 02-06's Python `sort_chest` composition both read from.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device (Lua) -> Bridge | The device is a dumb executor; the bridge is the only party that decides what tool to call and with what args (unchanged this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01a | Tampering | `push_one_slot`'s `args.from`/`args.dest` peripheral names | low | accept | Same trust model as the existing `list_chest` tool: the bridge (trusted, token-authenticated) supplies these names; a malformed name surfaces as a Lua `error()` caught by `session()`'s existing `pcall(tool, ...)` wrapper, returned as `{ok=false, error=...}` — no crash, no new surface introduced by this plan. |
| T-02-01b | Information Disclosure | `rules.json` removal | low | mitigate | Removing rule persistence from the device closes the (minor) exposure of sorting-rule data sitting in the world save; rules move to a git-ignored bridge-side file in Plan 02-06 (D-08). |
</threat_model>

<verification>
`client.lua` parses cleanly under `luaparse` (Lua 5.2 grammar) and contains exactly the D-07 primitive
set: status, list_chest, push_one_slot, and (turtle-only) move/turn/dig/inspect/refuel, plus run_lua
behind ALLOW_EVAL. No sorting or rule-management functions remain.
</verification>

<success_criteria>
`turtle/turtle-helper/turtle/client.lua` is tracked in git, syntactically valid, and thin — one-to-one
CC:Tweaked primitives only, matching D-07 exactly, ready for Plan 02-03's fake worker to mirror and
Plan 02-06's Python composition to call.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-01-SUMMARY.md` when done
</output>
