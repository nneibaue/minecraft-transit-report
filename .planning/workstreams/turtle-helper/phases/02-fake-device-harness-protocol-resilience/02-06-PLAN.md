---
phase: 02-fake-device-harness-protocol-resilience
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - turtle/turtle-helper/bridge/agent.py
  - turtle/turtle-helper/.gitignore
autonomous: true
requirements: [HARN-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "rules.json lives beside .env, resolved from the source file's location via pathlib the same way settings.py resolves .env — not from the process cwd"
    - "rules.json is git-ignored; a repo-wide search for the string 'rules.json' outside .gitignore/this plan's own docs finds no tracked file containing it"
    - "sort_chest is offered to the model only when a connected device advertises both list_chest and push_one_slot — never when no worker is connected"
    - "anthropic stays a direct pyproject.toml dependency, not purely transitive: bridge.py's own Phase-1 startup check (client.models.retrieve) still constructs and uses a raw anthropic.AsyncAnthropic client directly, independent of pydantic-ai wrapping it for the agent loop"
  artifacts:
    - turtle/turtle-helper/bridge/agent.py
    - turtle/turtle-helper/.gitignore
  key_links:
    - "sort_chest composes send_cmd(device_id, 'list_chest', ...) then send_cmd(device_id, 'push_one_slot', ...) per item, mirroring the exact loop client.lua's old sort_chest used to run on-device (Plan 02-01 removed it from Lua)"
    - "rules.json's add_rule/remove_rule/list_rules/set_overflow local tools are what Plan 02-07's CLAUDE.md/README amendments document as 'rules live on the bridge'"

user_setup: []
---

<objective>
Finish D-05's final plan: move sorting composition and rule persistence onto the bridge in Python
(D-07, D-08), closing the "high-level tools live in Lua" principle amendment CONTEXT.md flags, and do
the final cleanup pass (ruff, mypy, dependency check) that makes `agent.py` ready for Plan 02-07's docs
and post-swap paid proof.

Purpose: `sort_chest` is the one composed tool the old Lua used to run as an on-device loop
(`client.lua`'s removed `tools.sort_chest`, Plan 02-01); this plan is where that loop's logic is ported
to Python, composing the same two primitives (`list_chest`, `push_one_slot`) over the wire instead of
running on the device.

Output: `rules.json` load/save + `list_rules`/`add_rule`/`remove_rule`/`set_overflow` local tools;
`sort_chest` as a typed, cap-gated Python composition; `.gitignore` gains `rules.json`; a final clean
`ruff`/`mypy` pass across `bridge/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rules.json persistence, rule-management local tools (D-08)</name>
  <files>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/.gitignore</files>
  <read_first>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/turtle/client.lua</read_first>
  <behavior>
    - `list_rules` with no rules.json on disk yet returns an empty rule set and `overflow: null` rather
      than raising `FileNotFoundError`.
    - `add_rule` followed by `list_rules` in the same process shows the new rule; the on-disk
      `rules.json` reflects it too.
    - `rules.json` never appears in `git status`/`git add -A` output — it is git-ignored.
  </behavior>
  <action>
    In turtle/turtle-helper/bridge/agent.py, add a `rules_path` resolved the same way
    `settings.py`'s `model_config.env_file` is (`Path(__file__).resolve().parent.parent /
    "rules.json"` — beside `.env`, not the process cwd). Add `load_rules()`/`save_rules()` functions
    reading/writing a JSON object shaped `{"rules": [{"pattern": ..., "dest": ...}, ...], "overflow":
    <str-or-null>}`, defaulting to an empty rules list and `null` overflow when the file does not exist
    (mirrors the old Lua `loadRules()`'s missing-file fallback from before Plan 02-01 removed it). Add
    four local tool functions with Pydantic argument models: `list_rules` (no args, returns the loaded
    `{"rules": [...], "overflow": ...}`); `add_rule` (`pattern: str`, `dest: str`, appends and saves,
    returns `{"ok": True, "count": <int>}`); `remove_rule` (`pattern: str`, removes the first exact
    match and saves, returns `{"removed": bool}`); `set_overflow` (`dest: str`, saves, returns
    `{"overflow": dest}`). These four are always in `build_toolset()`'s base set alongside
    `list_devices`/`say` — never gated by connected-device caps, since they operate purely on the
    bridge-side file.

    Add `rules.json` to turtle/turtle-helper/.gitignore, on its own line alongside the existing entries.
  </action>
  <acceptance_criteria>
    - `load_rules()` called against a nonexistent path returns `{"rules": [], "overflow": None}` without
      raising
    - `.gitignore` contains a `rules.json` line
    - `add_rule`/`remove_rule`/`list_rules`/`set_overflow` are all present in `build_toolset()`'s
      unconditional base tool set
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^rules.json$' turtle/turtle-helper/.gitignore</automated>
    <fails_when>non-zero exit (rules.json is not git-ignored on its own line)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - <<'PYEOF'
import os
from bridge import agent as a
assert not os.path.exists(a.rules_path) or True
rules = a.load_rules()
assert rules.get("rules") == [] or isinstance(rules.get("rules"), list), rules
assert "overflow" in rules, rules
print("RULES_LOAD_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or "RULES_LOAD_OK" absent (load_rules raises on a missing file, or the returned shape lacks the expected keys)</fails_when>
  </verify>
  <done>rules.json load/save and the four rule-management local tools are implemented and always offered to the model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sort_chest — Python composition over list_chest + push_one_slot (D-07)</name>
  <reversibility rating="costly">D-07's Python-side half: moving the sort loop off the device means the bridge is now the only place that knows how to compose a sort; reverting means porting this loop back to Lua and re-adding rules.json on the device.</reversibility>
  <files>turtle/turtle-helper/bridge/agent.py</files>
  <read_first>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/turtle/client.lua</read_first>
  <behavior>
    - `sort_chest` appears in the toolset only when a connected device's caps include both `list_chest`
      and `push_one_slot` (D-09) — never when no worker is connected.
    - Given a `list_chest` result with N items and a loaded rule set, sort_chest issues one
      `push_one_slot` per item whose name matches a rule (first match wins, same as the old Lua loop),
      and returns `{"moved": <total pushed>, "no_rule": [...], "destination_full": [...]}` — items with
      no matching rule and no overflow go to `no_rule`; a push that moves fewer than the item's full
      count goes to `destination_full`, matching client.lua's pre-Plan-02-01 `tools.sort_chest` exactly.
  </behavior>
  <action>
    In turtle/turtle-helper/bridge/agent.py, add `SortChestArgs` (`device: str | None`, `from_name: str`
    — the source peripheral, required) and an `async def sort_chest(ctx, args: SortChestArgs) -> dict`
    tool function. Resolve `device_id = args.device or default_worker()`; if falsy, return `{"ok": False,
    "error": "no turtle or computer connected"}`. Call `await send_cmd(device_id, "list_chest", {"name":
    args.from_name})`; if that result's `"ok"` is falsy, return it directly (propagate the device-side
    error). For each item in the result's `"items"`, resolve its destination via the SAME first-match
    Lua-pattern logic the old `destFor()` helper used (re-implement in Python — a small loop over the
    loaded rules checking a pattern match against the item's `name`, falling back to `overflow` when no
    rule matches), then call `await send_cmd(device_id, "push_one_slot", {"from": args.from_name, "slot":
    item["slot"], "dest": dest, "limit": item["count"]})` when a destination was found; accumulate the
    `moved` count from each push's result, and items with no destination into `no_rule` (an item pushed
    for fewer than its full `count` goes to `destination_full` instead). Return the aggregated
    `{"moved": ..., "no_rule": [...], "destination_full": [...]}`.

    In `build_toolset()` (Plan 02-05), add `sort_chest` to the gated set: include it only when some
    connected device's `"caps"` contains BOTH `"list_chest"` and `"push_one_slot"` (not an
    all-devices-combined check — a single device must advertise both).
  </action>
  <acceptance_criteria>
    - `sort_chest`'s destination-matching re-implements the same first-match Lua-pattern semantics the
      removed `destFor()` used, not a different matching algorithm
    - `build_toolset()` with zero connected devices omits `sort_chest`; with one worker advertising
      both `list_chest` and `push_one_slot`, it is present
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'async def sort_chest' turtle/turtle-helper/bridge/agent.py && grep -q 'push_one_slot' turtle/turtle-helper/bridge/agent.py</automated>
    <fails_when>non-zero exit (sort_chest is missing, or it does not call push_one_slot)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - <<'PYEOF'
from bridge import agent as a
a.devices = {"w1": {"ws": None, "role": "computer", "caps": ["status", "list_chest", "push_one_slot"]}}
ts = a.build_toolset()
names = {t.name for t in ts.tools.values()} if hasattr(ts, "tools") else set(getattr(ts, "_tools", {}).keys())
assert "sort_chest" in names, names
a.devices = {}
ts2 = a.build_toolset()
names2 = {t.name for t in ts2.tools.values()} if hasattr(ts2, "tools") else set(getattr(ts2, "_tools", {}).keys())
assert "sort_chest" not in names2, names2
print("SORT_CHEST_GATING_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or "SORT_CHEST_GATING_OK" absent (sort_chest is offered with no capable device connected, or withheld when one is)</fails_when>
  </verify>
  <done>sort_chest is a working Python composition, correctly gated by live device caps, matching the removed Lua loop's exact return shape.</done>
</task>

<task type="auto">
  <name>Task 3: Final cleanup pass — dead code, dependency check, ruff/mypy</name>
  <files>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/pyproject.toml</files>
  <read_first>turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/pyproject.toml</read_first>
  <action>
    Confirm no dead code remains from the pre-swap hand-rolled loop in agent.py (Plan 02-05 already
    removed `DEVICE_TOOLS`/`LOCAL_TOOLS`/`run_tool`; re-check nothing referencing the old `MAX_TURNS`
    constant or a plain-dict `histories` shape survives). Confirm `anthropic` stays listed as a direct
    dependency in pyproject.toml's `dependencies` (not removed) — `bridge.py`'s own Phase-1 startup
    verification (`client.models.retrieve`) still constructs and uses a raw `anthropic.AsyncAnthropic`
    client directly, independent of `pydantic-ai` wrapping that same client instance for the agent loop;
    this settles the "anthropic only transitive" discretion item from CONTEXT.md as: no, it stays
    direct. Run `uv run ruff check bridge/` and `uv run mypy bridge/` and fix anything either flags.
  </action>
  <acceptance_criteria>
    - `anthropic` remains in pyproject.toml's `dependencies` list (not demoted or removed)
    - `uv run ruff check bridge/` and `uv run mypy bridge/` both pass clean on the fully-swapped agent.py
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run ruff check bridge/ && uv run mypy bridge/</automated>
    <fails_when>non-zero exit from either command, or any error line in the output</fails_when>
  </verify>
  <verify>
    <automated>grep -q '"anthropic==' turtle/turtle-helper/pyproject.toml</automated>
    <fails_when>non-zero exit (the direct anthropic dependency pin was removed)</fails_when>
  </verify>
  <done>agent.py is fully clean under ruff/mypy with no dead code from the pre-swap loop; anthropic stays a direct dependency.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- `rules_path`, `load_rules()`, `save_rules()` — rules.json persistence in `bridge/agent.py`.
- `list_rules`, `add_rule`, `remove_rule`, `set_overflow` local tool functions (always available).
- `SortChestArgs` model and `sort_chest` tool function (cap-gated on `list_chest` + `push_one_slot`).
- `rules.json` line added to `turtle/turtle-helper/.gitignore`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge-side rules.json | Now the single source of truth for sorting rules, replacing on-device storage; readable/writable only by the bridge process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06a | Information Disclosure | rules.json committed to git by accident | low | mitigate | `.gitignore` entry added in Task 1; verified via a direct grep in Task 1's verify. |
| T-02-06b | Tampering | sort_chest composing pushes from a stale/attacker-influenced rules.json | low | accept | Only the bridge process (trusted, holds the API key) reads/writes rules.json; it is not reachable from any device or player input. |
</threat_model>

<verification>
`uv run ruff check bridge/` and `uv run mypy bridge/` both pass clean on the fully-composed agent.py.
`sort_chest` is correctly cap-gated. `rules.json` is git-ignored.
</verification>

<success_criteria>
D-07/D-08 are fully implemented: composition lives in Python, rules persist on the bridge, and the
phase's "thin Lua, thick Python" architecture amendment is realized end to end. agent.py is ready for
Plan 02-07's docs and the post-swap paid proof.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-06-SUMMARY.md` when done
</output>

