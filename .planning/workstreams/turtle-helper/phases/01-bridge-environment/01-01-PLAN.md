---
phase: 01-bridge-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - turtle/turtle-helper/pyproject.toml
  - turtle/turtle-helper/uv.lock
  - turtle/turtle-helper/.gitignore
  - turtle/turtle-helper/.env.example
  - turtle/turtle-helper/bridge/__init__.py
  - turtle/turtle-helper/bridge/settings.py
  - turtle/turtle-helper/bridge/agent.py
  - turtle/turtle-helper/bridge/bridge.py
autonomous: true
requirements: [BRIDGE-01, BRIDGE-02, BRIDGE-03, BRIDGE-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "uv sync (after installing uv) produces a working .venv, and uv run bridge/bridge.py prints a 'listening on ws://127.0.0.1:8765' log line with no import errors"
    - "bridge/bridge.py's server code calls the current websockets asyncio server API with a single-argument handler; no reference to the deprecated module-level synchronous server constructor or its legacy module remains anywhere in bridge/"
    - "With BRIDGE_TOKEN unset, running the bridge exits immediately (exit code 1) with a one-line config error message and no Python traceback; with all required vars set, the startup log shows the resolved MODEL as claude-sonnet-5 (or the MODEL env override)"
    - "BRIDGE_TOKEN, ALLOWED_PLAYERS, and ANTHROPIC_API_KEY are read only via bridge/settings.py's pydantic-settings BaseSettings model, sourced from a git-ignored .env plus a committed .env.example; no secret value is hardcoded in bridge/*.py"
    - "bridge.py is split into settings.py, agent.py, and bridge.py under bridge/; importing any of the three causes no side effects (no os.environ reads, no AsyncAnthropic construction, no Settings() construction at import time)"
    - "ruff check bridge/ and mypy bridge/ both pass with zero violations/errors (verified in Task 3 of this plan)"
    - "Each device connection is handled by its own handler() coroutine; an exception while processing one device's event or command is caught within that connection's handling and does not crash the bridge process or other connected devices' sessions"
    - "An empty ALLOWED_PLAYERS value fails Settings validation (ValidationError, exit 1) rather than silently defaulting to everyone allowed"
    - statement: "ALLOWED_PLAYERS entries and BRIDGE_TOKEN comparisons use plain Python string equality (exact code-point match, no case-folding or Unicode normalization), consistent with Minecraft's ASCII-range usernames and hex-encoded bridge tokens"
      verification: backstop
  artifacts:
    - turtle/turtle-helper/pyproject.toml
    - turtle/turtle-helper/uv.lock
    - turtle/turtle-helper/.env.example
    - turtle/turtle-helper/.gitignore
    - turtle/turtle-helper/bridge/__init__.py
    - turtle/turtle-helper/bridge/settings.py
    - turtle/turtle-helper/bridge/agent.py
    - turtle/turtle-helper/bridge/bridge.py
  key_links:
    - "bridge/bridge.py main() builds Settings() and an AsyncAnthropic client, then calls agent.configure(settings, client, devices, send_cmd, say, default_worker) before serving"
    - "bridge/bridge.py main() -> websockets.asyncio.server.serve(handler, settings.host, settings.port, ping_interval=..., ping_timeout=...)"
    - "bridge/bridge.py on_event() -> bridge/agent.py handle_request(user, text)"
    - "bridge/agent.py run_tool() -> the send_cmd/say/default_worker/devices objects injected from bridge.py via configure()"
  prohibitions: []
---

<objective>
Get `bridge.py` onto solid ground: a pinned, uv-managed Python environment; configuration sourced only from the environment through a typed, validated `Settings` model; the current `websockets` asyncio server API; and the starter split into `settings.py` / `agent.py` / `bridge.py` so Phase 2's harness has a clean seam to import. Nothing in this plan touches the game, the harness, or the agent loop's behavior — it is a refactor and environment migration only.

Purpose: This is the foundation every later phase connects to (ROADMAP Phase 1 goal). Phase 2's fake device harness imports `bridge/settings.py`; Phase 2 also replaces `agent.py`'s internals with Pydantic AI (D-14) without touching `bridge.py`, which is only possible if the module boundary drawn here is right.
Output: A working `uv run bridge/bridge.py` that validates config, verifies the model/key is real (added in Task 2), and listens on `ws://127.0.0.1:8765`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/ROADMAP.md
@.planning/workstreams/turtle-helper/STATE.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-RESEARCH.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-PATTERNS.md
@turtle/turtle-helper/CLAUDE.md
@turtle/turtle-helper/bridge/bridge.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: uv environment + typed Settings + the settings.py/agent.py/bridge.py split, listening on the new websockets API</name>
  <files>turtle/turtle-helper/pyproject.toml, turtle/turtle-helper/uv.lock, turtle/turtle-helper/.gitignore, turtle/turtle-helper/.env.example, turtle/turtle-helper/bridge/__init__.py, turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/bridge/bridge.py</files>
  <read_first>
    turtle/turtle-helper/bridge/bridge.py (the current single-file starter being split; note lines 26-34 config reads, 41-70 send_cmd/say/default_worker, 76-175 DEVICE_TOOLS/LOCAL_TOOLS/SYSTEM/run_tool/handle_request, 179-231 handler/on_event/main)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md (D-01 through D-18, especially D-01/D-02/D-03/D-04/D-05/D-06/D-07/D-09/D-10/D-13/D-14/D-15/D-16/D-17/D-18)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-PATTERNS.md (settings.py / agent.py / bridge.py pattern assignments, exact field lists and code shape)
    .planning/workstreams/turtle-helper/research/STACK.md (Development Environment Setup, pyproject.toml structure, websockets migration snippet)
  </read_first>
  <precondition>This machine has internet access to install uv (winget or the official installer script) and to download packages via uv sync from PyPI.</precondition>
  <action>
    Install uv per D-10: try `winget install --id astral-sh.uv --exact --scope user` first; if winget is unavailable or the command fails, fall back to the official PowerShell installer (`powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"`, documented in research/STACK.md). Confirm with `uv --version`. If the new binary is not yet on the current shell's PATH, locate it (commonly under the user profile's local bin directory or the winget shim path) and export its directory onto PATH for the rest of this session.

    Create turtle/turtle-helper/pyproject.toml per D-09/D-10/D-11: a [project] table with name "turtle-helper", version "0.1.0", requires-python ">=3.12", dependencies listing websockets 17.1, anthropic 1.7.0, and pydantic-settings 2.15.0 pinned exactly; a [dependency-groups] dev group listing ruff 0.5.1 and mypy 1.14.0 pinned exactly; [tool.ruff] with line-length 100 and target-version "py312", and [tool.ruff.lint] select = ["E", "F", "W", "I", "N", "UP", "B", "C4"]; [tool.mypy] with python_version "3.12", disallow_untyped_defs = true, warn_return_any = true, warn_unused_configs = true. Per Claude's Discretion, do not add a [project.scripts] entry this phase — uv run bridge/bridge.py stays the only documented invocation, keeping pyproject.toml minimal.

    Deliberate deviation from research/STACK.md's sample pyproject.toml, verified empirically during planning: also add a [build-system] table (requires = ["hatchling>=1.27"], build-backend = "hatchling.build") and [tool.hatch.build.targets.wheel] packages = ["bridge"]. Without this, uv sync treats the project as dependency-only (non-installable), and running bridge/bridge.py directly as a script then raises ModuleNotFoundError on any absolute import of the bridge package, because the project root never lands on sys.path — confirmed by testing plain CPython's script-execution sys.path behavior in this planning session. The explicit packages key is required because the package directory (bridge/) does not match the project name (turtle-helper), so hatchling's own name-based auto-detection would not find it. If Check A in the verify block below still fails after this, the fallback (do not use relative imports in bridge.py, since it always runs as __main__) is to insert `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` at the very top of bridge.py, before the bridge.* imports.

    Run `uv sync` from turtle/turtle-helper/ to create .venv and generate uv.lock; commit the lockfile. Create turtle/turtle-helper/bridge/__init__.py as an empty package marker (one-line module docstring is fine, no logic).

    Create turtle/turtle-helper/bridge/settings.py per D-01/D-02/D-03/D-04/D-16. Import Field, BeforeValidator, and ValidationError from pydantic, and BaseSettings, SettingsConfigDict from pydantic_settings — this corrects a mistake in RESEARCH.md's sample import (BaseSettings lives only in the pydantic-settings package under Pydantic v2, never in pydantic itself). Add `from __future__ import annotations` and a one-line module docstring. Define a module-level function (for example `split_comma_separated(value: object) -> list[str]`) that: for a str input, splits on commas, strips whitespace, and drops empty entries; for a list input, returns it unchanged; otherwise returns an empty list. Define a type alias combining `Annotated[list[str], BeforeValidator(...)]` with that function (for example `CommaSeparatedPlayers`). Define `class Settings(BaseSettings):` with every field carrying `Field(description=...)`: host: str (default "127.0.0.1"), port: int (default 8765), model: str (default "claude-sonnet-5"), command_prefix: str (default "$robot"), robot_name: str (default "Robot"), cmd_timeout: int (default 120), ping_interval: int (default 20), ping_timeout: int (default 20), bridge_token: str (required — no default), allowed_players: CommaSeparatedPlayers (required — no default and no default_factory, so a missing key raises ValidationError per D-03), anthropic_api_key: str (required — no default). Set `model_config = SettingsConfigDict(env_file=Path(__file__).resolve().parent.parent / ".env", env_file_encoding="utf-8", case_sensitive=False, extra="ignore")` per D-02 (resolved from the source file's own location, not the current working directory).

    Create turtle/turtle-helper/bridge/agent.py per D-13/D-14/D-16. Move DEVICE_TOOLS, LOCAL_TOOLS, histories, MAX_TURNS, run_tool(), and handle_request() from the current bridge/bridge.py unchanged in logic, adding full type hints and `from __future__ import annotations`. The hand-rolled tool-use loop stays as-is per D-14 — the Pydantic AI rewrite is out of scope this phase and lands in Phase 2. Replace the current module-level SYSTEM f-string (which reads the old ROBOT_NAME constant at import time — a side effect D-13 forbids) with a plain string template holding a `{robot_name}` placeholder in place of the old interpolated value, plus a module-level annotated-only global `system: str` (type annotation only, no value, so importing the module has zero side effects). Declare these additional module-level annotated-only globals for the same reason: `settings: Settings`, `client: anthropic.AsyncAnthropic`, `devices: dict[str, dict[str, object]]`, and typed callables for `send_cmd`, `say`, and `default_worker` (use `typing.Callable`/`typing.Awaitable`). Add `def configure(new_settings, new_client, new_devices, new_send_cmd, new_say, new_default_worker) -> None:` (typed per the globals above) that does `global settings, client, devices, send_cmd, say, default_worker, system` and assigns each parameter, plus formats `system` from the template using `new_settings.robot_name`. bridge.py calls this once from main() to hand values across the module boundary without agent.py ever importing bridge.py (breaking what would otherwise be a circular import, since bridge.py imports handle_request from agent.py). Update handle_request()'s model call to read `model=settings.model` and `system=system` (the module globals configure() populated) instead of the removed constants, and to call through `client.messages.create(...)` instead of the removed module-level client object. run_tool()'s body is unchanged — it already only references devices, say, default_worker, and send_cmd by name, which now resolve to the injected globals.

    Rewrite turtle/turtle-helper/bridge/bridge.py per D-05/D-06/D-07/D-13/D-15/D-16/D-17/D-18 (D-08's model/key verification is added in Task 2 of this plan, not here). Replace the current top-of-file env reads and the import-time Anthropic client construction with `from bridge.settings import Settings` and `from bridge import agent` — absolute imports, because bridge.py always runs directly as __main__ via `uv run bridge/bridge.py`, and a relative import (`from .settings import Settings`) fails in that context with an "attempted relative import with no known parent package" error; agent.py may use either style but this plan keeps both modules on absolute imports for consistency. Declare module-level annotated-only globals `settings: Settings` and `client: anthropic.AsyncAnthropic` (no value, so import has no side effects). Replace the plain `import websockets` with `from websockets.asyncio.server import serve` and `from websockets.exceptions import ConnectionClosed`; under `if TYPE_CHECKING:` import `ServerConnection` from the same asyncio server module. Keep `devices: dict[str, dict[str, object]] = {}` and `pending: dict[str, asyncio.Future[dict[str, object]]] = {}` as module-level dicts, names unchanged per D-16. Keep send_cmd(), say(), default_worker(), handler(), and on_event() with their existing bodies and names, changing only: the token comparison in handler() to `settings.bridge_token`; handler()'s parameter to a single `websocket: ServerConnection` argument per D-17 (rename internal `ws` references to `websocket` or alias once at the top of the function); send_cmd()'s timeout value to `settings.cmd_timeout`; say()'s prefix value to `settings.robot_name`; on_event()'s prefix and allow-list checks to `settings.command_prefix.lower()` and `settings.allowed_players`, and its dispatch call to `agent.handle_request(user, request)`; the connection-closed exception handler to the imported `ConnectionClosed` (not a `websockets.`-prefixed attribute access). send_cmd() itself moves unchanged per D-15 — the ConnectionClosed/leaked-pending-future fix is Phase 2's RESIL-03, not this phase. Rewrite `async def main() -> None:` to: build `Settings()` inside a try/except ValidationError block that prints one line per error (iterate `exc.errors()`, print each error's `loc[0]` and `msg`) then `raise SystemExit(1)` on failure, no traceback, per D-03; on success assign the module globals `settings` and `client` (build the AsyncAnthropic client from `settings.anthropic_api_key`); log a config summary at INFO covering resolved model, host:port, command_prefix, allowed_players, ping_interval/ping_timeout, and which .env file was loaded (or "none" if it does not exist) per D-07, plus a separate line reporting bridge_token as only "set" or "NOT SET" — never its value — and never logging anthropic_api_key in any form; call `agent.configure(settings, client, devices, send_cmd, say, default_worker)`; then serve with `async with serve(handler, settings.host, settings.port, ping_interval=settings.ping_interval or None, ping_timeout=settings.ping_timeout or None):` per D-05/D-06, log "listening on ws://{host}:{port}" at INFO, and `await asyncio.Future()`. Keep `if __name__ == "__main__": asyncio.run(main())` at the bottom. Update the module docstring at the top of bridge.py (the current install/env-var comment block) to point at turtle/turtle-helper/README.md's "Setup > 1. Bridge" section for the real recipe instead of duplicating install commands in two places, and drop the inline optional-env-vars comment's outdated default model mention since Task 1's settings.py is now the single source of truth for defaults.

    Create turtle/turtle-helper/.env.example (committed) listing every Settings field as an uppercase env var with a short comment, matching research/ARCHITECTURE.md section 4.2's key list: HOST, PORT, MODEL (showing claude-sonnet-5 as the example value), ANTHROPIC_API_KEY (comment pointing at the Anthropic console), BRIDGE_TOKEN (comment with the token-generation one-liner `python -c "import secrets; print(secrets.token_hex(24))"`), ALLOWED_PLAYERS (comma-separated, with an example), COMMAND_PREFIX, ROBOT_NAME, CMD_TIMEOUT, PING_INTERVAL, PING_TIMEOUT.

    Create turtle/turtle-helper/.gitignore (Claude's discretion, placed at the turtle-helper root rather than the repo's Gradle-focused root .gitignore) listing: .env, .venv/, __pycache__/, *.pyc, .ruff_cache/, .mypy_cache/.
  </action>
  <reversibility rating="costly">Settings.py's field shape (D-01) and the three-module split (D-13) become the config and import contract Phase 2's harness and the Pydantic AI agent.py replacement build on; changing either later touches multiple files across phases.</reversibility>
  <verify>
    <automated>cd turtle/turtle-helper && uv sync && uv run python -c "from bridge.settings import Settings; from bridge import agent; print('IMPORT_OK')"</automated>
    <fails_when>output does not contain IMPORT_OK, or output contains ModuleNotFoundError, ImportError, or Traceback</fails_when>

    <automated>! grep -rEq 'websockets\.(serve|legacy)' turtle/turtle-helper/bridge/</automated>
    <fails_when>non-zero exit (a match was found, meaning a legacy server reference remains in bridge/)</fails_when>

    <automated>grep -q 'from websockets.asyncio.server import serve' turtle/turtle-helper/bridge/bridge.py</automated>
    <fails_when>non-zero exit (the current asyncio server import line is missing)</fails_when>

    <automated>cd turtle/turtle-helper && env -u BRIDGE_TOKEN -u ANTHROPIC_API_KEY -u ALLOWED_PLAYERS bash -c 'uv run bridge/bridge.py; echo "EXIT_CODE:$?"' 2>&1 | tail -10</automated>
    <fails_when>output does not contain EXIT_CODE:1, or output contains Traceback</fails_when>

    <automated>cd turtle/turtle-helper && env BRIDGE_TOKEN=test-token-0001 ALLOWED_PLAYERS=TestPlayer ANTHROPIC_API_KEY=sk-ant-test-dummy-0001 bash -c 'uv run bridge/bridge.py; echo "EXIT_CODE:$?"' 2>&1 | tail -10</automated>
    <fails_when>output does not contain EXIT_CODE:1, or output contains Traceback (an empty ALLOWED_PLAYERS must fail Settings validation, not crash)</fails_when>

    <automated>cd turtle/turtle-helper && ( env BRIDGE_TOKEN=test-token-0001 ALLOWED_PLAYERS=TestPlayer ANTHROPIC_API_KEY=sk-ant-test-dummy-0001 uv run bridge/bridge.py > /tmp/bridge-tracer.log 2>&1 & echo $! > /tmp/bridge-tracer.pid ); sleep 6; kill "$(cat /tmp/bridge-tracer.pid)" 2>/dev/null; cat /tmp/bridge-tracer.log</automated>
    <fails_when>output does not contain "listening on ws://127.0.0.1:8765", or output contains Traceback</fails_when>

    <automated>grep -q 'claude-sonnet-5' turtle/turtle-helper/bridge/settings.py && ! grep -rq 'claude-sonnet-4-5' turtle/turtle-helper/bridge/</automated>
    <fails_when>non-zero exit (either the new default string is missing from settings.py, or the old placeholder string is still present somewhere in bridge/)</fails_when>

    <automated>! grep -rn 'os\.environ' turtle/turtle-helper/bridge/bridge.py turtle/turtle-helper/bridge/agent.py</automated>
    <fails_when>non-zero exit (a direct environment-variable read remains outside settings.py)</fails_when>
  </verify>
  <acceptance_criteria>
    - turtle/turtle-helper/bridge/settings.py defines `class Settings(BaseSettings):` with all eleven fields listed above, `bridge_token`/`allowed_players`/`anthropic_api_key` carrying no default
    - turtle/turtle-helper/bridge/agent.py contains `def configure(` and no longer contains a module-level `claude = anthropic` client construction or a module-level `os.environ` read
    - turtle/turtle-helper/bridge/bridge.py contains `def configure(` is NOT required there (bridge.py sets its own globals directly in main()), but bridge.py does contain `agent.configure(`
    - `uv run bridge/bridge.py` with a missing BRIDGE_TOKEN exits 1 with a one-line message and no traceback; with a full, well-formed (if dummy) config it logs a listening line on ws://127.0.0.1:8765
    - turtle/turtle-helper/.env.example lists all eleven Settings keys in uppercase with the token-generation one-liner in a comment
    - turtle/turtle-helper/.gitignore excludes .env and .venv/
  </acceptance_criteria>
  <done>bridge/ is split into settings.py, agent.py, bridge.py; the bridge validates config, fails fast on a missing token, and listens on the new websockets API from a uv-managed environment.</done>
</task>

<task type="auto">
  <name>Task 2: Startup verification — fail fast on bad Anthropic credentials, warn-and-listen on connection issues</name>
  <files>turtle/turtle-helper/bridge/bridge.py</files>
  <read_first>
    turtle/turtle-helper/bridge/bridge.py (as rewritten by Task 1 of this plan — main()'s current shape)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-RESEARCH.md ("Pattern 3: Startup Verification Before Serving", and the D-08 exception-class confirmation in "User Constraints")
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md (D-08)
  </read_first>
  <precondition>This machine has internet access to api.anthropic.com, so the deliberately-invalid-key verify below reaches a real authentication response instead of failing at DNS/connect.</precondition>
  <!--
    Expected behavior this task adds (no pytest this phase per D-11/RESEARCH.md Validation Architecture —
    verified by the automated shell check below plus the Manual-Only entries in this plan's <verification>):
    - A deliberately invalid ANTHROPIC_API_KEY causes an AuthenticationError from models.retrieve, which
      must be fatal: log one line at ERROR, raise SystemExit(1), no traceback.
    - An unknown MODEL value (valid key) causes a NotFoundError, which must be fatal the same way, naming
      the bad model in the message.
    - A connection/timeout error (APIConnectionError) must NOT be fatal: log one line at WARNING and
      continue to agent.configure(...) and serve(...) as normal.
    - A successful retrieve logs one INFO line confirming the model, then continues exactly as Task 1 left
      main() (configure + serve).
  -->
  <action>
    In bridge.py's main(), immediately after building `client = anthropic.AsyncAnthropic(api_key=settings.anthropic_api_key)` and before calling `agent.configure(...)`/`serve(...)`, add a token-free startup check per D-08: `await client.models.retrieve(settings.model)` inside a try block. On `anthropic.AuthenticationError`: log at ERROR a one-line message ("invalid or revoked ANTHROPIC_API_KEY" or similar) and `raise SystemExit(1)` — no traceback, matching the D-03 style already established in Task 1. On `anthropic.NotFoundError`: log at ERROR a one-line message naming `settings.model` as unknown and `raise SystemExit(1)`. On `anthropic.APIConnectionError`: log at WARNING a one-line message noting the connection issue and that the bridge will still listen; do not raise or exit — fall through to `agent.configure(...)` and `serve(...)` exactly as Task 1 left them. On success, log at INFO that the model was verified, naming `settings.model`. `anthropic` is already imported in bridge.py for `AsyncAnthropic`; these three exception classes live on the same top-level `anthropic` module.
  </action>
  <verify>
    <automated>cd turtle/turtle-helper && env BRIDGE_TOKEN=test-token-0001 ALLOWED_PLAYERS=TestPlayer ANTHROPIC_API_KEY=sk-ant-invalid-deliberately-bad-0001 bash -c 'uv run bridge/bridge.py; echo "EXIT_CODE:$?"' 2>&1 | tail -15</automated>
    <fails_when>output does not contain EXIT_CODE:1, or output contains Traceback (an invalid key must produce a one-line fatal message, not a raw exception)</fails_when>
  </verify>
  <acceptance_criteria>
    - bridge.py's main() contains a `client.models.retrieve(` call, awaited, wrapped in a try block
    - the try block catches `anthropic.AuthenticationError` and `anthropic.NotFoundError` as fatal (SystemExit(1)) and `anthropic.APIConnectionError` as a non-fatal warning
    - running the bridge with a deliberately invalid ANTHROPIC_API_KEY exits 1 with no traceback (verified above)
  </acceptance_criteria>
  <done>An invalid API key or unknown model stops the bridge at startup with a one-line message; a connection issue only warns, and the bridge keeps listening either way once past that point.</done>
</task>

<task type="auto">
  <name>Task 3: Lint and type-check bridge/ to green</name>
  <files>turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/bridge/agent.py, turtle/turtle-helper/bridge/bridge.py</files>
  <read_first>
    turtle/turtle-helper/pyproject.toml ([tool.ruff] and [tool.mypy] configuration created in Task 1 of this plan)
  </read_first>
  <action>
    Run `uv run ruff check bridge/` and `uv run mypy bridge/` from turtle/turtle-helper/. Fix every reported violation or error in settings.py, agent.py, and bridge.py (missing type hints, unused imports, import ordering per the I rule, naming per the N rule, etc.) without changing the behavior established in Task 1 and Task 2, until both commands report zero issues. Do not add a bare suppression comment to silence a real issue — fix the underlying code; a suppression is acceptable only for a genuine, documented false positive (for example a third-party stub gap), with a one-line comment explaining why.
  </action>
  <verify>
    <automated>cd turtle/turtle-helper && uv run ruff check bridge/</automated>
    <fails_when>non-zero exit, or output reports any violations (a clean run prints an all-clear line and exits 0)</fails_when>

    <automated>cd turtle/turtle-helper && uv run mypy bridge/</automated>
    <fails_when>non-zero exit, or output contains "error:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check bridge/` exits 0 with no reported violations
    - `uv run mypy bridge/` exits 0 with no reported errors
  </acceptance_criteria>
  <done>bridge/ is clean under the project's ruff and mypy configuration.</done>
</task>

</tasks>

## Artifacts this phase produces

- `turtle/turtle-helper/pyproject.toml` — new file: `[project]`, `[dependency-groups]`, `[build-system]`, `[tool.hatch.build.targets.wheel]`, `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.mypy]`
- `turtle/turtle-helper/uv.lock` — new generated lockfile
- `turtle/turtle-helper/.env.example` — new file
- `turtle/turtle-helper/.gitignore` — new file
- `turtle/turtle-helper/bridge/__init__.py` — new file (package marker)
- `turtle/turtle-helper/bridge/settings.py` — new file: `class Settings(BaseSettings)`, fields `host`, `port`, `model`, `command_prefix`, `robot_name`, `cmd_timeout`, `ping_interval`, `ping_timeout`, `bridge_token`, `allowed_players`, `anthropic_api_key`; function `split_comma_separated()`; type alias `CommaSeparatedPlayers`
- `turtle/turtle-helper/bridge/agent.py` — new file: moved `DEVICE_TOOLS`, `LOCAL_TOOLS`, `histories`, `MAX_TURNS`, `run_tool()`, `handle_request()`; new `SYSTEM_TEMPLATE`, module globals `settings`, `client`, `devices`, `send_cmd`, `say`, `default_worker`, `system`; new `configure()` function
- `turtle/turtle-helper/bridge/bridge.py` — rewritten in place: `devices`, `pending`, `send_cmd()`, `say()`, `default_worker()`, `handler()`, `on_event()`, `main()` (moved/adapted); new module globals `settings`, `client`

## Prohibition Recall (spec-less probe, PROHIB_ABSENT)

Stage 1 (adversarial recall) over BRIDGE-01..04 surfaced two candidates worth naming: (1) secrets silently
leaking into the config-summary log, and (2) an empty/misconfigured ALLOWED_PLAYERS silently defaulting
to "everyone allowed." Both are canon security concerns (OWASP Sensitive Data Exposure / Broken Access
Control) already owned by dedicated tooling — see `<threat_model>` T-1-01 and T-1-02 below — so per the
canon-referral rule they are breadcrumbed there rather than minted as bespoke `must_haves.prohibitions`
items. No other candidate survived Stage 2 precision (everything else was routine engineering already
covered by the edge predicates above or by Task 3's lint/type gate). Result: zero bespoke prohibitions
for this phase; `must_haves.prohibitions: []` in the frontmatter is this outcome, not a skipped step.

## Flagged Assumptions (spec-less probe, unclassified rows)

- **BRIDGE-01 (unclassified):** the probe did not classify a specific edge predicate for the pinned-environment
  requirement beyond the happy-path recipe proven in Task 1's verify block. Flagged for manual review during
  phase verification — e.g. confirm the documented recipe also reproduces on a second, clean machine, not
  only this one.
- **BRIDGE-03 (unclassified):** the probe did not classify a specific edge predicate for the model-ID default
  beyond the happy-path default. Flagged for manual review — e.g. confirm behavior if `MODEL` is set to an
  empty string in `.env` versus omitted entirely (both should currently fall back to the class default per
  pydantic-settings semantics, but this was not exercised by an automated check in this plan).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file -> Settings | Local file read by pydantic-settings at startup; git-ignored, holds BRIDGE_TOKEN/ANTHROPIC_API_KEY/ALLOWED_PLAYERS |
| Process environment -> Settings | Real environment variables override .env; a shared shell/CI log could echo them |
| Settings.anthropic_api_key -> Anthropic API | Secret used to authenticate the startup `models.retrieve` call and every later agent-loop call |
| bridge.py log output -> console | The config summary must describe the resolved config without leaking BRIDGE_TOKEN or ANTHROPIC_API_KEY values |
| Settings.host/port -> websockets.asyncio.server.serve() | Determines whether the server is reachable beyond this machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Information Disclosure | bridge.py main() config-summary log (Task 1) | high | mitigate | BRIDGE_TOKEN logged only as "set"/"NOT SET", never its value; ANTHROPIC_API_KEY never logged in any form, per D-07 |
| T-1-02 | Elevation of Privilege | bridge/settings.py Settings.allowed_players validation (Task 1) | high | mitigate | An empty or missing ALLOWED_PLAYERS raises ValidationError instead of silently allowing every player, per D-03; verified by an automated check in Task 1 |
| T-1-03 | Tampering (unwanted network exposure) | bridge/settings.py Settings.host default + serve() bind (Task 1) | medium | mitigate | HOST defaults to 127.0.0.1 (loopback-only); binding beyond localhost requires an explicit env override reserved for a later milestone, per D-05 |
| T-1-04 | Denial of Service (silent misconfiguration / wasted spend) | bridge.py startup model/key verification (Task 2) | medium | mitigate | `models.retrieve(MODEL)` runs once at startup; AuthenticationError/NotFoundError are fatal (exit 1) instead of letting the bridge run and fail on every real request, per D-08 |
| T-1-05 | Repudiation / log-spam | Anthropic connection-error handling at startup (Task 2) | low | mitigate | A connection/timeout error logs a single warning at startup; there is no per-tick retry loop in Phase 1 that could spam logs |
| T-1-SC | Tampering (supply chain) | uv-managed dependency install: websockets, anthropic, pydantic-settings, ruff, mypy (Task 1) | high | mitigate | RESEARCH.md's Package Legitimacy Audit already verified all five packages (official source repos, high weekly downloads, no ASSUMED/SUS/SLOP findings); no blocking human checkpoint required |
</threat_model>

<verification>
Task-level automated checks (above) cover: environment/import correctness, the legacy-API removal, fail-fast on missing/empty required config, the listening happy path, the model-ID default, no stray os.environ reads, the auth-failure fatal-exit path, and ruff/mypy cleanliness.

Manual-only (require a real ANTHROPIC_API_KEY the developer controls, or a way to simulate a dropped network connection — neither is reliably automatable from this plan):
- Unknown-MODEL path (`anthropic.NotFoundError`): with a real, valid ANTHROPIC_API_KEY, set `MODEL` to a made-up value and confirm the bridge exits 1 naming the bad model.
- Connection-error path (`anthropic.APIConnectionError`): with a real key, temporarily block outbound access to api.anthropic.com (or unplug networking) and confirm the bridge logs a warning and still reaches the listening state.
- Full happy path with a real key: confirm the config summary shows `model=claude-sonnet-5` (or the real MODEL override) and the "verified model" INFO line appears before the listening line.
</verification>

<success_criteria>
- `uv run bridge/bridge.py` from a fresh `uv sync` listens on `ws://127.0.0.1:8765` with a well-formed `.env`
- A missing or empty required config value exits fast with a one-line message, never a traceback
- No legacy `websockets.serve`/`websockets.legacy` reference remains anywhere in `bridge/`
- `bridge/` is split into `settings.py` / `agent.py` / `bridge.py`, each importable with zero side effects
- `ruff check bridge/` and `mypy bridge/` both pass clean
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-01-SUMMARY.md` when done
</output>
