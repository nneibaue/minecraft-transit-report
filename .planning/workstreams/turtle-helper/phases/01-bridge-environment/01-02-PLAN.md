---
phase: 01-bridge-environment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - turtle/turtle-helper/README.md
  - .planning/workstreams/turtle-helper/PROJECT.md
autonomous: true
requirements: [BRIDGE-01, BRIDGE-04]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "turtle/turtle-helper/README.md 'Setup > 1. Bridge' documents the uv install (winget primary, PowerShell installer fallback, Git Bash note), uv sync, the .env.example -> .env copy, and uv run bridge/bridge.py, replacing the old pip-based install block"
    - "turtle/turtle-helper/README.md Setup section documents that BRIDGE_TOKEN, ALLOWED_PLAYERS, and ANTHROPIC_API_KEY are set via the git-ignored .env file (copied from the committed .env.example), not shell export commands, closing out the documented-recipe half of BRIDGE-04"
    - ".planning/workstreams/turtle-helper/PROJECT.md Constraints section reflects the relaxed Python dependency footprint (websockets + anthropic + pydantic-settings, uv-managed, pydantic-ai arriving in Phase 2) and the Key Decisions table records the relaxation with rationale and date"
  artifacts:
    - turtle/turtle-helper/README.md
    - .planning/workstreams/turtle-helper/PROJECT.md
  key_links:
    - "README.md 'Setup > 1. Bridge' recipe names the same uv commands, .env.example keys, and run command that 01-01-PLAN.md's Task 1 actually builds"
  prohibitions: []
---

<objective>
Bring the two documentation artifacts this phase's decisions touch up to date: `turtle/turtle-helper/README.md`'s bridge setup instructions (now uv-based, per D-12), and `.planning/workstreams/turtle-helper/PROJECT.md`'s Constraints/Key Decisions (the Python dependency footprint was explicitly relaxed for this phase, per the CONTEXT.md "Constraint change" note). Independent of 01-01-PLAN.md's code changes — this plan documents decisions already fully specified in CONTEXT.md, not code discovered during implementation — so it runs in the same wave.

Purpose: BRIDGE-01's "documented... venv recipe" and BRIDGE-04's "documented, git-ignored local recipe" acceptance criteria are only satisfied once the recipe is actually written down where a developer will find it; PROJECT.md is GSD's own source of truth and must not silently drift from a locked-in-CONTEXT.md decision.
Output: An accurate README setup section and an accurate PROJECT.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-PATTERNS.md
@turtle/turtle-helper/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md "Setup &gt; 1. Bridge" with the uv + .env recipe</name>
  <files>turtle/turtle-helper/README.md</files>
  <read_first>
    turtle/turtle-helper/README.md (current "Setup > 1. Bridge" section, lines 39-48)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md (D-09, D-10, D-12)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-PATTERNS.md (README.md pattern section with the target recipe text)
  </read_first>
  <action>
    Replace README.md's "Setup > 1. Bridge" section (currently lines 39-48: a `cd bridge` line, an old pip-based dependency install line, raw shell `export` commands for the three secrets, and a bare `python bridge.py` invocation) with the uv-based recipe per D-09/D-10/D-12: a Prerequisites subsection naming uv (install via `winget install --id astral-sh.uv --exact` for PowerShell, with a Git Bash note pointing at the same winget command or the official installer script from research/STACK.md) and `uv --version` to verify; a Setup subsection with `cd turtle-helper`, `uv sync` (installs from the committed uv.lock, creates .venv), `cp .env.example .env`; a Configure subsection naming the three required keys to fill in — ANTHROPIC_API_KEY (from the Anthropic console), BRIDGE_TOKEN (via the `python -c "import secrets; print(secrets.token_hex(24))"` one-liner), ALLOWED_PLAYERS (comma-separated player names); a Run subsection with `uv run bridge/bridge.py` and a one-line note that it listens on :8765 by default and that HOST/PORT/MODEL and the other Settings fields can be overridden in .env. Remove the standalone tunnel/VPS "Dev"/"Prod" bullet points from this section per D-12, replacing them with a single one-line pointer noting that hosting and tunnels return in a later milestone. Keep the existing "Same box as the MC server" computercraft-server.toml `[[http.rules]]` snippet as-is — that is Phase 3 scope, not touched this phase. Do not touch the "2. In game" or "3. Talk to it" sections.
  </action>
  <verify>
    <automated>grep -q 'uv sync' turtle/turtle-helper/README.md && grep -q 'uv run bridge/bridge.py' turtle/turtle-helper/README.md && grep -q '.env.example' turtle/turtle-helper/README.md</automated>
    <fails_when>non-zero exit (one of the new uv/.env commands is missing from README.md's Setup section)</fails_when>

    <automated>! grep -Eq 'export (ANTHROPIC_API_KEY|BRIDGE_TOKEN|ALLOWED_PLAYERS)=' turtle/turtle-helper/README.md</automated>
    <fails_when>non-zero exit (a match was found — the old raw shell export instructions for a secret are still present)</fails_when>
  </verify>
  <acceptance_criteria>
    - README.md's "Setup > 1. Bridge" section contains `uv sync`, `cp .env.example .env`, and `uv run bridge/bridge.py`
    - README.md no longer instructs the reader to `export` a secret directly in the shell
    - the cloudflared/VPS Dev/Prod bullet list is replaced with a one-line pointer to a later milestone
    - the "Same box as the MC server" `computercraft-server.toml` snippet is unchanged
  </acceptance_criteria>
  <done>README.md's bridge setup section matches the uv + .env recipe this phase's decisions locked in.</done>
</task>

<task type="auto">
  <name>Task 2: Amend PROJECT.md Constraints and Key Decisions for the relaxed Python footprint</name>
  <files>.planning/workstreams/turtle-helper/PROJECT.md</files>
  <read_first>
    .planning/workstreams/turtle-helper/PROJECT.md (current Constraints and Key Decisions sections)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-CONTEXT.md (the "Constraint change (author's decision)" note under D-04, and D-09)
    .planning/workstreams/turtle-helper/phases/01-bridge-environment/01-PATTERNS.md (PROJECT.md pattern section with the target replacement text)
  </read_first>
  <action>
    In PROJECT.md's Constraints section, replace the "Dependencies" bullet (currently describing a two-package pip-install footprint with no framework) with wording reflecting the relaxed footprint per CONTEXT.md's "Constraint change" note: a uv-managed project with websockets, anthropic, and pydantic-settings as direct dependencies (pydantic itself arrives transitively via anthropic), pydantic-ai added in Phase 2; still no web framework or database; the file-count language changes from "one file" to the three-module bridge/settings.py / bridge/agent.py / bridge/bridge.py split decided in Phase 1 (D-13). If PROJECT.md's Context section elsewhere references a requirements.txt-based recipe, update that wording to uv.lock too, per D-09 (BRIDGE-01's requirements.txt wording is amended by D-09). In the Key Decisions table, add one new row: Decision names the relaxation of the Python dependency footprint and the drop of requirements.txt in favor of a uv-managed pyproject.toml/uv.lock; Rationale references BRIDGE-01's D-09 amendment (the lockfile replaces requirements.txt) and that pydantic-settings/pydantic-ai are worth the added dependency for typed config and the Phase 2 agent-loop rewrite; Outcome is "✓ Good — 2026-09-20" (matching CONTEXT.md's gathered date).
  </action>
  <verify>
    <automated>grep -q 'pydantic-settings' .planning/workstreams/turtle-helper/PROJECT.md && grep -q 'uv' .planning/workstreams/turtle-helper/PROJECT.md</automated>
    <fails_when>non-zero exit (the Constraints section was not updated to mention the relaxed footprint)</fails_when>

    <automated>! grep -q 'pip install. of two packages is the whole Python footprint' .planning/workstreams/turtle-helper/PROJECT.md</automated>
    <fails_when>non-zero exit (a match was found — the old two-package constraint sentence is still present verbatim)</fails_when>
  </verify>
  <acceptance_criteria>
    - PROJECT.md's Constraints "Dependencies" bullet names websockets, anthropic, and pydantic-settings, and mentions pydantic-ai arriving in Phase 2
    - PROJECT.md's Key Decisions table has a new row dated 2026-09-20 for the dependency-footprint relaxation
    - the old "pip install of two packages" sentence no longer appears verbatim
  </acceptance_criteria>
  <done>PROJECT.md's Constraints and Key Decisions reflect the footprint relaxation the author approved in CONTEXT.md, not the pre-phase two-package description.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan modifies two existing documentation files and introduces no new symbols (no classes, functions, CLI flags, or fields) — see 01-01-PLAN.md's "Artifacts this phase produces" section for the code-level artifacts this phase creates.

<verification>
Both tasks' automated checks are static greps over the two modified files; no runtime behavior to verify here. The genuine test of README.md's accuracy is that a developer following it end-to-end reaches the same "listening on ws://127.0.0.1:8765" state 01-01-PLAN.md's Task 1 verifies directly — covered there, not duplicated here.
</verification>

<success_criteria>
- README.md's "Setup > 1. Bridge" section is uv-based and matches the actual recipe built in 01-01-PLAN.md
- PROJECT.md no longer describes the pre-phase two-package, one-file Python footprint as current
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/01-bridge-environment/01-02-SUMMARY.md` when done
</output>
