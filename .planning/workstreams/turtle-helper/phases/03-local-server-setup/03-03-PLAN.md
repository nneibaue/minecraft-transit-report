---
phase: 03-local-server-setup
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [SRV-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 6
  confidence: low

must_haves:
  truths:
    - "The real computercraft-server.toml at <SERVER_DIR>/world/serverconfig/ carries a 127.0.0.1 allow rule positioned before the stock $private deny rule"
    - "insert_allow_rule() is idempotent against the real file: a second call in the same session makes no further change"
    - "The allow rule survives a full server restart (Forge's boot-time config rewrite preserves [[http.rules]] order, per RESEARCH.md assumption A4)"
  artifacts:
    - "<SERVER_DIR>/world/serverconfig/computercraft-server.toml (real server file, outside the repo, not git-tracked -- referenced here as goal-backward proof of SRV-01, not as a plan deliverable)"
  key_links:
    - "This plan's real toml edit is the prerequisite Plan 03-04's device onboarding and D-13 smoke check both assume is already in place"
---

<objective>
Apply Plan 03-01's deploy engine to the REAL dedicated server: configure the real `.env`'s `SERVER_DIR`,
confirm the server is stopped, insert the `127.0.0.1` allow rule into the real
`computercraft-server.toml`, and restart the server to prove the rule takes effect and survives Forge's
boot-time config rewrite.

Purpose: SRV-01's acceptance criterion is explicit that the allow rule must be applied to the real
server and proven with a real restart -- a temp-fixture proof (Plan 03-01) is necessary but not
sufficient. Every step here that needs the server stopped, restarted, or otherwise controlled is a
`checkpoint:human-action`, per the project's live-run rule: no executor starts, stops, or restarts the
Minecraft server.

Output: the real `<SERVER_DIR>/world/serverconfig/computercraft-server.toml` carries the allow rule,
confirmed present both immediately after insertion and again after a full server restart.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure SERVER_DIR in the real .env and verify Settings resolves it</name>
  <files>turtle/turtle-helper/.env (real, git-ignored local file -- not a repo-tracked deliverable)</files>
  <read_first>turtle/turtle-helper/.env.example, turtle/turtle-helper/bridge/settings.py</read_first>
  <action>
    Read the operator's real turtle/turtle-helper/.env (git-ignored, already contains BRIDGE_TOKEN,
    ALLOWED_PLAYERS, and the other Phase 1/2 keys). Check only for the presence of a line starting with
    "SERVER_DIR=" (e.g. via a count of matching lines) -- never print the full file contents, and never
    print any OTHER key's value while doing this check. If no SERVER_DIR line exists, append exactly one
    line: SERVER_DIR=C:\Users\nneib\Documents\Server-Files-1.1.1\Server-Files-1.1.1 (the confirmed real
    server root recorded in CONTEXT.md's inspection). If a SERVER_DIR line already exists with a
    non-empty value, leave the file unchanged and note the existing value instead of overwriting it.
  </action>
  <acceptance_criteria>
    - The real .env contains exactly one SERVER_DIR= line with a non-empty value after this task
    - No value from any OTHER .env key was printed while performing this check
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
from bridge.settings import Settings
s = Settings()
assert s.server_dir is not None, "server_dir not set"
assert str(s.server_dir).rstrip("\\/").endswith("Server-Files-1.1.1"), s.server_dir
print("SERVER_DIR_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or "SERVER_DIR_OK" absent (SERVER_DIR missing, empty, or pointing somewhere unexpected)</fails_when>
  </verify>
  <done>The real .env has a SERVER_DIR value, and Settings() resolves it correctly, with no other .env value printed.</done>
</task>

<task type="auto">
  <name>Task 2: Probe whether the dedicated server is currently running (read-only)</name>
  <files>none -- a read-only network probe</files>
  <read_first>turtle/turtle-helper/deploy/server_state.py</read_first>
  <action>
    Call deploy.server_state.is_server_running() (default host 127.0.0.1, mc_port 25565, 2-second
    timeout) against the real machine and print exactly one line reporting the boolean result. This is
    read-only -- a single TCP connect attempt, immediately closed -- and is therefore consistent with
    the project's live-run rule (probing is not controlling): it neither starts nor stops anything.
  </action>
  <acceptance_criteria>
    - The probe returns a boolean with no unhandled exception, and the result is printed plainly
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python -c "from deploy.server_state import is_server_running; print('SERVER_RUNNING:', is_server_running())"</automated>
    <fails_when>non-zero exit (the probe function itself raised instead of returning a bool)</fails_when>
  </verify>
  <done>The current running/stopped state of the dedicated server is known and printed for the next checkpoint to reference.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Checkpoint -- confirm the ATM9 server is stopped (D-12)</name>
  <action>Confirm the ATM9 dedicated server is stopped before I edit the CC:Tweaked allow rule (D-12).</action>
  <instructions>
    Task 2 just probed whether the ATM9 dedicated server is currently listening on port 25565.

    If it reported SERVER_RUNNING: true, please stop the server now using your normal method (closing
    the run.bat console window, or however you currently shut it down). Forge rewrites
    computercraft-server.toml at boot and holds the file open while running, so D-12 requires the server
    to be fully stopped before I write the allow rule -- editing it live risks a lost or corrupted write.

    If Task 2 reported SERVER_RUNNING: false, no action is needed.
  </instructions>
  <verification>I will re-probe port 25565 immediately before writing to the toml file, so a stale confirmation cannot slip through.</verification>
  <resume-signal>Type "stopped" (or "already stopped") once the server is confirmed down.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply the 127.0.0.1 allow rule to the real computercraft-server.toml</name>
  <files>&lt;SERVER_DIR&gt;/world/serverconfig/computercraft-server.toml (real, outside the repo)</files>
  <read_first>turtle/turtle-helper/deploy/rules.py, turtle/turtle-helper/deploy/server_state.py</read_first>
  <action>
    Re-check is_server_running(); if it now returns True (the operator's confirmation was stale, or the
    server restarted unexpectedly), STOP and report this back rather than writing -- do not proceed on a
    stale confirmation. If it returns False, call deploy.rules.insert_allow_rule(toml_path) against the
    REAL path built from Settings().server_dir joined with "world", "serverconfig",
    "computercraft-server.toml" -- this file write against the real server tree, performed only once the
    server is confirmed stopped, is an ordinary executor action, not a live-process operation. Read the
    file back afterward and confirm the "127.0.0.1" host line appears strictly before the "$private"
    host line (D-11's ordering), then call insert_allow_rule a second time immediately afterward and
    confirm it returns False with no further file change (idempotency proven against the real file, not
    just Plan 03-01's fixture).
  </action>
  <acceptance_criteria>
    - The real computercraft-server.toml contains "127.0.0.1" positioned before "$private"
    - A second insert_allow_rule call against the real file in the same session returns False
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
from bridge.settings import Settings
from deploy.server_state import is_server_running
from deploy.rules import insert_allow_rule

assert not is_server_running(), "server must be stopped before editing the toml"
s = Settings()
p = s.server_dir / "world" / "serverconfig" / "computercraft-server.toml"
first = insert_allow_rule(p)
second = insert_allow_rule(p)
content = p.read_text(encoding="utf-8")
assert content.index('host = "127.0.0.1"') < content.index('host = "$private"')
assert second is False, "second run should be a no-op"
print("REAL_TOML_RULE_OK", first, second)
PYEOF</automated>
    <fails_when>non-zero exit, "REAL_TOML_RULE_OK" absent, or the ordering assertion fails</fails_when>
  </verify>
  <done>The real computercraft-server.toml carries the 127.0.0.1 allow rule before the $private deny, applied idempotently.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Checkpoint -- restart the ATM9 server to apply the allow rule</name>
  <action>Restart the ATM9 server to apply the new allow rule.</action>
  <instructions>
    The allow rule is now written into the real config file. CC:Tweaked/Forge only reads this file at
    boot -- there is no hot reload (RESEARCH.md pitfall).

    Please restart the dedicated server now via your normal method (running run.bat), and wait for it to
    finish loading the world.
  </instructions>
  <verification>Once you confirm, I will re-probe port 25565 (read-only) to confirm the server came back up, and re-read the toml file to confirm the rule survived Forge's boot-time rewrite.</verification>
  <resume-signal>Type "restarted" once the server has finished loading.</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Confirm the server is back up and the rule survived the restart</name>
  <files>&lt;SERVER_DIR&gt;/world/serverconfig/computercraft-server.toml (real, read-only check)</files>
  <read_first>turtle/turtle-helper/deploy/server_state.py</read_first>
  <action>
    Re-run is_server_running(); expect True. Re-read the real toml file and confirm the "127.0.0.1" /
    "allow" pair is still present, still positioned before "$private" (Forge preserves [[http.rules]]
    order across its config rewrite, per RESEARCH.md assumption A4).
  </action>
  <acceptance_criteria>
    - is_server_running() returns True after the restart
    - The allow rule is present and correctly ordered in the toml file after the restart
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
from bridge.settings import Settings
from deploy.server_state import is_server_running

assert is_server_running(), "server did not come back up on port 25565"
s = Settings()
p = s.server_dir / "world" / "serverconfig" / "computercraft-server.toml"
content = p.read_text(encoding="utf-8")
assert content.index('host = "127.0.0.1"') < content.index('host = "$private"')
print("RESTART_SURVIVED_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or "RESTART_SURVIVED_OK" absent (server not back up, or the rule was lost/reordered by Forge's rewrite)</fails_when>
  </verify>
  <done>Server confirmed back up with the allow rule intact after a full restart -- SRV-01's restart-then-works requirement is proven.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- No new repo-tracked symbols. This plan applies Plan 03-01's already-produced `deploy.rules.
  insert_allow_rule` and `deploy.server_state.is_server_running` against the real, external
  `<SERVER_DIR>/world/serverconfig/computercraft-server.toml`, and updates the operator's local,
  git-ignored `.env` with a `SERVER_DIR` value.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> real server config | The executor may write to the real toml file only after a human-confirmed, re-verified "server stopped" state; it never starts, stops, or restarts the server process itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03a | Tampering | computercraft-server.toml edited against the real server | high | mitigate | The toml-edit task is gated behind an explicit human-confirmed "server stopped" checkpoint (D-12), and re-verifies is_server_running() itself immediately before writing -- a stale confirmation cannot slip through. |
| T-03-03b | Denial of Service | Server restart | medium | accept | Restarting the dedicated server is the operator's own action via their existing run.bat workflow; no new failure mode is introduced by this phase beyond what already existed. |
</threat_model>

<verification>
The real computercraft-server.toml carries the 127.0.0.1 allow rule before $private, applied only while
the server was confirmed stopped, proven idempotent against the real file, and confirmed to survive a
full server restart.
</verification>

<success_criteria>
SRV-01 is proven against the real dedicated server: the allow rule exists, is correctly ordered, was
applied without racing a live process, and survives a restart.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-03-SUMMARY.md` when done
</output>
