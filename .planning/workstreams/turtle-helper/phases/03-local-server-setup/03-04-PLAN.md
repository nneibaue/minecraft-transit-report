---
phase: 03-local-server-setup
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - turtle/turtle-helper/README.md
  - turtle/turtle-helper/CLAUDE.md
autonomous: false
requirements: [SRV-02, SRV-03, SRV-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "A real per-computer folder under <SERVER_DIR>/world/computercraft/computer/ contains chat.lua, client.lua, startup.lua, secret.txt, bridge.txt, and _marker.txt, confirmed present and byte-correct against the live server (SRV-02)"
    - "Rebooting the marked device re-launches chat.lua or client.lua on its own via startup.lua's runtime role check, with no operator intervention beyond the reboot itself (SRV-04)"
    - "The bridge token exists in exactly one place on the server (the marked folder's secret.txt) and in zero files across the entire repository (SRV-03)"
  artifacts:
    - turtle/turtle-helper/README.md
    - turtle/turtle-helper/CLAUDE.md
    - "<SERVER_DIR>/world/computercraft/computer/<id>/ (real, confirmed folder -- not git-tracked)"
  key_links:
    - "Task 4's real smoke-check text feeds directly into Task 5's README rewrite, so the documented failure/success messages are the ones actually observed, not RESEARCH.md's MEDIUM-confidence guess"
  prohibitions:
    - statement: "MUST NOT document (or leave documented) a wget, pastebin, or GitHub-raw-URL step for getting Lua onto a device -- uv run deploy is the only documented path this milestone (D-01)"
      status: resolved
      verification: judgment
---

<objective>
Place a real device on the live server, run Plan 03-01's deploy engine against it, prove SRV-02
(byte-correct on-disk placement, confirmed folder path), SRV-03 (token isolation), and SRV-04
(reboot-relaunches-itself), then rewrite README.md's "In game" section and CLAUDE.md's "Dev loop" line
to describe the proven recipe with the real captured smoke-check text.

Purpose: no computer has ever been placed on this server before Phase 3, so SRV-02's folder path was
MEDIUM-HIGH confidence from research, not confirmed. This plan is where that confirmation, and the rest
of Phase 3's real-infrastructure proof, actually happens -- every step needing an in-game action or a
device reboot is a `checkpoint:human-action`, per the project's live-run rule.

Output: one real, marked, deployed device on the live server; `README.md`'s "Setup > 2. In game" section
rewritten; `CLAUDE.md`'s "Dev loop" line updated with the deploy script name.
</objective>

## Flagged Assumption (spec-less edge probe, SRV-02 "unclassified")

The per-computer folder path `<world>/computercraft/computer/<id>/` was MEDIUM-HIGH confidence from
RESEARCH.md going into this plan, since no computer had ever been placed on this server before Phase 3
(`world/computercraft/` did not exist as of the CONTEXT.md inspection). Task 2 below confirms it for
real against a live device placement; if the actual path differs, Task 2's action requires recording the
real path so Task 5's docs reflect the confirmed value, not the researched guess. This assumption is
resolved by Task 2's live confirmation, not assumed away -- flagged here per the spec-less probe
fallback's mandatory treatment of an "unclassified" row (never authored as a backstop truth).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Place a device and mark it for deploy (D-03)</name>
  <action>Deploy (Plan 03-01) only touches folders containing a `_marker.txt` marker file -- this keeps it away from your other turtles (quarry, crater, ...) on the same server.</action>
  <instructions>
    In game, on the server (now running with the 127.0.0.1 allow rule applied):

    1. Place either an Advanced Computer with a Chat Box attached (for the "chat" role) or a turtle /
       Advanced Computer without one (for the "worker" role) -- either is fine to prove SRV-02..04 now;
       Phase 4 needs at least one of each eventually.
    2. At that computer's terminal, run: edit _marker.txt -- this creates and opens an empty file.
    3. Save and exit without typing anything into it (Ctrl then S, then Ctrl then T, in the in-game
       editor).
    4. Optionally, run label set &lt;name&gt; if you want a friendly device id.
  </instructions>
  <verification>I'll scan &lt;SERVER_DIR&gt;/world/computercraft/computer/*/ for a folder containing _marker.txt in the next task.</verification>
  <resume-signal>Reply with which kind of device you placed (chat / worker) and "marked" once step 3 is done.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run deploy against the real server; confirm folder path and files written (SRV-02)</name>
  <files>&lt;SERVER_DIR&gt;/world/computercraft/computer/&lt;id&gt;/* (real, outside the repo)</files>
  <read_first>turtle/turtle-helper/deploy/deploy.py, turtle/turtle-helper/deploy/rules.py</read_first>
  <precondition>The operator confirmed a marker file was created in-game in the previous checkpoint.</precondition>
  <action>
    Run `uv run deploy` against the real SERVER_DIR -- an ordinary filesystem write while the server is
    running (RESEARCH.md confirms placing files while the server runs is safe; only a reboot is needed
    for the device to notice; this is distinct from the toml-edit case in Plan 03-03, which specifically
    required the server stopped). Capture the full stdout and stderr. Confirm: (1) exactly one new
    folder under &lt;SERVER_DIR&gt;/world/computercraft/computer/ now contains _marker.txt, chat.lua,
    client.lua, startup.lua, secret.txt, and bridge.txt; (2) the folder's path matches the researched
    pattern &lt;world&gt;/computercraft/computer/&lt;numeric-id&gt;/ (ROADMAP success criterion 2 -- SRV-02's
    folder path was MEDIUM-HIGH confidence until now; if it differs, record the ACTUAL path so Task 5's
    docs update reflects reality, not the researched guess); (3) secret.txt's content is exactly the
    real BRIDGE_TOKEN value -- confirm this via length/equality comparison in-process, never by printing
    it to the terminal; (4) bridge.txt contains exactly "ws://127.0.0.1:8765" (or whatever HOST/PORT the
    real .env resolves to) and never the substring "localhost"; (5) the captured deploy stdout/stderr
    does not contain the real token string anywhere -- check this with a boolean "in" test, never by
    printing the captured output alongside the token.
  </action>
  <acceptance_criteria>
    - Exactly one marked folder under the real computer root contains all six expected files
    - The confirmed folder path matches computer/&lt;numeric-id&gt;/ (or the actual observed pattern is
      recorded if it differs)
    - secret.txt matches the real token by equality check, never printed
    - bridge.txt contains the IPv4 literal and never "localhost"
    - The captured deploy output contains no occurrence of the real token string
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run deploy > /tmp/deploy_real.log 2>&1; echo "EXIT:$?"; tail -20 /tmp/deploy_real.log</automated>
    <fails_when>EXIT is not 0, or the log is empty</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
from pathlib import Path
from bridge.settings import Settings

s = Settings()
computer_root = s.server_dir / "world" / "computercraft" / "computer"
marked = [d for d in computer_root.iterdir() if d.is_dir() and (d / "_marker.txt").exists()]
assert len(marked) >= 1, "no marked folder found"
folder = marked[0]
expected = {"_marker.txt", "chat.lua", "client.lua", "startup.lua", "secret.txt", "bridge.txt"}
present = {p.name for p in folder.iterdir()}
assert expected <= present, present
assert (folder / "secret.txt").read_text(encoding="utf-8").strip() == s.bridge_token
bridge_txt = (folder / "bridge.txt").read_text(encoding="utf-8")
assert "localhost" not in bridge_txt
log = Path("/tmp/deploy_real.log").read_text(encoding="utf-8", errors="replace")
assert s.bridge_token not in log, "token leaked into deploy output"
print("REAL_DEPLOY_OK", folder.name)
PYEOF</automated>
    <fails_when>non-zero exit, or "REAL_DEPLOY_OK" absent (a required file missing, secret.txt mismatched, localhost present, or the token leaked into the captured log)</fails_when>
  </verify>
  <done>A real marked folder on the live server contains all five deploy-written files plus its own marker, confirmed byte-correct with zero token leakage; the actual folder path is recorded for the docs task.</done>
</task>

<task type="auto">
  <name>Task 3: SRV-03 token-isolation proof -- repo-wide and server-wide search</name>
  <files>none -- read-only search</files>
  <read_first>turtle/turtle-helper/bridge/settings.py, .gitignore</read_first>
  <action>
    Prove SRV-03: the BRIDGE_TOKEN value appears nowhere except the marked device's secret.txt. Read the
    token from the real .env via Settings without ever printing it. Run a LIST-ONLY search (grep -l /
    -rl, never -o or an unredacted -n context) for the exact token string across two roots: (1) the
    whole repository working tree, excluding .git -- expect ZERO matching files; (2)
    &lt;SERVER_DIR&gt;/world -- expect exactly ONE matching file, the marked folder's secret.txt. Print
    only the list of matching FILE PATHS from each search, never the matching line content, so the token
    value itself never reaches the transcript.
  </action>
  <acceptance_criteria>
    - Zero files in the repository match the token
    - Exactly one file under &lt;SERVER_DIR&gt;/world matches the token, and it is the marked folder's
      secret.txt
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import subprocess
from bridge.settings import Settings

s = Settings()
token = s.bridge_token
repo_root = "."
server_world = str(s.server_dir / "world")

repo_hits = subprocess.run(
    ["grep", "-rlF", "--exclude-dir=.git", token, repo_root],
    capture_output=True, text=True,
).stdout.splitlines()
server_hits = subprocess.run(
    ["grep", "-rlF", token, server_world],
    capture_output=True, text=True,
).stdout.splitlines()

assert repo_hits == [], repo_hits
assert len(server_hits) == 1, server_hits
assert server_hits[0].endswith("secret.txt"), server_hits[0]
print("TOKEN_ISOLATION_OK", server_hits[0])
PYEOF</automated>
    <fails_when>non-zero exit, or "TOKEN_ISOLATION_OK" absent (any repo match, zero or more than one server match, or the one match is not a secret.txt path)</fails_when>
  </verify>
  <done>The token is proven to exist nowhere in the repo and exactly once on the server, in the marked device's own secret.txt.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Reboot the device and run the D-13 smoke check (both failure shapes)</name>
  <precondition>A valid ANTHROPIC_API_KEY is present in turtle/turtle-helper/.env -- starting the bridge in this task calls anthropic.AsyncAnthropic.models.retrieve() at boot and exits fatally without it, so the smoke check's success half cannot be observed with a misconfigured .env.</precondition>
  <action>Deploy has written startup.lua and the token/URL files; the device hasn't picked them up yet -- CC:Tweaked caches startup.lua until reboot.</action>
  <instructions>
    1. At the device's terminal, run `reboot` (or physically break/restart it). Confirm which program
       auto-launched -- `chat` if a Chat Box was attached, `client` otherwise (D-08's runtime detection).
    2. With the bridge NOT yet running, at the `lua` prompt on that same computer, run:
       http.websocket("ws://127.0.0.1:8765") -- paste back the exact return value or error message
       (RESEARCH.md expects something like a connection-refused-style error, now that the allow rule is
       in place but nothing is listening).
    3. Start the bridge yourself in your own terminal (`uv run bridge/bridge.py` from turtle-helper, or
       try the new `uv run launch`) and wait for its "listening on" log line.
    4. Re-run the exact same http.websocket("ws://127.0.0.1:8765") call at the same lua prompt -- paste
       back the result (expected: a websocket handle object, not an error).
  </instructions>
  <verification>I can't run in-game commands myself; I'll fold both exact messages you paste back into the docs in the next task.</verification>
  <resume-signal>Paste: which script launched, the pre-bridge error text, and the post-bridge success result.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update README.md and CLAUDE.md with the proven deploy recipe (D-01, discretion)</name>
  <files>turtle/turtle-helper/README.md, turtle/turtle-helper/CLAUDE.md</files>
  <read_first>turtle/turtle-helper/README.md, turtle/turtle-helper/CLAUDE.md</read_first>
  <action>
    Replace README.md's "Setup > 2. In game" section (the current paste-by-hand recipe: edit secret.txt,
    edit chat.lua, edit client.lua) with the deploy-based recipe: add SERVER_DIR=&lt;path&gt; to .env; in
    game, place a device and create _marker.txt at its terminal (edit _marker.txt, save, exit); from the
    PC, run `uv run deploy`; restart the server once (for the allow rule, per Plan 03-03) and reboot the
    device once (for startup.lua and any subsequent Lua edit) after any deploy; the D-13 smoke check
    (http.websocket("ws://127.0.0.1:8765") at the lua prompt) documented with the ACTUAL error and
    success text captured in Task 4 -- name both failure shapes D-13 calls out (before the rule:
    domain-not-permitted-style; with the rule but no bridge: connection-refused-style) using the real
    captured strings, not RESEARCH.md's guessed wording. Do not reintroduce any wget, pastebin, or
    raw.githubusercontent.com step anywhere in this section -- D-01 is the only documented path this
    milestone. Update CLAUDE.md's "Dev loop" line (currently: "Lua goes onto devices by placing files in
    the local dedicated server's per-computer folders (Phase 3), not pastebin/wget") to name the script:
    `uv run deploy`, plus the marker-file step.
  </action>
  <acceptance_criteria>
    - README.md's "In game" section documents `uv run deploy`, `_marker.txt`, and the D-13 smoke check
      with real (not guessed) message text
    - README.md contains no "wget", "pastebin", or "raw.githubusercontent" string anywhere
    - CLAUDE.md's Dev loop line names `uv run deploy`
  </acceptance_criteria>
  <verify>
    <automated>F=turtle/turtle-helper/README.md; grep -q "uv run deploy" "$F" && grep -q "_marker.txt" "$F" && ! grep -qi "wget" "$F" && ! grep -qi "pastebin" "$F" && ! grep -qi "raw.githubusercontent" "$F"</automated>
    <fails_when>non-zero exit (the deploy recipe is missing from README.md, or a forbidden alternate-install string was reintroduced)</fails_when>
  </verify>
  <verify>
    <automated>grep -q "uv run deploy" turtle/turtle-helper/CLAUDE.md</automated>
    <fails_when>non-zero exit (CLAUDE.md's Dev loop line was not updated with the script name)</fails_when>
  </verify>
  <done>README.md's In-game section and CLAUDE.md's Dev loop line describe the proven uv run deploy recipe with real captured smoke-check text, and no alternate install path is documented.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Rewritten `README.md` "Setup > 2. In game" section (deploy-based recipe, real smoke-check text).
- Updated `CLAUDE.md` "Dev loop" line (names `uv run deploy`).
- Real, external (not git-tracked) per-device files produced on the live server: `_marker.txt`
  (operator-created), `secret.txt`, `bridge.txt`, `startup.lua` (all deploy-written), plus the copied
  `chat.lua`/`client.lua`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-game operator action -> deploy's marker scan | Only the operator (sole player with server access on this private, single-PC setup) can create computers and marker files in-game; deploy trusts a marker's mere presence as the opt-in signal. |
| Executor -> secret search | The token-isolation proof (Task 3) handles a live secret value in-process; it must never let that value reach stdout, a log file, or this plan's own SUMMARY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04a | Information Disclosure | Repo-wide / server-wide token search (SRV-03 proof) | high | mitigate | The proof uses `grep -rlF` (list-only, fixed-string) so only file paths are ever printed -- the token's surrounding content, and the token value itself, never reach the transcript; the token is passed to grep only as an in-process argument sourced from the untouched `.env`, never echoed. |
| T-03-04b | Tampering | Operator-authored README/CLAUDE.md recipe (D-01) | medium | mitigate | The rewritten "In game" section documents only the `uv run deploy` path; the task's acceptance criteria explicitly grep the new section for the absence of "wget"/"pastebin"/"raw.githubusercontent" to keep D-01's no-alternate-install-path decision enforced in docs, not just in code. |
| T-03-04c | Spoofing | Marked folder created by the operator in-game | low | accept | Only the operator (trusted, sole player with server access) can create computers and marker files on this single-PC, private server; no untrusted party can inject a marker. |
</threat_model>

<verification>
A real device is placed, marked, deployed to, and rebooted; its folder is confirmed byte-correct and its
token isolated to that one file; the D-13 smoke check is run with both real failure/success messages
captured; README.md and CLAUDE.md are updated to match the proven recipe.
</verification>

<success_criteria>
SRV-02, SRV-03, and SRV-04 are all proven against a real device on the live server, and the documented
recipe in README.md/CLAUDE.md reflects the confirmed reality (folder path, marker filename, smoke-check
text) rather than RESEARCH.md's pre-run guesses.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-04-SUMMARY.md` when done
</output>