---
phase: 03-local-server-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - turtle/turtle-helper/base/chat.lua
  - turtle/turtle-helper/turtle/client.lua
autonomous: true
requirements: [SRV-01, SRV-02]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "chat.lua and client.lua contain no wss://YOUR-BRIDGE-HOST placeholder and no 'localhost' string; both fall back to ws://127.0.0.1:8765 and override BRIDGE_URL from bridge.txt when that file is present"
    - "chat.lua's DEVICE_ID follows the same label-or-id rule as client.lua (os.getComputerLabel() or a device-<id> fallback), so every hello frame's device id follows one naming rule (D-10)"
    - "turtle/turtle-helper/base/chat.lua is git-tracked as of this plan's commit (was previously untracked on disk)"
  artifacts:
    - turtle/turtle-helper/base/chat.lua
    - turtle/turtle-helper/turtle/client.lua
  key_links:
    - "deploy.py (Plan 03-01 Task 2) copies these exact files byte-identical into every marked folder; the bridge.txt override this plan adds is what that task's bridge.txt write is read by"
  prohibitions:
    - statement: "MUST NOT contain the string 'localhost' anywhere in chat.lua or client.lua's BRIDGE_URL handling -- Windows resolves 'localhost' to IPv6 ::1 first, which the private-range deny rule blocks even after the 127.0.0.1 allow rule is added (D-06, Pitfall 1)"
      status: resolved
      verification: judgment
---

<objective>
Amend `chat.lua` and `client.lua` per D-06 (bridge-URL file, IPv4-literal fallback, placeholder removed)
and D-10 (consistent label-or-id device naming), and bring `chat.lua` under git tracking for the first
time.

Purpose: these are the two files Plan 03-01's deploy engine copies byte-identical into every marked
folder, and the two failure modes RESEARCH.md's Pitfall 1 warns about (a stale `wss://YOUR-BRIDGE-HOST`
placeholder, or a `localhost` string that Windows resolves to a blocked IPv6 address) live entirely in
these two files. Getting them right here, independently of the deploy engine, is what lets Plan 03-04's
real-device smoke check succeed on the first try.

Output: `turtle/turtle-helper/base/chat.lua` (git-tracked from this plan on) and
`turtle/turtle-helper/turtle/client.lua`, both syntax-valid and ready for deploy to copy verbatim.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/PROJECT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend chat.lua (D-10 naming, D-06 bridge URL) and client.lua (D-06 bridge URL) -- first git-add for chat.lua</name>
  <reversibility rating="reversible">D-06 amends Phase 2's D-08 ("the device stores only secret.txt and its Lua") by one more deploy-written file; CONTEXT.md rates this reversible -- dropping bridge.txt just means restoring a constant in two Lua files.</reversibility>
  <files>turtle/turtle-helper/base/chat.lua, turtle/turtle-helper/turtle/client.lua</files>
  <read_first>turtle/turtle-helper/base/chat.lua, turtle/turtle-helper/turtle/client.lua, .planning/workstreams/turtle-helper/phases/02-fake-device-harness-protocol-resilience/02-01-PLAN.md</read_first>
  <action>
    Edit turtle/turtle-helper/base/chat.lua: replace line 7's local BRIDGE_URL assignment (currently the
    literal string "wss://YOUR-BRIDGE-HOST" with a "change me" comment) with a plain assignment of the
    literal string "ws://127.0.0.1:8765" -- the IPv4-literal fallback default D-13 requires. The
    wss://YOUR-BRIDGE-HOST placeholder is removed entirely, not just commented out: CONTEXT.md's gotcha
    section states the placeholder "would break the LOOP-05 diff if left; D-06 removes it," so no trace
    of that string may remain anywhere in the file. Replace line 8's local DEVICE_ID assignment
    (currently the literal string "base") with os.getComputerLabel() or a fallback string built as
    "device-" concatenated with os.getComputerID() -- the same label-or-id pattern client.lua already
    uses (D-10). Immediately after the existing TOKEN line (currently line 19, the
    readFile("secret.txt") assignment), add a bridge-URL override: read "bridge.txt" via the existing
    readFile helper into a local variable, and when that read returns non-nil, reassign BRIDGE_URL to
    that value trimmed with the same gsub("%s+$", "") pattern already used for TOKEN. This must be a
    plain reassignment of the existing local BRIDGE_URL, not a new local declaration -- Lua would
    otherwise shadow it and the session()/connectLoop() closures further down would keep reading the
    original top-level value.

    Edit turtle/turtle-helper/turtle/client.lua: replace line 11's local BRIDGE_URL assignment
    (currently "wss://YOUR-BRIDGE-HOST" with a "change me (ws:// for local dev)" comment) with the same
    plain assignment of "ws://127.0.0.1:8765" -- same IPv4-literal fallback, same placeholder-removal
    rule as chat.lua. Immediately after the existing "TOKEN = TOKEN:gsub(\"%s+$\", \"\")" line (currently
    line 29), add the identical bridge-URL override pattern described above (read "bridge.txt" via
    readFile, reassign BRIDGE_URL when present, trimmed the same way).

    Since turtle/turtle-helper/base/chat.lua is untracked (confirmed: `git ls-files` returns nothing for
    this path and `git check-ignore -v` exits 1 -- it is present on disk, not gitignored, simply never
    committed), `git add` it as part of this task's commit so it becomes tracked for the first time this
    phase, exactly as Plan 02-01 did for client.lua in Phase 2. turtle/turtle-helper/turtle/client.lua is
    already tracked; no `git add` is needed for it, only the edit.
  </action>
  <acceptance_criteria>
    - Neither file contains the string "YOUR-BRIDGE-HOST" or the substring "localhost" anywhere
    - Both files contain the literal string "127.0.0.1:8765" and a readFile("bridge.txt") call
    - chat.lua's DEVICE_ID assignment uses os.getComputerLabel() with the device-<id> fallback pattern
    - `git status` / `git ls-files` shows chat.lua tracked after this task's commit
  </acceptance_criteria>
  <verify>
    <automated>git ls-files --error-unmatch turtle/turtle-helper/base/chat.lua >/dev/null</automated>
    <fails_when>non-zero exit (chat.lua still untracked after this task's commit)</fails_when>
  </verify>
  <verify>
    <automated>F1=turtle/turtle-helper/base/chat.lua; F2=turtle/turtle-helper/turtle/client.lua; ! grep -q "YOUR-BRIDGE-HOST" "$F1" "$F2" && ! grep -qi "localhost" "$F1" "$F2" && grep -q "127.0.0.1:8765" "$F1" && grep -q "127.0.0.1:8765" "$F2" && grep -q 'readFile("bridge.txt")' "$F1" && grep -q 'readFile("bridge.txt")' "$F2"</automated>
    <fails_when>non-zero exit (a placeholder or localhost string survived, or the bridge.txt override is missing from either file)</fails_when>
  </verify>
  <verify>
    <automated>grep -q 'os.getComputerLabel() or ("device-" .. os.getComputerID())' turtle/turtle-helper/base/chat.lua</automated>
    <fails_when>non-zero exit (D-10's naming rule was not applied to chat.lua)</fails_when>
  </verify>
  <done>Both Lua files use the IPv4-literal fallback with no placeholder or localhost string, read bridge.txt for a URL override, and chat.lua uses the label-or-id naming rule; chat.lua is now git-tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Syntax-check both amended Lua files (luaparse, Lua 5.2 grammar)</name>
  <files>turtle/turtle-helper/base/chat.lua, turtle/turtle-helper/turtle/client.lua</files>
  <read_first>turtle/turtle-helper/base/chat.lua, turtle/turtle-helper/turtle/client.lua</read_first>
  <action>
    No Lua runtime exists on this PC (CLAUDE.md and CONTEXT.md gotchas). Verify both files the same way
    Plan 02-01 verified client.lua: install luaparse (npm) in a throwaway `mktemp -d` directory outside
    the repo (never add package.json or node_modules to the turtle-helper tree), then parse each file
    with luaVersion set to "5.2" and confirm no exception is thrown for either file.
  </action>
  <acceptance_criteria>
    - luaparse parses both files with no thrown error
  </acceptance_criteria>
  <verify>
    <automated>cd "$(mktemp -d)" && npm i luaparse --silent >/dev/null 2>&1 && ROOT="$(git rev-parse --show-toplevel)" && node -e "const p=require('luaparse');const fs=require('fs');for (const f of process.argv.slice(1)) { p.parse(fs.readFileSync(f,'utf8'), {luaVersion:'5.2'}); } console.log('LUA_PARSE_OK');" "$ROOT/turtle/turtle-helper/base/chat.lua" "$ROOT/turtle/turtle-helper/turtle/client.lua"</automated>
    <fails_when>non-zero exit, or "LUA_PARSE_OK" absent from the output (a syntax error in either file)</fails_when>
  </verify>
  <done>Both chat.lua and client.lua parse cleanly under Lua 5.2 grammar.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Amended `BRIDGE_URL` fallback constant (IPv4 literal, placeholder removed) plus a `bridge.txt`
  read-and-override in both `base/chat.lua` and `turtle/client.lua`.
- `DEVICE_ID` label-or-id naming pattern applied to `base/chat.lua` (already present in `client.lua`).
- `base/chat.lua` becomes git-tracked for the first time.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device Lua -> bridge URL resolution | The device trusts whatever `bridge.txt` says, or the compiled-in IPv4 fallback, to be the correct bridge address; both come from the operator's own local deploy, not from the network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02a | Information Disclosure | BRIDGE_URL fallback / bridge.txt read | high | mitigate | The wss://YOUR-BRIDGE-HOST placeholder and any "localhost" string are removed; both files fall back to the IPv4 literal ws://127.0.0.1:8765 so a resolver quirk (Windows preferring ::1 for localhost) never routes the hello/token past a host match the deny rule wouldn't otherwise block as reliably (Pitfall 1) -- checked by a grep asserting neither file contains "localhost". |
| T-03-02b | Tampering | chat.lua's first git-add | low | accept | chat.lua was previously untracked and unreviewed by version control; this task's first `git add` brings it under the same review/diff process as every other tracked Lua file -- no new risk, closes an existing gap. |
</threat_model>

<verification>
Both files contain the IPv4-literal fallback, the bridge.txt override, and (chat.lua) the label-or-id
naming rule, with no placeholder or localhost string anywhere; both parse cleanly under Lua 5.2 grammar.
</verification>

<success_criteria>
`turtle/turtle-helper/base/chat.lua` is git-tracked, and both Lua files are ready for Plan 03-01's deploy
engine to copy byte-identical into a real marked folder.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-02-SUMMARY.md` when done
</output>
