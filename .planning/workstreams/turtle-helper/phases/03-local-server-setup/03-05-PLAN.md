---
phase: 03-local-server-setup
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - turtle/turtle-helper/deploy/deploy.py
  - turtle/turtle-helper/tests/test_deploy_files.py
  - turtle/turtle-helper/pyproject.toml
  - turtle/turtle-helper/deploy/__init__.py
  - turtle/turtle-helper/deploy/launcher.py
  - turtle/turtle-helper/bridge/settings.py
  - turtle/turtle-helper/.env.example
  - turtle/turtle-helper/startup.lua
  - turtle/turtle-helper/tests/test_device_lua.py
  - turtle/turtle-helper/tests/test_deploy_rules.py
files_deleted:
  - turtle/turtle-helper/deploy/deploy.py
  - turtle/turtle-helper/tests/test_deploy_files.py
autonomous: false
requirements: [SRV-01, SRV-02, SRV-03, SRV-04, SRV-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Before any in-game step, raw.githubusercontent.com served install.lua, startup.lua, base/chat.lua and turtle/client.lua byte-identical to origin/main (4df2021). The orchestrator pushed main at Task 1, never as an executor side effect (D-18). The raw CDN can serve a stale body or a cached 404 for up to about 5 minutes after a push"
    - "Device A (computer 0) was set up with nothing but the wget run line, the typed token and Enter at the URL prompt. Its folder <SERVER_DIR>/world/computercraft/computer/0/ holds chat.lua, client.lua and startup.lua byte-identical to origin/main, secret.txt equal to BRIDGE_TOKEN, and bridge.txt equal to ws://127.0.0.1:8765. The installer's own first write created that folder, which confirms the SRV-02 path through the only placement path there is (SRV-02; SRV-05 install half; D-14, D-15, D-17, D-19)"
    - "After a reboot, device A's startup.lua printed 'chat.lua up to date' and 'client.lua up to date' and launched chat (Chat Box attached). The bridge logged the line device connected: device-0 (chat) caps=['say'], under the device-<id> name (D-10). No PC-side step was involved (SRV-04; SRV-05; D-08, D-16; ROADMAP criterion 5, first half)"
    - "The D-13 smoke check ran at a real lua prompt with the bridge up (computer 1, the former device B; D-13 allows any computer). ws://127.0.0.1:8765 printed 'table: 3c9a6ef1 nil', a handle. ws://127.0.0.2:8765 printed 'false Domain not permitted' (no rule covers the address). ws://127.0.0.1:8766 printed 'false Could not connect' (the rule allows it, but nothing listens). All three are recorded verbatim for the docs (SRV-01 in-game smoke check; ROADMAP criterion 1)"
    - "The developer placement path is gone from the repo (D-19). deploy/deploy.py, tests/test_deploy_files.py, the deploy entry in [project.scripts], and startup.lua's marker-file skip with its test are all deleted. startup.lua runs the GitHub update on every boot and keeps its line-1 header. deploy/rules.py, deploy/server_state.py, deploy/launcher.py (uv run launch) and tests/test_deploy_rules.py are kept and pass. launcher.py no longer imports the deleted module (SRV-02: no second placement path)"
    - "Every folder under <SERVER_DIR>/world/computercraft/computer/ has a numeric name (0 and 1), computer/0 is a turtle-helper device folder, and no turtle-helper device folder holds a marker file. computer/1 (the former device B) holds only an empty leftover marker file from before D-19, which nothing reads any more, and is left untouched (SRV-02 folder-to-device mapping; D-19)"
    - "The token appears in no tracked or untracked-unignored repo file, nowhere in git history, and in no server log (UTF-8 or UTF-16LE, rotated .gz files included). In the world it appears only as computercraft/computer/<numeric id>/secret.txt (SRV-03; ROADMAP criterion 3)"
  artifacts:
    - "<SERVER_DIR>/world/computercraft/computer/0/ (device A, created and filled by install.lua; outside the repo, not git-tracked)"
    - "turtle/turtle-helper/startup.lua (unconditional boot-time update; D-19 removed the marker-file skip)"
    - "turtle/turtle-helper/tests/test_device_lua.py (the marker-skip test replaced by a no-marker test)"
    - "turtle/turtle-helper/tests/test_deploy_rules.py (rule, probe and launcher tests only)"
    - "turtle/turtle-helper/deploy/launcher.py (uv run launch, with REPO_ROOT defined locally)"
    - ".planning/workstreams/turtle-helper/phases/03-local-server-setup/03-05-SUMMARY.md (records the captured in-game text under fixed keys)"
  key_links:
    - "Plan 03-06's README check compares its text verbatim against this plan's SUMMARY lines SMOKE_OK, SMOKE_DENIED and SMOKE_REFUSED ('table: 3c9a6ef1 nil', 'Domain not permitted', 'Could not connect'), so the docs carry observed text, not RESEARCH.md's guesses"
    - "Task 4's D-19 commit is made here but pushed only at Plan 03-06 Task 2. startup.lua does not update itself, so device A keeps running the startup.lua it installed from 4df2021, with the now-dead marker branch. That is harmless (D-19), and Plan 03-06's update proof relies on that startup.lua still fetching chat.lua and client.lua"
    - "base/chat.lua and turtle/client.lua stay byte-identical to origin/main in this plan. Their stale Setup comments are Plan 03-06's update payload"
    - "launcher.py must define REPO_ROOT itself once deploy/deploy.py is gone. The launcher tests in test_deploy_rules.py prove that uv run launch still imports"
  prohibitions:
    - statement: "MUST NOT push, start, stop or restart the server or the bridge, or act in game, from an executor. The push is an orchestrator checkpoint and every in-game step belongs to the operator (D-18, the project's live-run rule)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the bridge token value reach the transcript, a log file or the SUMMARY. Every check compares it in-process and prints only paths, ids and counts (SRV-03, D-15)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit chat.lua or client.lua, on a device or in the repo, in this plan. Lua runtime fixes belong to Phase 4 LOOP-05, and the stale comments are Plan 03-06's payload"
      status: resolved
      verification: "git diff --quiet origin/main -- turtle/turtle-helper/base/chat.lua turtle/turtle-helper/turtle/client.lua (Task 4, D19_CLEAN_OK)"
    - statement: "MUST NOT delete or break deploy/rules.py, deploy/server_state.py, deploy/launcher.py or tests/test_deploy_rules.py. Phase 6's host setup builds on them (D-19)"
      status: resolved
      verification: "uv run python tests/test_deploy_rules.py reports # fail 0 (Task 4)"
    - statement: "MUST NOT write anything into <SERVER_DIR>/world from this plan. The folder and token checks are read-only, and a second, PC-side placement path must not come back in any form (D-19)"
      status: resolved
      verification: judgment
---

<objective>
Put Plan 03-04's in-game install in front of the live server, prove Phase 3's remaining claims on a real device, and remove the developer placement path (D-19):

1. The orchestrator pushes `main` (D-18). The executor confirms raw GitHub serves the pushed bytes.
2. The operator sets up device A with nothing but the one `wget run` line, the typed token and Enter (D-14, D-15, D-17). At the same prompt, before rebooting, they run the D-13 smoke check and capture the success shape and both failure shapes verbatim. Then they reboot and watch `startup.lua` bring the role program up and connect (SRV-04, SRV-05).
3. The executor then does three things:
   - removes the developer deploy path from the repo (D-19);
   - confirms device A's real folder layout, the SRV-02 path;
   - proves the token lives only in the per-device `secret.txt` (SRV-03).

Status at revision (2026-09-25):
- Task 1 was performed by the orchestrator (origin/main = 4df2021).
- Tasks 2 and 3 are done, and Task 3's evidence is in hand (listed in Task 4).
- Only Task 4 remains.

Purpose: this is where the MEDIUM-confidence folder path, the installer, the boot-time launch and the token isolation stop being fixture claims. It is also where the repo drops to exactly one way files reach a device (D-19). Every step that needs the push, the running server or a hand in game is a checkpoint.

Output:
- one real device on the live server (device A, computer 0, installed from GitHub);
- the D-19 removal commit;
- a SUMMARY that records the captured in-game text under fixed keys for Plan 03-06's docs.
</objective>

## Flagged Assumption (spec-less edge probe, SRV-02 "unclassified")

RESEARCH.md (A1) rated the per-computer folder path `<world>/computercraft/computer/<id>/` MEDIUM-HIGH confidence. Before Task 3, `world/computercraft/ids.json` read `{"computer": 0}`: id 0 had been handed out, but no `computer/0/` folder existed, because that computer had never written a file.

Device A is computer 0. Its folder was created by the installer's own first `fs.open` write, which is the only placement path left after D-19.

Task 4's layout check asserts three things:
- every folder name under `computer/` is numeric;
- no turtle-helper device folder holds a marker file (computer/1, the former device B, keeps an empty leftover one that nothing reads);
- `computer/0/` holds the five expected files.

If the real layout differs, Task 4 records the actual path under `FOLDER_PATH:`, so Plan 03-06's docs use what was observed. The assumption is resolved by live confirmation, not assumed away.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-03-SUMMARY.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-04-SUMMARY.md
@turtle/turtle-helper/install.lua
@turtle/turtle-helper/startup.lua
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push main to GitHub so install.lua and startup.lua are fetchable (D-18)</name>
  <action>Plan 03-04's install.lua and startup.lua existed only in local commits. The in-game wget line and every device's boot-time update fetch from GitHub main, so main had to be pushed before anything happened in game. Pushing is an orchestrator action handed over at this checkpoint, never an executor side effect (D-18; the author authorized pushes for this recipe on 2026-09-25). Performed by the orchestrator on 2026-09-25: origin/main is 4df2021.</action>
  <instructions>
    For the orchestrator:

    1. Confirm `git status --short turtle/turtle-helper` is clean, and that `git log --oneline origin/main..main` lists Plan 03-04's commits (test(03-04), feat(03-04), docs(03-04)).
    2. Run `git push origin main`.
    3. Until the push has completed, do not open or curl the raw install.lua URL. raw.githubusercontent.com sits behind a CDN that can cache a 404 or an older body for a path for up to about 5 minutes. The planner probed that URL once at plan time and got a 404; that cache entry has long expired by the time this checkpoint surfaces.
  </instructions>
  <verification>Task 2 compares the four raw URLs with origin/main byte for byte. It does not hand anything to the operator until all four match.</verification>
  <resume-signal>Reply "pushed &lt;short sha&gt;" once `git push origin main` has succeeded.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm raw GitHub serves the pushed bytes for all four files (D-18, CDN caveat)</name>
  <files>none -- read-only network check against raw.githubusercontent.com</files>
  <read_first>turtle/turtle-helper/install.lua, turtle/turtle-helper/startup.lua</read_first>
  <precondition>The orchestrator replied "pushed &lt;sha&gt;" at Task 1.</precondition>
  <action>
    Run the automated check below from the repository root. It proves three things:
    - the local origin/main ref matches main for the four device files, so the push included them;
    - each raw URL under the pinned base serves bytes whose git blob hash (`git hash-object --no-filters --stdin`) equals origin/main's blob for that path;
    - install.lua is really there, not a 404.

    If any line prints MISSING, the push did not include that file: stop and report it. If any line prints DIFF, the CDN is still serving a cached copy. Wait a few minutes and run the check again. Never go on to Task 3 while any line prints DIFF. If a file still prints DIFF about 10 minutes after the push, stop and report it, because the push may not contain that file.

    This check reads only public files and never touches the token.
  </action>
  <acceptance_criteria>
    - All four files print SAME, and the output ends with RAW_MATCH_OK
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --quiet main origin/main -- turtle/turtle-helper/install.lua turtle/turtle-helper/startup.lua turtle/turtle-helper/base/chat.lua turtle/turtle-helper/turtle/client.lua && BASE=https://raw.githubusercontent.com/nneibaue/minecraft-transit-report/main/turtle/turtle-helper; ok=1; for f in install.lua startup.lua base/chat.lua turtle/client.lua; do want=$(git rev-parse --verify --quiet "origin/main:turtle/turtle-helper/$f") || { echo "MISSING $f"; ok=0; continue; }; got=$(curl -fsSL "$BASE/$f" | git hash-object --no-filters --stdin); if [ "$got" = "$want" ]; then echo "SAME $f"; else echo "DIFF $f"; ok=0; fi; done; [ "$ok" = 1 ] && echo RAW_MATCH_OK</automated>
    <fails_when>non-zero exit, a MISSING or DIFF line, or RAW_MATCH_OK absent (MISSING: the push did not include that file; DIFF: the CDN still serves a stale body or a cached 404, so its blob hash differs from origin/main's)</fails_when>
  </verify>
  <done>raw.githubusercontent.com serves all four device files byte-identical to origin/main, so the in-game wget line fetches exactly what was pushed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: In game -- install device A with the wget line, run the D-13 smoke check, reboot it (D-13, D-14, D-15, D-17)</name>
  <precondition>Task 2 printed RAW_MATCH_OK in this run, and the operator's server and bridge are both running in their own terminals.</precondition>
  <action>Only a player at an in-game computer can run wget, type the token and reboot a device. The executor cannot, and no executor acts in game (live-run rule). These steps set up the admin path, the only install path (D-19), on device A in one visit.</action>
  <instructions>
    Keep the bridge running in its terminal throughout. Until Phase 4, do not type "$robot" in game chat, because that would spend an API call. Never paste the token back to me.

    Optional first step (D-15). If BRIDGE_TOKEN in turtle/turtle-helper/.env is a long random string, you may change it to a short passphrase you can type easily. If you do, restart the bridge in its terminal (Ctrl+C, then `uv run bridge/bridge.py`) before step 1.

    Device A. Recommended hardware: an Advanced Computer with a Chat Box touching it (the chat role). The already-placed computer #0 works too once a Chat Box is attached. Any computer is fine if you have no Chat Box; it will run as a worker.

    1. At device A's shell prompt, type exactly:
       wget run https://raw.githubusercontent.com/nneibaue/minecraft-transit-report/main/turtle/turtle-helper/install.lua
    2. At "Bridge token (typing is hidden):", type BRIDGE_TOKEN's value and press Enter. Only * characters appear.
    3. At the bridge URL prompt, press Enter to keep ws://127.0.0.1:8765.
    4. Copy down every line the installer printed after the wget line (the banner, the "Installed on computer ..." list and the reboot hint).
    5. Smoke check (D-13), still at the shell prompt and BEFORE rebooting. Type `lua`, then enter each line below on its own, and copy what each prints:
       a. ws, err = http.websocket("ws://127.0.0.1:8765") print(ws, err) if ws then ws.close() end
          Expected: a table value and nil. This is the success shape with the rule in place and the bridge up. The bridge may log a "rejected ... no valid hello" warning for this bare connection; that is expected.
       b. print(http.websocket("ws://127.0.0.2:8765"))
          Expected: false plus an error. This is what a computer sees when no allow rule covers the address: 127.0.0.2 is private, and the rule only allows 127.0.0.1.
       c. print(http.websocket("ws://127.0.0.1:8766"))
          Expected: false plus a connection error. This is the "rule present, bridge not listening" shape, because nothing listens on 8766.
       Then type exit() to leave the lua prompt.
    6. Type `reboot`. Copy the lines startup.lua prints (for example "chat.lua up to date"), and note which program starts: chat if a Chat Box is attached, client otherwise.
    7. In the bridge terminal, copy the "device connected: ..." line for device A.

    If the wget line or the installer prints an error at any step, stop there and paste it.
  </instructions>
  <verification>Task 4 removes the developer path from the repo (D-19), checks device A's folder on disk, proves the token isolation, and records your pasted text in the SUMMARY under fixed keys.</verification>
  <resume-signal>Paste: (1) the installer output from step 4; (2) the three smoke-check results a, b and c; (3) the post-reboot lines and which program started; (4) the bridge's "device connected:" line.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Remove the developer deploy path (D-19), confirm device A's real folder (SRV-02), prove token isolation (SRV-03), record the captured text</name>
  <files>turtle/turtle-helper/deploy/deploy.py (deleted), turtle/turtle-helper/tests/test_deploy_files.py (deleted), turtle/turtle-helper/pyproject.toml, turtle/turtle-helper/deploy/__init__.py, turtle/turtle-helper/deploy/launcher.py, turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/.env.example, turtle/turtle-helper/startup.lua, turtle/turtle-helper/tests/test_device_lua.py, turtle/turtle-helper/tests/test_deploy_rules.py; read-only: &lt;SERVER_DIR&gt;/world (outside the repo)</files>
  <read_first>.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md (Amendments, D-19), turtle/turtle-helper/startup.lua, turtle/turtle-helper/deploy/launcher.py, turtle/turtle-helper/deploy/__init__.py, turtle/turtle-helper/pyproject.toml, turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/.env.example, turtle/turtle-helper/tests/test_device_lua.py, turtle/turtle-helper/tests/test_deploy_rules.py</read_first>
  <precondition>The operator's Task 3 paste includes a bridge "device connected:" line for device A.</precondition>
  <action>
    Evidence in hand from Task 3 (2026-09-25):
    - Device A is computer 0, named device-0 (no label), in the chat role.
    - Its boot lines were "chat.lua up to date", "client.lua up to date" and "[1:11]  connected to bridge".
    - The bridge logged: device connected: device-0 (chat) caps=['say'].
    - Smoke check a printed "table: 3c9a6ef1 nil".
    - Smoke check b printed "false Domain not permitted".
    - Smoke check c printed "false Could not connect".
    - The installer's output was not captured, because the operator rebooted before copying it.
    - The smoke checks ran at the lua prompt of computer 1 (the former device B), which D-13 allows. Computer 1's folder holds only the empty leftover marker file from Task 3's old device-B step. It is not a device and is left untouched.

    4a. Remove the developer placement path (D-19). There must be exactly one way files reach a device: the in-game install.lua line plus startup.lua's boot-time update.
    - Delete turtle/turtle-helper/deploy/deploy.py and turtle/turtle-helper/tests/test_deploy_files.py with git rm.
    - pyproject.toml: remove the deploy line from [project.scripts], and keep the harness and launch lines. Keep "deploy" in the hatch packages list, because the package still holds rules.py, server_state.py and launcher.py for Phase 6.
    - deploy/__init__.py: rewrite the docstring. The package holds the host-side helpers (the CC:Tweaked allow rule, the server probe and uv run launch), and devices get their files only from the in-game install.lua (D-19).
    - deploy/launcher.py: it imported REPO_ROOT from the deleted module. Define REPO_ROOT in launcher.py itself as the turtle-helper directory, Path(__file__).resolve().parent.parent, with a one-line comment. There is no behaviour change.
    - bridge/settings.py: the server_dir field description names uv run launch as the only consumer and drops the "local deployment" wording. .env.example: the SERVER_DIR comment names uv run launch in the same way. These are text-only changes.
    - startup.lua: delete the marker-file branch and the three header comment lines that describe it, so the chat.lua and client.lua update loop runs unconditionally on every boot. Reword line 2 to say that install.lua (the in-game wget line) puts this file on the device and that re-running that line updates startup.lua itself. Leave line 1, fetch(), the FILES table and the role launch unchanged.
    - tests/test_device_lua.py:
      - replace the marker-skip test with test_no_device_file_knows_a_marker, which asserts that neither startup.lua nor install.lua mentions a marker;
      - update the TESTS list;
      - update the docstring so it names D-19 and points at tests/test_deploy_rules.py as the layout precedent, not the deleted test file.
    - tests/test_deploy_rules.py:
      - delete the tests of the deleted CLI (the five test_main_* tests), their run_main helper, the fixture_toml helper they used, and the import of the deleted module;
      - drop any import that becomes unused;
      - keep the five rule tests, the two server-probe tests and the three launcher tests;
      - reword the docstring to "the allow rule, server probe and launcher".
    - Do not touch base/chat.lua or turtle/client.lua. Their stale Setup comments are Plan 03-06's update payload.
    - Run the first three checks below, then commit all of 4a as one commit with a plain `git commit`, not the gsd commit helper, because that helper does not record staged deletions. Use a message like refactor(03-05): remove the developer deploy path (D-19). Do not push (D-18); Plan 03-06 Task 2 pushes it.

    4b. Confirm device A's folder, the SRV-02 path. Run the fourth check. It is read-only against the server folder, which is safe while the server runs, and it asserts:
    - every folder name under computer/ is numeric;
    - no turtle-helper device folder holds a marker file (computer/1, the former device B, keeps an empty leftover one that nothing reads);
    - computer/0 is a turtle-helper device folder;
    - its chat.lua, client.lua and startup.lua equal `git show origin/main:...`. That is the pushed 4df2021 content, not the working tree: the working tree's startup.lua now carries the 4a edit, and a CRLF checkout would differ anyway;
    - secret.txt equals BRIDGE_TOKEN, compared in-process only;
    - bridge.txt is exactly ws://127.0.0.1:8765.

    4c. Prove token isolation (SRV-03). Run the fifth check.
    - It reads the token through Settings and searches in-process for its UTF-8 bytes across four places:
      - every tracked or untracked-but-not-ignored file in the repository (the gitignored .env is the token's source of truth and is deliberately out of scope);
      - the full `git log --all -p` history;
      - every file under &lt;SERVER_DIR&gt;/world except session.lock, which the running server holds locked and which only holds a timestamp marker;
      - every file in &lt;SERVER_DIR&gt;/logs and every *.log in the server root, decompressing .gz archives and searching the UTF-16LE encoding as well, because run.bat's Tee-Object writes server.log as UTF-16LE.
    - It prints only counts and relative paths.
    - If it reports matches outside a secret.txt, the token is probably too short or too common a word. Report the paths only, never the content, and tell the operator that a more distinctive passphrase is needed (D-15).

    4d. Write the SUMMARY. It must contain these lines, each at the start of a line, taken from the evidence above:
    - FOLDER_PATH: &lt;SERVER_DIR&gt;/world/computercraft/computer/0/ (pattern computer/&lt;numeric id&gt;/).
    - DEVICE_A: computer 0, device-0, chat.
    - INSTALLER_OUTPUT: not captured (the operator rebooted before copying it; the 4b folder check stands in for it).
    - SMOKE_OK: table: 3c9a6ef1 nil
    - SMOKE_DENIED: Domain not permitted
    - SMOKE_REFUSED: Could not connect
    - BOOT_OUTPUT: followed by a fenced block of the three boot lines.
    - BRIDGE_LOG: the device connected line.

    Also record the 4a commit's short sha in the SUMMARY. No token value appears in any of them.
  </action>
  <acceptance_criteria>
    - deploy/deploy.py and tests/test_deploy_files.py no longer exist. pyproject.toml has no deploy script entry and still has launch. tests/test_device_lua.py and tests/test_deploy_rules.py both print "# fail 0". ruff check and mypy are clean
    - startup.lua and install.lua parse under luaparse (LUA_PARSE_OK)
    - Outside tests/test_device_lua.py, no tracked turtle-helper file names the marker file or the deleted module
    - chat.lua and client.lua are unchanged from origin/main (D19_CLEAN_OK)
    - The layout check prints REAL_LAYOUT_OK with computer 0 among the installed ids
    - The token-isolation check prints TOKEN_ISOLATION_OK with repo=0, history=0 and logs=0, and every world match has the form computercraft/computer/&lt;numeric id&gt;/secret.txt
    - The SUMMARY carries the FOLDER_PATH, DEVICE_A, INSTALLER_OUTPUT, SMOKE_OK, SMOKE_DENIED, SMOKE_REFUSED, BOOT_OUTPUT and BRIDGE_LOG lines and the 4a commit's short sha, and no token value
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && test ! -e deploy/deploy.py && test ! -e tests/test_deploy_files.py && ! grep -q '^deploy = ' pyproject.toml && grep -q '^launch = "deploy.launcher:main"' pyproject.toml && uv run python tests/test_device_lua.py && uv run python tests/test_deploy_rules.py && uv run ruff check bridge/ harness/ deploy/ tests/ && uv run mypy bridge/ harness/ deploy/</automated>
    <fails_when>non-zero exit, or "# fail 0" absent from either TAP run (a deleted file still exists, the script entry survived or launch went missing, a test still imports the deleted module, launcher.py lost REPO_ROOT, or ruff or mypy report an issue)</fails_when>
  </verify>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)" && cd "$(mktemp -d)" && npm i luaparse --silent >/dev/null 2>&1 && node -e "const p=require('luaparse');const fs=require('fs');for (const f of process.argv.slice(1)) { p.parse(fs.readFileSync(f,'utf8'), {luaVersion:'5.2'}); } console.log('LUA_PARSE_OK');" "$ROOT/turtle/turtle-helper/startup.lua" "$ROOT/turtle/turtle-helper/install.lua"</automated>
    <fails_when>non-zero exit, or LUA_PARSE_OK absent (removing the marker-file branch left startup.lua syntactically broken)</fails_when>
  </verify>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! git grep -n -e '_marker.txt' -e 'deploy\.deploy' -- turtle/turtle-helper ':!turtle/turtle-helper/tests/test_device_lua.py' && git diff --quiet origin/main -- turtle/turtle-helper/base/chat.lua turtle/turtle-helper/turtle/client.lua && echo D19_CLEAN_OK</automated>
    <fails_when>non-zero exit, or D19_CLEAN_OK absent (a tracked file still names the marker file or imports the deleted module, or chat.lua or client.lua was edited, which would spend Plan 03-06's payload)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import subprocess
from bridge.settings import Settings

s = Settings()
root = s.server_dir / "world" / "computercraft" / "computer"
repo = {"chat.lua": "base/chat.lua", "client.lua": "turtle/client.lua", "startup.lua": "startup.lua"}
ours = b"-- startup.lua : turtle-helper"
tok = s.bridge_token.encode("utf-8")
def main_bytes(rel):
    return subprocess.run(["git", "show", "origin/main:turtle/turtle-helper/" + rel], capture_output=True, check=True).stdout
folders = [f for f in root.iterdir() if f.is_dir()]
assert folders and all(f.name.isdigit() for f in folders), [f.name for f in folders]
installed = [f for f in folders if (f / "startup.lua").is_file() and (f / "startup.lua").read_bytes().startswith(ours)]
assert "0" in [f.name for f in installed], [f.name for f in installed]
marked = [f.name for f in installed if any(p.name.startswith("_marker") for p in f.iterdir())]
assert not marked, ("marker file in a device folder (D-19)", marked)
for f in installed:
    for name, rel in repo.items():
        assert (f / name).read_bytes() == main_bytes(rel), (f.name, name, "differs from origin/main")
    assert (f / "secret.txt").read_bytes().strip() == tok, (f.name, "secret.txt is not BRIDGE_TOKEN")
    assert (f / "bridge.txt").read_bytes().strip() == b"ws://127.0.0.1:8765", (f.name, "bridge.txt")
print("REAL_LAYOUT_OK installed=%s folders=%s" % ([f.name for f in installed], sorted(f.name for f in folders)))
PYEOF</automated>
    <fails_when>non-zero exit, or REAL_LAYOUT_OK absent (a non-numeric folder, a marker file in a device folder, computer 0 is not a turtle-helper folder, a Lua file differs from origin/main, or secret.txt or bridge.txt does not hold the expected value)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import gzip
import subprocess
from pathlib import Path
from bridge.settings import Settings

s = Settings()
utf8 = s.bridge_token.encode("utf-8")
utf16 = s.bridge_token.encode("utf-16-le")
top = Path(subprocess.run(["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True, check=True).stdout.strip())
listing = subprocess.run(["git", "ls-files", "-z", "--cached", "--others", "--exclude-standard"], cwd=top, capture_output=True, check=True).stdout.decode("utf-8")
repo_hits = [p for p in listing.split("\0") if p and (top / p).is_file() and utf8 in (top / p).read_bytes()]
assert repo_hits == [], repo_hits
history = subprocess.run(["git", "log", "--all", "-p", "--no-color"], cwd=top, capture_output=True, check=True).stdout
assert utf8 not in history, "token found in git history"
world = s.server_dir / "world"
files = [p for p in world.rglob("*") if p.is_file() and p.name != "session.lock"]
world_hits = sorted(p.relative_to(world).as_posix() for p in files if utf8 in p.read_bytes())
parts = [h.split("/") for h in world_hits]
assert world_hits and all(len(x) == 4 and x[:2] == ["computercraft", "computer"] and x[2].isdigit() and x[3] == "secret.txt" for x in parts), world_hits
def blob(p):
    data = p.read_bytes()
    return gzip.decompress(data) if p.suffix == ".gz" else data
logs = [p for p in (s.server_dir / "logs").iterdir() if p.is_file()] + [p for p in s.server_dir.glob("*.log") if p.is_file()]
log_hits = [p.name for p in logs if utf8 in blob(p) or utf16 in blob(p)]
assert log_hits == [], log_hits
print("TOKEN_ISOLATION_OK repo=0 history=0 logs=0 world=%s" % world_hits)
PYEOF</automated>
    <fails_when>non-zero exit, or TOKEN_ISOLATION_OK absent (the token is in a repo file, in git history, in a server log, or in a world file other than a computer/&lt;id&gt;/secret.txt)</fails_when>
  </verify>
  <done>The repo has exactly one placement path (D-19): the developer deploy module, its tests, its script entry and startup.lua's marker skip are gone. The kept host helpers still pass their tests, and chat.lua and client.lua are untouched. Device A's real folder, computer/0/, matches origin/main and confirms the numeric-id path. The token lives only in that folder's secret.txt. The captured in-game text is recorded under fixed SUMMARY keys, with no token value.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- The D-19 removal commit:
  - `deploy/deploy.py` and `tests/test_deploy_files.py` deleted;
  - `pyproject.toml` without the deploy script;
  - `deploy/__init__.py`, `deploy/launcher.py` (local REPO_ROOT), `bridge/settings.py` and `.env.example` reworded around `uv run launch`;
  - `startup.lua` with an unconditional boot-time update;
  - `tests/test_device_lua.py` with the no-marker test;
  - `tests/test_deploy_rules.py` limited to the rule, probe and launcher tests.
- The SUMMARY, carrying the fixed keys FOLDER_PATH, DEVICE_A, INSTALLER_OUTPUT, SMOKE_OK, SMOKE_DENIED, SMOKE_REFUSED, BOOT_OUTPUT and BRIDGE_LOG, plus the 4a commit's short sha.
- One real device folder on the live server, not git-tracked: `computer/0/` (device A), holding chat.lua, client.lua, startup.lua, secret.txt and bridge.txt, all written by install.lua. `computer/1/` (the former device B) holds only an empty leftover marker file, is not a device, and was left untouched.
- `main` pushed to GitHub by the orchestrator at Task 1 (4df2021, D-18). The D-19 commit stays local until Plan 03-06 Task 2.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> live secret | Tasks 2 and 4 work next to the real BRIDGE_TOKEN (Settings, device A's secret.txt). Its value must never reach the transcript or the SUMMARY |
| GitHub CDN -> in-game install | The first device install trusts whatever raw.githubusercontent.com serves at that moment |
| Operator keyboard -> device A | The token is typed at a masked prompt in game |
| Repo -> Phase 6 host setup | The D-19 removal must leave the kept host helpers importable and tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | Task 4 layout and isolation checks | high | mitigate | The token is compared in-process only (bytes `in` / `==`). Assertion messages carry file names and ids, never content. The isolation scan prints only counts and relative paths. The resume signal tells the operator never to paste the token. |
| T-03-05-02 | Information Disclosure | server logs and world save | high | mitigate | The isolation scan covers the world (except the locked session.lock), logs/ including .gz archives, and the root *.log files, in UTF-8 and UTF-16LE. It requires every world match to be a computer/&lt;id&gt;/secret.txt (SRV-03). |
| T-03-05-03 | Tampering | stale CDN content at first install | medium | mitigate | Task 2 compares every raw URL with origin/main byte for byte and blocks Task 3 until all four match. The devices' own header and compile checks from Plan 03-04 are a second line. |
| T-03-05-04 | Denial of Service | kept host helpers after the D-19 deletion | medium | mitigate | launcher.py defines REPO_ROOT itself. test_deploy_rules.py (rule, probe and launcher tests) must print "# fail 0", and ruff and mypy run over deploy/ and tests/ before the commit. |
| T-03-05-05 | Spoofing | bare smoke-check websocket to the bridge | low | accept | The bridge rejects any connection that sends no hello within 10 s and logs a warning. Check a closes the handle itself, and no device identity or token is sent. |
| T-03-05-06 | Repudiation | who pushed main | low | accept | The push is an explicit orchestrator checkpoint with a recorded sha in the resume signal (D-18). It is never a silent executor side effect. |
| T-03-05-07 | Tampering | device A still runs the pre-D-19 startup.lua with a dead marker branch | low | accept | startup.lua does not update itself. Device A's folder holds no marker file (the 4b check proves it), and the check reads only the device's own filesystem, so the branch never fires there. Re-running the wget line replaces the file whenever the admin chooses (D-19). |
</threat_model>

<verification>
- raw GitHub served the pushed bytes before anything happened in game.
- Device A was set up with only the wget line, the token and Enter, then rebooted into its role program and connected.
- The D-13 success shape and both failure shapes were captured verbatim.
- The developer placement path is removed. The kept host helpers pass their tests, and chat.lua and client.lua are untouched.
- computer/0/ confirms the numeric `computer/<id>/` path, and no device folder holds a marker file.
- The token appears only in device A's `secret.txt`.
</verification>

<success_criteria>
- SRV-02 is proven on the live server: the installer placed the files, the folder path is confirmed, and no second placement path exists.
- SRV-03 (token isolation), SRV-04 (reboot relaunches the role program) and the install half of SRV-05 are proven on the live server.
- SRV-01's in-game smoke check has run.
- The SUMMARY holds the real text Plan 03-06 needs for the README.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-05-SUMMARY.md` when done. It must include the fixed-key lines listed in Task 4 step 4d, each at the start of a line, and no token value.
</output>
