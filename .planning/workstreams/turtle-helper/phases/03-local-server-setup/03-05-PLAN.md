---
phase: 03-local-server-setup
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [SRV-01, SRV-02, SRV-03, SRV-04, SRV-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Before any in-game step, raw.githubusercontent.com serves install.lua, startup.lua, base/chat.lua and turtle/client.lua byte-identical to origin/main. The orchestrator pushed main at this plan's first checkpoint, never as an executor side effect (D-18). The raw CDN can serve a stale body or a cached 404 for up to about 5 minutes after a push"
    - "A fresh computer set up with only the wget run line, the typed token and Enter at the URL prompt holds, in <SERVER_DIR>/world/computercraft/computer/<numeric id>/: chat.lua, client.lua and startup.lua byte-identical to origin/main, secret.txt equal to BRIDGE_TOKEN, and bridge.txt equal to ws://127.0.0.1:8765. The installer's own first file write created that folder, which confirms the SRV-02 path (SRV-05 install half; D-14, D-15, D-17)"
    - "After a reboot, that computer's startup.lua launches chat (Chat Box attached) or client on its own, and the bridge logs 'device connected:' for it under its label or device-<id> (D-10). No PC-side step is involved (SRV-04; SRV-05; ROADMAP criterion 5, first half)"
    - "The D-13 smoke check ran at a real lua prompt with the bridge up: http.websocket('ws://127.0.0.1:8765') returned a handle. The denied-address shape (ws://127.0.0.2:8765, which is private and allowed by no rule) and the no-listener shape (ws://127.0.0.1:8766, which the rule allows but nothing listens on) were captured verbatim for the docs (SRV-01 in-game smoke check; ROADMAP criterion 1)"
    - "uv run deploy, run against the live and running server, skipped the allow-rule step, wrote the five device files byte-correct into the one marked folder, and printed no token. Every unmarked folder, including the installed device's, stayed byte-identical (SRV-02 developer path; D-03, D-04, D-05, D-06, D-12)"
    - "The token appears in no tracked or untracked-unignored repo file, nowhere in git history, and in no server log (UTF-8 or UTF-16LE, rotated .gz files included). In the world it appears only as computercraft/computer/<id>/secret.txt, one per set-up device (SRV-03; ROADMAP criterion 3)"
    - "Two devices set up in one session never share files. Each writes only its own computer/<id>/ folder, so every world match of the token maps to exactly one numeric device id (SRV-03 concurrency case)"
    - "If a device fails at runtime, the device's files stay the repo's bytes. A fault in install.lua or startup.lua is fixed in the repo and re-pushed through a push checkpoint; a chat.lua or client.lua runtime error is recorded verbatim as Phase 4 LOOP-05 input"
  artifacts:
    - "<SERVER_DIR>/world/computercraft/computer/<device A id>/ (real folder created by install.lua; outside the repo, not git-tracked)"
    - "<SERVER_DIR>/world/computercraft/computer/<device B id>/ (real marked folder filled by uv run deploy; outside the repo, not git-tracked)"
    - ".planning/workstreams/turtle-helper/phases/03-local-server-setup/03-05-SUMMARY.md (records the captured in-game text under fixed keys)"
  key_links:
    - "The SMOKE_DENIED and SMOKE_REFUSED lines in this plan's SUMMARY are what Plan 03-06's README check compares against verbatim, so the docs carry observed text, not RESEARCH.md's guesses"
    - "Device B's _marker.txt makes it the deploy-managed control for Plan 03-06's update proof: it must keep its pre-push Lua while device A takes the update"
  prohibitions:
    - statement: "MUST NOT push, start, stop or restart the server or the bridge, or act in game, from an executor. The push is an orchestrator checkpoint and every in-game step belongs to the operator (D-18, the project's live-run rule)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the bridge token value reach the transcript, a log file or the SUMMARY. Every check compares it in-process and prints only paths, ids and counts (SRV-03, D-05, D-15)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit chat.lua or client.lua, on a device or in the repo, to make a first-run error go away in this plan. Lua runtime fixes belong to Phase 4 LOOP-05"
      status: resolved
      verification: judgment
---

<objective>
Put Plan 03-04's in-game install in front of the live server and prove Phase 3's remaining claims on real devices:

1. The orchestrator pushes `main` (D-18). The executor confirms raw GitHub serves the pushed bytes.
2. The operator sets up device A with nothing but the one `wget run` line, the typed token and Enter (D-14, D-15, D-17). At the same prompt, before rebooting, the operator runs the D-13 smoke check, capturing the success shape and both failure shapes verbatim. Then they reboot and watch `startup.lua` bring the role program up and connect (SRV-04, SRV-05).
3. The operator marks device B.
4. The executor runs the developer path `uv run deploy` against it (SRV-02), confirms both real folder layouts, and proves the token lives only in the per-device `secret.txt` files (SRV-03).

Purpose: this is where the MEDIUM-confidence folder path, the installer, the boot-time launch and the token isolation stop being fixture claims. Every step that needs the push, the running server or a hand in game is a checkpoint.

Output: two real, set-up devices on the live server (A installed from GitHub, B deploy-managed), and a SUMMARY that records the captured in-game text under fixed keys for Plan 03-06's docs.
</objective>

## Flagged Assumption (spec-less edge probe, SRV-02 "unclassified")

The per-computer folder path `<world>/computercraft/computer/<id>/` is MEDIUM-HIGH confidence from RESEARCH.md (A1). As of 2026-09-25, `world/computercraft/ids.json` reads `{"computer": 0}`: id 0 has been handed out, but no `computer/0/` folder exists yet, because that computer has never written a file.

This plan confirms the path twice. Device A's folder is created by the installer's own first `fs.open` write. Device B's folder is created by the operator's `edit _marker.txt`.

Task 4's layout check asserts that every folder name under `computer/` is numeric and holds the expected files. If the real layout differs, Task 4 records the actual path under `FOLDER_PATH:` so Plan 03-06's docs use what was observed. This is resolved by live confirmation, not assumed away.

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-03-SUMMARY.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-04-SUMMARY.md
@turtle/turtle-helper/install.lua
@turtle/turtle-helper/startup.lua
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push main to GitHub so install.lua and startup.lua are fetchable (D-18)</name>
  <action>Plan 03-04's install.lua and startup.lua exist only in local commits. The in-game wget line and every device's boot-time update fetch from GitHub main, so main must be pushed before anything happens in game. Pushing is an orchestrator action handed over at this checkpoint, never an executor side effect (D-18; the author authorized pushes for this recipe on 2026-09-25).</action>
  <instructions>
    For the orchestrator:

    1. Confirm `git status --short turtle/turtle-helper` is clean, and that `git log --oneline origin/main..main` lists Plan 03-04's commits (test(03-04), feat(03-04), docs(03-04)).
    2. Run `git push origin main`.
    3. Until the push has completed, do not open or curl the raw install.lua URL. raw.githubusercontent.com sits behind a CDN that can cache a 404 or an older body for a path for up to about 5 minutes. The planner probed that URL once at plan time and got a 404; that cache entry has long expired by the time this checkpoint surfaces.
  </instructions>
  <verification>Task 2 compares the four raw URLs with origin/main byte for byte. It does not hand anything to the operator until all four match.</verification>
  <resume-signal>Reply "pushed &lt;short sha&gt;" once `git push origin main` has succeeded.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm raw GitHub serves the pushed bytes for all four files (D-18, CDN caveat)</name>
  <files>none -- read-only network check against raw.githubusercontent.com</files>
  <read_first>turtle/turtle-helper/install.lua, turtle/turtle-helper/startup.lua</read_first>
  <precondition>The orchestrator replied "pushed &lt;sha&gt;" at Task 1.</precondition>
  <action>
    Run the automated check below from the repository root. It proves three things:
    - the local origin/main ref matches main for the four device files, so the push included them;
    - each raw URL under the pinned base serves bytes whose git blob hash (`git hash-object --no-filters --stdin`) equals origin/main's blob for that path;
    - install.lua is really there, not a 404.

    If any line prints MISSING, the push did not include that file: stop and report it. If any line prints DIFF, the CDN is still serving a cached copy. Wait a few minutes and run the check again. Never go on to Task 3 while any line prints DIFF. If a file still prints DIFF about 10 minutes after the push, stop and report it, because the push may not contain that file.

    This check reads only public files and never touches the token.
  </action>
  <acceptance_criteria>
    - All four files print SAME, and the output ends with RAW_MATCH_OK
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --quiet main origin/main -- turtle/turtle-helper/install.lua turtle/turtle-helper/startup.lua turtle/turtle-helper/base/chat.lua turtle/turtle-helper/turtle/client.lua && BASE=https://raw.githubusercontent.com/nneibaue/minecraft-transit-report/main/turtle/turtle-helper; ok=1; for f in install.lua startup.lua base/chat.lua turtle/client.lua; do want=$(git rev-parse --verify --quiet "origin/main:turtle/turtle-helper/$f") || { echo "MISSING $f"; ok=0; continue; }; got=$(curl -fsSL "$BASE/$f" | git hash-object --no-filters --stdin); if [ "$got" = "$want" ]; then echo "SAME $f"; else echo "DIFF $f"; ok=0; fi; done; [ "$ok" = 1 ] && echo RAW_MATCH_OK</automated>
    <fails_when>non-zero exit, a MISSING or DIFF line, or RAW_MATCH_OK absent (MISSING: the push did not include that file; DIFF: the CDN still serves a stale body or a cached 404, so its blob hash differs from origin/main's)</fails_when>
  </verify>
  <done>raw.githubusercontent.com serves all four device files byte-identical to origin/main, so the in-game wget line fetches exactly what was pushed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: In game -- install device A with the wget line, run the D-13 smoke check, reboot it; mark device B (D-13, D-14, D-15, D-17, D-03)</name>
  <precondition>Task 2 printed RAW_MATCH_OK in this run, and the operator's server and bridge are both running in their own terminals.</precondition>
  <action>Only a player at an in-game computer can run wget, type the token and reboot a device. The executor cannot, and no executor acts in game (live-run rule). These steps set up the admin path on device A and the developer path's marker on device B in one visit.</action>
  <instructions>
    Keep the bridge running in its terminal throughout. Until Phase 4, do not type "$robot" in game chat, because that would spend an API call. Never paste the token back to me.

    Optional first step (D-15). If BRIDGE_TOKEN in turtle/turtle-helper/.env is a long random string, you may change it to a short passphrase you can type easily. If you do, restart the bridge in its terminal (Ctrl+C, then `uv run bridge/bridge.py`) before step 1.

    Device A: the admin path. Recommended hardware: an Advanced Computer with a Chat Box touching it (the chat role). The already-placed computer #0 works too once a Chat Box is attached. Any computer is fine if you have no Chat Box; it will run as a worker.

    1. At device A's shell prompt, type exactly:
       wget run https://raw.githubusercontent.com/nneibaue/minecraft-transit-report/main/turtle/turtle-helper/install.lua
    2. At "Bridge token (typing is hidden):", type BRIDGE_TOKEN's value and press Enter. Only * characters appear.
    3. At the bridge URL prompt, press Enter to keep ws://127.0.0.1:8765.
    4. Copy down every line the installer printed after the wget line (the banner, the "Installed on computer ..." list and the reboot hint).
    5. Smoke check (D-13), still at the shell prompt and BEFORE rebooting. Type `lua`, then enter each line below on its own, and copy what each prints:
       a. ws, err = http.websocket("ws://127.0.0.1:8765") print(ws, err) if ws then ws.close() end
          Expected: a table value and nil. This is the success shape with the rule in place and the bridge up. The bridge may log a "rejected ... no valid hello" warning for this bare connection; that is expected.
       b. print(http.websocket("ws://127.0.0.2:8765"))
          Expected: false plus an error. This is what a computer sees when no allow rule covers the address: 127.0.0.2 is private, and the rule only allows 127.0.0.1.
       c. print(http.websocket("ws://127.0.0.1:8766"))
          Expected: false plus a connection error. This is the "rule present, bridge not listening" shape, because nothing listens on 8766.
       Then type exit() to leave the lua prompt.
    6. Type `reboot`. Copy the lines startup.lua prints (for example "chat.lua up to date"), and note which program starts: chat if a Chat Box is attached, client otherwise.
    7. In the bridge terminal, copy the "device connected: ..." line for device A.

    Device B: the developer path. Recommended hardware: a turtle or an Advanced Computer WITHOUT a Chat Box (the worker role).

    8. Place device B, open its terminal, and type `edit _marker.txt`. Press Ctrl, choose Save; press Ctrl again, choose Exit. Leave the file empty. Do not reboot B yet.

    If the wget line or the installer prints an error at any step, stop there and paste it.
  </instructions>
  <verification>Task 4 checks both folders on disk, runs the developer deploy against device B, proves the token isolation, and records your pasted text in the SUMMARY under fixed keys.</verification>
  <resume-signal>Paste: (1) the installer output from step 4; (2) the three smoke-check results a, b and c; (3) the post-reboot lines and which program started; (4) the bridge's "device connected:" line; (5) "B marked" plus what kind of device B is.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Developer deploy to device B, confirm both real folder layouts (SRV-02), prove token isolation (SRV-03), record the captured text</name>
  <files>&lt;SERVER_DIR&gt;/world/computercraft/computer/&lt;device B id&gt;/* (real, outside the repo -- written by uv run deploy); no repo files</files>
  <read_first>turtle/turtle-helper/deploy/deploy.py, turtle/turtle-helper/bridge/settings.py, turtle/turtle-helper/install.lua, turtle/turtle-helper/startup.lua</read_first>
  <precondition>The operator's Task 3 paste includes a bridge "device connected:" line for device A and the words "B marked".</precondition>
  <action>
    1. Classify the Task 3 paste before running anything.
      - If device A did not connect, find the cause from the pasted text.
        - A fault in install.lua or startup.lua is this phase's code. Fix it in the repo, run Plan 03-04's test_device_lua.py and the luaparse check, commit, and return a new push checkpoint worded like Task 1 (D-18). Then return a repeat of Task 3 before continuing.
        - A runtime error in chat.lua or client.lua is recorded verbatim as Phase 4 LOOP-05 input, and those files are not edited here. Continue with steps 2 and 3, and mark ROADMAP criterion 5's "connects" half as blocked on Phase 4 in the SUMMARY.
      - Never edit files on a device by hand.

    2. Run the first automated check (layout plus deploy). It calls deploy.deploy.main([]) in-process, with stdout and stderr captured into memory. Because the server is running, deploy skips the allow-rule step (D-12), as it should. The check then asserts:
      - every folder under computer/ has a numeric name (the SRV-02 path);
      - exactly one folder holds _marker.txt (device B);
      - at least one folder has a turtle-helper startup.lua and no marker (device A);
      - device A's folder is byte-identical before and after deploy (D-03: unmarked folders are never touched);
      - A's chat.lua, client.lua and startup.lua equal `git show origin/main:...`;
      - A's secret.txt equals BRIDGE_TOKEN, compared in-process only; a mismatch means the operator mistyped it (D-15);
      - A's bridge.txt is exactly ws://127.0.0.1:8765 (D-17);
      - B's three Lua files equal the working-tree files, B's secret.txt equals the token, and B's bridge.txt equals ws://HOST:PORT from Settings (D-04, D-05, D-06);
      - the captured deploy output contains no token.

    3. Run the second automated check (token isolation, SRV-03).
      - It reads the token through Settings and searches in-process for its UTF-8 bytes across four places:
        - every tracked or untracked-but-not-ignored file in the repository (the gitignored .env is the token's source of truth and is deliberately out of scope);
        - the full `git log --all -p` history;
        - every file under <SERVER_DIR>/world except session.lock, which the running server holds locked and which only holds a timestamp marker;
        - every file in <SERVER_DIR>/logs and every *.log in the server root, decompressing .gz archives and searching the UTF-16LE encoding as well, because run.bat's Tee-Object writes server.log as UTF-16LE.
      - It prints only counts and relative paths.
      - If it reports matches outside a secret.txt, the token is probably too short or too common a word. Report the paths only, never the content, and tell the operator that a more distinctive passphrase is needed (D-15).

    4. Keep the operator's pasted text for the SUMMARY. The SUMMARY must contain these lines, each at the start of a line, exactly as the operator reported them:
      - FOLDER_PATH: the observed per-computer path pattern, using the real ids.
      - DEVICE_A: its id, label and role.
      - DEVICE_B: its id and hardware.
      - INSTALLER_OUTPUT: followed by a fenced block of the installer lines.
      - SMOKE_OK: what check a printed.
      - SMOKE_DENIED: only the error message check b printed after false.
      - SMOKE_REFUSED: only the error message check c printed after false.
      - BOOT_OUTPUT: followed by a fenced block of the post-reboot lines.
      - BRIDGE_LOG: the device connected line.

    No value of the token appears in any of them.
  </action>
  <acceptance_criteria>
    - The layout check prints REAL_LAYOUT_OK with device A's and device B's ids
    - The token-isolation check prints TOKEN_ISOLATION_OK with repo=0, history=0 and logs=0, and every world match has the form computercraft/computer/&lt;numeric id&gt;/secret.txt
    - The SUMMARY carries the FOLDER_PATH, DEVICE_A, DEVICE_B, INSTALLER_OUTPUT, SMOKE_OK, SMOKE_DENIED, SMOKE_REFUSED, BOOT_OUTPUT and BRIDGE_LOG lines, and no token value
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import contextlib
import io
import subprocess
from bridge.settings import Settings
from deploy import deploy as d

s = Settings()
root = s.server_dir / "world" / "computercraft" / "computer"
repo = {"chat.lua": "base/chat.lua", "client.lua": "turtle/client.lua", "startup.lua": "startup.lua"}
ours = b"-- startup.lua : turtle-helper"
folders = [f for f in root.iterdir() if f.is_dir()]
assert folders and all(f.name.isdigit() for f in folders), [f.name for f in folders]
marked = [f for f in folders if (f / "_marker.txt").is_file()]
installed = [f for f in folders if f not in marked and (f / "startup.lua").is_file() and (f / "startup.lua").read_bytes().startswith(ours)]
assert len(marked) == 1 and installed, ([f.name for f in marked], [f.name for f in installed])
def snap(f):
    return {p.name: p.read_bytes() for p in f.iterdir() if p.is_file()}
before = {f.name: snap(f) for f in installed}
out = io.StringIO()
with contextlib.redirect_stdout(out), contextlib.redirect_stderr(out):
    rc = d.main([])
assert rc == 0, "deploy exit %s" % rc
assert s.bridge_token not in out.getvalue(), "token leaked into deploy output"
tok = s.bridge_token.encode("utf-8")
def main_bytes(rel):
    return subprocess.run(["git", "show", "origin/main:turtle/turtle-helper/" + rel], capture_output=True, check=True).stdout
for f in installed:
    assert snap(f) == before[f.name], (f.name, "deploy touched an unmarked folder")
    for name, rel in repo.items():
        assert (f / name).read_bytes() == main_bytes(rel), (f.name, name, "differs from origin/main")
    assert (f / "secret.txt").read_bytes().strip() == tok, (f.name, "secret.txt is not BRIDGE_TOKEN")
    assert (f / "bridge.txt").read_bytes().strip() == b"ws://127.0.0.1:8765", (f.name, "bridge.txt")
for f in marked:
    for name, rel in repo.items():
        assert (f / name).read_bytes() == (d.REPO_ROOT / rel).read_bytes(), (f.name, name)
    assert (f / "secret.txt").read_bytes() == tok, (f.name, "secret.txt")
    assert (f / "bridge.txt").read_bytes() == ("ws://%s:%s" % (s.host, s.port)).encode(), (f.name, "bridge.txt")
print("REAL_LAYOUT_OK installed=%s marked=%s" % ([f.name for f in installed], [f.name for f in marked]))
PYEOF</automated>
    <fails_when>non-zero exit, or REAL_LAYOUT_OK absent (a non-numeric folder, no marked or no installed device, deploy touched device A, a file differs from origin/main or the working tree, a secret.txt or bridge.txt mismatch, or the token appeared in deploy's output)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import gzip
import subprocess
from pathlib import Path
from bridge.settings import Settings

s = Settings()
utf8 = s.bridge_token.encode("utf-8")
utf16 = s.bridge_token.encode("utf-16-le")
top = Path(subprocess.run(["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True, check=True).stdout.strip())
listing = subprocess.run(["git", "ls-files", "-z", "--cached", "--others", "--exclude-standard"], cwd=top, capture_output=True, check=True).stdout.decode("utf-8")
repo_hits = [p for p in listing.split("\0") if p and (top / p).is_file() and utf8 in (top / p).read_bytes()]
assert repo_hits == [], repo_hits
history = subprocess.run(["git", "log", "--all", "-p", "--no-color"], cwd=top, capture_output=True, check=True).stdout
assert utf8 not in history, "token found in git history"
world = s.server_dir / "world"
files = [p for p in world.rglob("*") if p.is_file() and p.name != "session.lock"]
world_hits = sorted(p.relative_to(world).as_posix() for p in files if utf8 in p.read_bytes())
parts = [h.split("/") for h in world_hits]
assert world_hits and all(len(x) == 4 and x[:2] == ["computercraft", "computer"] and x[2].isdigit() and x[3] == "secret.txt" for x in parts), world_hits
def blob(p):
    data = p.read_bytes()
    return gzip.decompress(data) if p.suffix == ".gz" else data
logs = [p for p in (s.server_dir / "logs").iterdir() if p.is_file()] + [p for p in s.server_dir.glob("*.log") if p.is_file()]
log_hits = [p.name for p in logs if utf8 in blob(p) or utf16 in blob(p)]
assert log_hits == [], log_hits
print("TOKEN_ISOLATION_OK repo=0 history=0 logs=0 world=%s" % world_hits)
PYEOF</automated>
    <fails_when>non-zero exit, or TOKEN_ISOLATION_OK absent (the token is in a repo file, in git history, in a server log, or in a world file other than a computer/&lt;id&gt;/secret.txt)</fails_when>
  </verify>
  <done>Device B's marked folder holds the five deploy-written files, byte-correct. Device A's installed folder is untouched by deploy and matches origin/main. Both folders confirm the computer/&lt;numeric id&gt;/ path. The token lives only in the per-device secret.txt files. The captured in-game text is recorded under fixed SUMMARY keys, with no token value.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- No repo source changes. The only repo file this plan writes is its SUMMARY, which carries the fixed keys FOLDER_PATH, DEVICE_A, DEVICE_B, INSTALLER_OUTPUT, SMOKE_OK, SMOKE_DENIED, SMOKE_REFUSED, BOOT_OUTPUT and BRIDGE_LOG.
- Real per-device folders on the live server (not git-tracked):
  - device A: chat.lua, client.lua, startup.lua, secret.txt and bridge.txt, all written by install.lua;
  - device B: `_marker.txt` (created by the operator) plus the five files written by `uv run deploy`.
- `main` pushed to GitHub by the orchestrator at Task 1 (D-18).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor -> live secret | Tasks 2 and 4 work next to the real BRIDGE_TOKEN (Settings, the device secret.txt files). Its value must never reach the transcript or the SUMMARY |
| GitHub CDN -> in-game install | The first device install trusts whatever raw.githubusercontent.com serves at that moment |
| Operator keyboard -> device A | The token is typed at a masked prompt in game |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | Task 4 layout and isolation checks | high | mitigate | The token is compared in-process only (bytes `in` / `==`). Assertion messages carry file names and ids, never content. Deploy's output is captured in memory and tested for the token. The isolation scan prints only counts and relative paths. The resume signal tells the operator never to paste the token. |
| T-03-05-02 | Information Disclosure | server logs and world save | high | mitigate | The isolation scan covers the world (except the locked session.lock), logs/ including .gz archives, and the root *.log files, in UTF-8 and UTF-16LE, and requires every world match to be a computer/&lt;id&gt;/secret.txt (SRV-03). |
| T-03-05-03 | Tampering | stale CDN content at first install | medium | mitigate | Task 2 compares every raw URL with origin/main byte for byte and blocks Task 3 until all four match. The devices' own header and compile checks from Plan 03-04 are a second line. |
| T-03-05-04 | Tampering | deploy against the running server's config | medium | mitigate | deploy's TCP probe sees the server up and skips the allow-rule edit (D-12). Only the marked folder is written, and device A's folder is proven byte-identical before and after. |
| T-03-05-05 | Spoofing | bare smoke-check websocket to the bridge | low | accept | The bridge rejects any connection that sends no hello within 10 s and logs a warning. Check a closes the handle itself, and no device identity or token is sent. |
| T-03-05-06 | Repudiation | who pushed main | low | accept | The push is an explicit orchestrator checkpoint with a recorded sha in the resume signal (D-18). It is never a silent executor side effect. |
</threat_model>

<verification>
- raw GitHub served the pushed bytes before anything happened in game.
- Device A was set up with only the wget line, the token and Enter, then rebooted into its role program and connected.
- The D-13 success shape and both failure shapes were captured verbatim.
- `uv run deploy` filled device B's marked folder byte-correct without touching device A.
- Both folders confirm the numeric `computer/<id>/` path.
- The token appears only in the two per-device `secret.txt` files.
</verification>

<success_criteria>
- SRV-02 (developer path, confirmed folder path), SRV-03 (token isolation), SRV-04 (reboot relaunches the role program) and the install half of SRV-05 are proven on the live server.
- SRV-01's in-game smoke check has run.
- The SUMMARY holds the real text Plan 03-06 needs for the README.
</success_criteria>

<output>
Create `.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-05-SUMMARY.md` when done. It must include the fixed-key lines listed in Task 4 step 4, each at the start of a line and exactly as the operator reported, and no token value.
</output>
