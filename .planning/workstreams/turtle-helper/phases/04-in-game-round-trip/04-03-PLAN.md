---
phase: 04-in-game-round-trip
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - turtle/turtle-helper/turtle/client.lua
  - turtle/turtle-helper/base/chat.lua
  - turtle/turtle-helper/tests/test_device_lua.py
autonomous: false
requirements: [LOOP-02, LOOP-03, LOOP-04, LOOP-05]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "LOOP-02: the second computer, a bare Advanced Computer with nothing attached, installed with the one wget line, the typed token and a reboot, makes the bridge log `device connected: device-<N> (computer) caps=['list_chest', 'push_one_slot', 'status']` (D-02)"
    - "Edge LOOP-02/adjacency: the second computer registers under its own id, with N not 0. device-0 stays the only `(chat)` device and is not replaced: no second Chat Box device and no `replacing stale connection` line for device-0 (Pitfall 6)"
    - "Edge LOOP-02/empty: with no peripherals attached, the second computer still advertises all three computer caps, because client.lua builds caps from its tools table and not from attached peripherals"
    - "Edge LOOP-02/ordering: the caps list arrives sorted as list_chest, push_one_slot, status, because client.lua's capabilities() calls table.sort"
    - "LOOP-03 with two devices (edge LOOP-03/adjacency): `$robot what devices are connected?` gets a spoken reply naming device-0 and device-<N> as two separate devices (D-03)"
    - statement: "Edge LOOP-03/ordering: the order of devices in the spoken list is not specified; any order passes as long as each connected device is named once"
      verification: backstop
    - "LOOP-04 tool error (D-05), when the author runs it: `$robot what is in minecraft:chest_99?` gets a plain-language reply, the bridge keeps running, and the second computer's debug.log shows a `recv:` line carrying list_chest and a `result:` line carrying `no inventory called`. When the author skips it, the SUMMARY says client.lua's cmd/result loop was not exercised in game"
    - "D-11 end state: neither device folder holds `debug` or `debug.log` at phase end, and both devices were rebooted with DEBUG off"
    - "LOOP-05 / ROADMAP criterion 5: every turtle-helper device folder (computer 0 and the second computer) holds chat.lua and client.lua byte-identical to origin/main, and main equals origin/main for both files (DEVICES_MATCH_MAIN_OK)"
    - "The repo, its history and this phase's SUMMARYs hold no token (REPO_TOKEN_CLEAN_OK)"
    - "The SUMMARY records from pasted text only (D-12): BOOT_A_NORMAL, BOOT_SECOND_NORMAL, BRIDGE_CONNECT_LINES, BRIDGE_REQUEST_LOG, CHAT_REPLY_AS_SHOWN. It also records FIX_LIST, VERIFIED_FACTS, NOTES_FOR_PHASE_5 and PAID_CALLS (D-06, D-09)"
  artifacts:
    - ".planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-03-SUMMARY.md (SECOND_CONNECT, DEVICES_ANSWER_2, TOOL_ERROR_ANSWER, CMD_RESULT_LOOP, BOOT_A_NORMAL, BOOT_SECOND_NORMAL, BRIDGE_CONNECT_LINES, BRIDGE_REQUEST_LOG, CHAT_REPLY_AS_SHOWN, FIX_LIST, VERIFIED_FACTS, NOTES_FOR_PHASE_5, PAID_CALLS)"
    - "turtle/turtle-helper/turtle/client.lua (changed only if the second computer surfaces a bug)"
  key_links:
    - "The wget line runs install.lua, which writes secret.txt and bridge.txt. The reboot then runs startup.lua, which finds no Chat Box and runs client.lua, which says hello as role computer"
    - "The tool error sends list_chest from the bridge to the second computer. client.lua's pcall turns it into ok=false with `/client.lua:<line>: no inventory called minecraft:chest_99`, and the model speaks a plain reply"
    - "FIX_LIST and VERIFIED_FACTS feed Phase 5 D-13's Verified in game block; the D-12 keys feed Phase 5 D-11's What you should see"
  prohibitions:
    - statement: "MUST NOT leave `debug` or `debug.log` on either device at phase end: debug.log holds every player's chat lines as typed"
      status: resolved
      verification: judgment
    - statement: "MUST NOT attach a Chat Box, modem, chest or turtle upgrade to the second computer in this phase (D-02; chests are CHORE-01's)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT print the token, write into the world folder, act in game, or make a paid call from an agent (live-run rule, D-06, SRV-03)"
      status: resolved
      verification: judgment
---

<objective>
Add the second computer as the last step (D-01, D-02), then close the phase:
- the second computer connects and appears with role and caps (LOOP-02);
- the devices question names both devices (LOOP-03);
- a tool error on the second computer gets a plain reply (LOOP-04 second case, D-05). This is the only in-game exercise of client.lua's cmd/result loop this phase;
- debug is turned off and the normal start-up text captured (D-11, D-12);
- both devices are proven byte-identical to main, and the fix list and paid-call count are recorded (LOOP-05, D-06, D-09).

Output: 04-03-SUMMARY.md, the phase's evidence file for Phase 5.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-CONTEXT.md
@.planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-RESEARCH.md
@.planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-02-SUMMARY.md
@.planning/workstreams/turtle-helper/phases/03-local-server-setup/03-06-PLAN.md
@turtle/turtle-helper/turtle/client.lua
@turtle/turtle-helper/install.lua
</context>

## How this plan runs

Every in-game step is the author's, and no agent starts or stops a process (live-run rule). Author-facing lines say "the base computer (the one with the Chat Box)" and "the second computer". If anything on the second computer shows an error, the orchestrator runs the same inline fix loop as Plan 04-02 Task 3: repo edit, luaparse, tests, commit `fix(04-03): …`, push, RAW_MATCH_OK, then the author holds Ctrl+R. A client.lua fix that changes a result shape is mirrored in harness/ and tests/ the same day (D-10).

The D-05 tool error is included, per Claude's discretion. It costs one Haiku call, and it is the only thing this phase that runs client.lua's cmd/result path in game. The author may still skip it (D-06).

## Artifacts this phase produces (this plan)

- The second device: a new folder `<SERVER_DIR>/world/computercraft/computer/<N>/`, created by install.lua in game, which connects as `device-<N>`.
- 04-03-SUMMARY.md keys: SECOND_CONNECT, DEVICES_ANSWER_2, TOOL_ERROR_ANSWER, CMD_RESULT_LOOP, BOOT_A_NORMAL, BOOT_SECOND_NORMAL, BRIDGE_CONNECT_LINES, BRIDGE_REQUEST_LOG, CHAT_REPLY_AS_SHOWN, FIX_LIST, VERIFIED_FACTS, NOTES_FOR_PHASE_5, PAID_CALLS.
- Check markers: CMD_RESULT_LOOP_OK, DEVICES_MATCH_MAIN_OK, REPO_TOKEN_CLEAN_OK.
- Zero or more `fix(04-03): …` commits.

## Flagged assumptions (edge probe and research, not auto-resolved)

- **LOOP-05 (edge probe: unclassified, left unresolved):** which Lua errors a first run surfaces cannot be known in advance. The byte compare proves the devices equal main, and FIX_LIST records what was actually seen. A code path nobody ran this phase (the turtle tools, push_one_slot, a non-empty list_chest) is not claimed verified.
- **A4:** the new computer most likely gets id 2 (`device-2`), or 1 if the author reuses computer 1's block. The checks discover the folder rather than assuming it.
- **A5:** the Chat Box's player lookup is assumed case-insensitive for a model-written whisper target. The bridge's plain-text fallback uses the exact event name anyway.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install the second computer with debug on (LOOP-02, D-02, D-11)</name>
  <precondition>Plan 04-02 is complete. The bridge and server are still running, and the base computer is connected.</precondition>
  <read_first>
    - turtle/turtle-helper/install.lua (the prompts: hidden token, Enter for ws://127.0.0.1:8765)
    - turtle/turtle-helper/startup.lua (role detection by `peripheral.find("chatBox")`)
    - .planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-RESEARCH.md Pitfall 6 and "Worker id and folder"
  </read_first>
  <action>Only the author can place and use a computer in game. Before handing this over, the orchestrator re-runs the four-file RAW_MATCH from Plan 04-01 Task 4 and requires RAW_MATCH_OK, so the wget line and the first boot fetch current bytes.</action>
  <instructions>
    1. Place a new Advanced Computer away from the base computer, with nothing next to it: no Chat Box, modem or chest. A Chat Box beside it would make it act like a second base computer.
    2. Open it and type:
       `wget run https://raw.githubusercontent.com/nneibaue/minecraft-transit-report/main/turtle/turtle-helper/install.lua`
    3. Type the bridge token when asked (it stays hidden). Press Enter to keep `ws://127.0.0.1:8765`.
    4. Type `mkdir debug`, then `reboot`.
    5. Copy what it prints while it starts, and the bridge's new `device connected:` line.

    If it shows an error instead, copy that. It gets fixed the same way as before.
  </instructions>
  <verification>Task 3 checks the second computer's folder on disk after Task 2.</verification>
  <acceptance_criteria>
    - The bridge paste shows `device connected: device-<N> (computer) caps=['list_chest', 'push_one_slot', 'status']`, with N not 0
    - No new `(chat)` connect line and no `replacing stale connection` line for device-0 appear
  </acceptance_criteria>
  <resume-signal>Paste the second computer's start-up lines and the bridge's `device connected:` line.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The devices question with both computers, then the tool error (LOOP-03, LOOP-04, D-03, D-05, D-12)</name>
  <read_first>
    - turtle/turtle-helper/turtle/client.lua (`tools.list_chest` at lines 55-64; `session` result shape)
    - turtle/turtle-helper/bridge/agent.py (list_devices; handle_request's `request from` and `answer for` lines)
    - .planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-RESEARCH.md Finding 7
  </read_first>
  <what-built>Both computers are connected: the base computer (the one with the Chat Box) and the second computer, which is the robot's hands.</what-built>
  <action>If either question gets no reply, or gets the "something went wrong" fallback, the orchestrator runs the Plan 04-02 Task 3 fix loop, with commits scoped fix(04-03), until it is answered. Each `request from` line adds one to the paid-call tally.</action>
  <how-to-verify>
    1. Type `$robot what devices are connected?`. Expect a reply naming both device-0 (the base computer) and the second computer's device id.
       - Copy the reply exactly as it shows in chat.
       - Copy the bridge lines from `request from` down to the last `answer for` or `spoke ...` line.
       - These are the lines Phase 5's docs will quote, so copy them exactly.
    2. One more paid question, recommended: `$robot what is in minecraft:chest_99?`. There is no such chest, so expect a plain reply that it couldn't find it. Copy the reply and the bridge's `answer for` line. Or reply "skip-tool-error".
    3. Check that the bridge window is still running.
  </how-to-verify>
  <acceptance_criteria>
    - Reply 1 names device-0 and device-<N> as separate devices, in any order, and does not contain "something went wrong" (LOOP-03)
    - Reply 2, if run, is plain language saying the chest or inventory was not found, and does not contain "something went wrong" (LOOP-04)
    - The bridge is still running after both
  </acceptance_criteria>
  <resume-signal>Paste reply 1 as shown in chat with its bridge lines, then reply 2 with its `answer for` line (or "skip-tool-error"), and say whether the bridge is still running.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Confirm client.lua's cmd/result loop ran on the second computer (LOOP-02, LOOP-04, D-05, D-10)</name>
  <files>none (read-only against &lt;SERVER_DIR&gt;/world/computercraft/computer/&lt;N&gt;/)</files>
  <read_first>
    - turtle/turtle-helper/turtle/client.lua (the `recv:` and `result:` debug lines from Plan 04-01 Task 2)
    - turtle/turtle-helper/harness/scenarios.py (the canned list_chest result shape, to compare against what client.lua actually sent)
  </read_first>
  <precondition>Task 2's paste is in. If the author replied "skip-tool-error", skip the check and record `CMD_RESULT_LOOP: not exercised in game (skipped by the author)`.</precondition>
  <action>
    Run the check below. It:
    - discovers the one turtle-helper device folder other than computer/0, meaning one holding secret.txt and a startup.lua whose header reads `-- startup.lua : turtle-helper`;
    - confirms its client.lua equals origin/main;
    - confirms its debug.log holds a `recv:` line carrying list_chest and a `result:` line carrying `no inventory called`.

    Then compare the `result:` frame's keys (type, cid, ok, error) with the harness's canned failure shape. They should match RESEARCH Finding 7. If they differ, that is a wire-shape difference, and D-10 requires a same-day mirror in harness/ and tests/.

    Record in 04-03-SUMMARY.md, each at the start of a line:
    - SECOND_CONNECT: Task 1's pasted lines;
    - DEVICES_ANSWER_2 and TOOL_ERROR_ANSWER: Task 2's pastes, in fenced blocks;
    - CMD_RESULT_LOOP: the check's output line, plus the `result:` frame copied from debug.log. It holds no token, since the handshake is never logged.
  </action>
  <acceptance_criteria>
    - The check prints CMD_RESULT_LOOP_OK naming the second computer's folder, or the SUMMARY records the skip
    - 04-03-SUMMARY.md holds SECOND_CONNECT, DEVICES_ANSWER_2, TOOL_ERROR_ANSWER and CMD_RESULT_LOOP
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import subprocess
from bridge.settings import Settings

root = Settings().server_dir / "world" / "computercraft" / "computer"
hdr = b"-- startup.lua : turtle-helper"
devs = [f for f in root.iterdir() if f.is_dir() and f.name != "0" and (f / "secret.txt").is_file() and (f / "startup.lua").is_file() and (f / "startup.lua").read_bytes().startswith(hdr)]
assert len(devs) == 1, [f.name for f in devs]
d = devs[0]
want = subprocess.run(["git", "show", "origin/main:turtle/turtle-helper/turtle/client.lua"], capture_output=True, check=True).stdout
assert (d / "client.lua").read_bytes() == want, "second computer client.lua differs from origin/main"
log = (d / "debug.log").read_text(encoding="utf-8", errors="replace").splitlines()
assert any("recv:" in s and "list_chest" in s for s in log), "no list_chest cmd in debug.log"
assert any("result:" in s and "no inventory called" in s for s in log), "no failing result in debug.log"
print("CMD_RESULT_LOOP_OK computer/%s" % d.name)
PYEOF</automated>
    <fails_when>non-zero exit, or CMD_RESULT_LOOP_OK absent (no single second device folder, its client.lua is behind origin/main, or the list_chest cmd and its failing result never reached debug.log)</fails_when>
  </verify>
  <done>client.lua's receive, dispatch and result path ran for real on the second computer, and the frame it sent is on record. Alternatively the skip is recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Debug off on both computers, and the normal start-up text (D-11 end state, D-12)</name>
  <read_first>
    - .planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-RESEARCH.md Finding 8 (Ctrl+T behaviour on a running device)
    - .planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-CONTEXT.md D-11 and D-12
  </read_first>
  <action>Only the author can type at the computers. debug.log holds every player's chat lines, so it must not stay in the world. The start-up lines pasted here are the normal, debug-free text Phase 5's "What you should see" quotes (D-12). The bridge request lines and the chat reply were already captured in Task 2, and are not asked for twice.</action>
  <instructions>
    1. At the base computer (the one with the Chat Box), hold Ctrl+T until the `>` prompt appears. Type `rm debug`, then `rm debug.log`, then `reboot`. Copy what it prints while it starts.
    2. At the second computer, do the same. It can take two holds of Ctrl+T before the prompt appears. Copy what it prints.
    3. Copy the bridge's two new `device connected:` lines.
  </instructions>
  <verification>Task 5 checks both folders on disk: no debug files left, and both hold main's Lua.</verification>
  <acceptance_criteria>
    - Neither pasted start-up block contains a `DEBUG` line
    - The bridge paste shows `device connected: device-0 (chat) caps=['say']` and `device connected: device-<N> (computer) caps=['list_chest', 'push_one_slot', 'status']`
  </acceptance_criteria>
  <resume-signal>Paste both computers' start-up lines and the two bridge `device connected:` lines.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Prove both devices equal main, the repo holds no token, and write the fix list (LOOP-05, D-06, D-09, D-12)</name>
  <files>.planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-01-SUMMARY.md and 04-02-SUMMARY.md (FIXES, PAID_CALLS_04_02, NOTES_FOR_PHASE_5)
    - .planning/workstreams/turtle-helper/phases/05-real-device-resilience-documentation/05-CONTEXT.md D-11 and D-13 (the shape Phase 5 reuses)
    - .planning/workstreams/turtle-helper/phases/03-local-server-setup/03-06-PLAN.md Task 5 (the repo token scan reused verbatim)
  </read_first>
  <precondition>Task 4's paste shows both computers reconnected with no DEBUG lines.</precondition>
  <action>
    1. Run the first check. It discovers every turtle-helper device folder, meaning one holding secret.txt and the turtle-helper startup.lua header, and requires computer 0 plus at least one more. It asserts:
       - main equals origin/main for base/chat.lua and turtle/client.lua;
       - every device folder's chat.lua and client.lua equal origin/main byte for byte. This is ROADMAP criterion 5's diff;
       - no device folder still holds `debug` or `debug.log`.

       Git runs from the repo top, following the 03-06 deviation. secret.txt is never opened.
    2. Run the second check, Phase 3's repo and history token scan, verbatim. It proves no paste, commit or SUMMARY carried the token.
    3. Write 04-03-SUMMARY.md, each key at the start of a line:
       - BOOT_A_NORMAL, BOOT_SECOND_NORMAL and BRIDGE_CONNECT_LINES: Task 4's pastes;
       - BRIDGE_REQUEST_LOG and CHAT_REPLY_AS_SHOWN: Task 2's devices-question pastes. These are the D-12 capture, and only pasted text is used;
       - FIX_LIST: one line per fix, from 04-01, 04-02 and this plan. Each is dated 2026-09-25 and tagged "CC:Tweaked 1.116.1 / Advanced Peripherals 0.7.46r", in the words Phase 5 D-13's Verified in game block reuses: what was wrong, the fix, the commit sha. The first line is the `$` restore: "found by jar inspection before the first run; confirmed in game by the DEBUG line";
       - VERIFIED_FACTS: shapes that held with no fix, each only where this phase saw it:
         - the chat event order `chat, username, message, uuid, isHidden`, from debug.log;
         - the Chat Box call returning `ok=true`, from `say returned:`;
         - the websocket URL match, proven by any `say` arriving;
         - the list_chest failure result shape from Task 3;
       - NOTES_FOR_PHASE_5: 04-02's notes, anything new, and D-04's docs note: Advanced Peripherals hides `$` messages from public chat, so other players see a broadcast answer without its question;
       - PAID_CALLS: 04-02's tally plus this plan's `request from` lines, model claude-haiku-4-5 (D-06).
  </action>
  <acceptance_criteria>
    - The first check prints DEVICES_MATCH_MAIN_OK with computer 0 and the second computer's folder among the names
    - The second check prints REPO_TOKEN_CLEAN_OK
    - 04-03-SUMMARY.md holds BOOT_A_NORMAL, BOOT_SECOND_NORMAL, BRIDGE_CONNECT_LINES, BRIDGE_REQUEST_LOG, CHAT_REPLY_AS_SHOWN, FIX_LIST, VERIFIED_FACTS, NOTES_FOR_PHASE_5 and PAID_CALLS
  </acceptance_criteria>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import subprocess
from bridge.settings import Settings

top = subprocess.run(["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True, check=True).stdout.strip()
def blob(rev, rel):
    return subprocess.run(["git", "show", "%s:turtle/turtle-helper/%s" % (rev, rel)], cwd=top, capture_output=True, check=True).stdout
root = Settings().server_dir / "world" / "computercraft" / "computer"
hdr = b"-- startup.lua : turtle-helper"
devs = [f for f in root.iterdir() if f.is_dir() and (f / "secret.txt").is_file() and (f / "startup.lua").is_file() and (f / "startup.lua").read_bytes().startswith(hdr)]
names = sorted(f.name for f in devs)
assert "0" in names and len(names) >= 2, names
for rel in ("base/chat.lua", "turtle/client.lua"):
    want = blob("origin/main", rel)
    assert blob("main", rel) == want, (rel, "main has unpushed Lua")
    for f in devs:
        assert (f / rel.split("/")[1]).read_bytes() == want, (f.name, rel, "device differs from origin/main")
left = ["%s/%s" % (f.name, n) for f in devs for n in ("debug", "debug.log") if (f / n).exists()]
assert not left, ("debug files left on a device", left)
print("DEVICES_MATCH_MAIN_OK %s" % names)
PYEOF</automated>
    <fails_when>non-zero exit, or DEVICES_MATCH_MAIN_OK absent (fewer than two devices, a device runs Lua that differs from origin/main, main holds an unpushed fix, or a debug marker or debug.log is still on a device)</fails_when>
  </verify>
  <verify>
    <automated>cd turtle/turtle-helper && uv run python - &lt;&lt;'PYEOF'
import subprocess
from pathlib import Path
from bridge.settings import Settings

utf8 = Settings().bridge_token.encode("utf-8")
top = Path(subprocess.run(["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True, check=True).stdout.strip())
listing = subprocess.run(["git", "ls-files", "-z", "--cached", "--others", "--exclude-standard"], cwd=top, capture_output=True, check=True).stdout.decode("utf-8")
hits = [p for p in listing.split("\0") if p and (top / p).is_file() and utf8 in (top / p).read_bytes()]
assert hits == [], hits
history = subprocess.run(["git", "log", "--all", "-p", "--no-color"], cwd=top, capture_output=True, check=True).stdout
assert utf8 not in history, "token found in git history"
print("REPO_TOKEN_CLEAN_OK")
PYEOF</automated>
    <fails_when>non-zero exit, or REPO_TOKEN_CLEAN_OK absent (the token reached a repo file, a SUMMARY or a commit)</fails_when>
  </verify>
  <done>Both in-game devices run exactly main's chat.lua and client.lua with debug off, and the repo is token-free. The SUMMARY carries the captured text, the fix list, the verified facts and the paid-call count Phase 5 builds on.</done>
</task>

</tasks>

<verification>
- LOOP-02: the pasted `device connected: device-<N> (computer) caps=['list_chest', 'push_one_slot', 'status']`.
- LOOP-03 (two devices) and LOOP-04 (tool error): the pasted replies; CMD_RESULT_LOOP_OK.
- LOOP-05: DEVICES_MATCH_MAIN_OK, with no debug files left, plus FIX_LIST.
- REPO_TOKEN_CLEAN_OK.
</verification>

<success_criteria>
- Two real computers are connected with the right roles and caps.
- The devices question names both.
- A tool error is answered in plain words, and the bridge survives it.
- Both devices run exactly main's Lua with debug off.
- Phase 5 has the captured text, the fix list and the paid-call count it needs.
</success_criteria>

<output>
Complete `.planning/workstreams/turtle-helper/phases/04-in-game-round-trip/04-03-SUMMARY.md` with the keys listed in Tasks 3 and 5.
</output>
