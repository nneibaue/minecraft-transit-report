---
phase: quick
quick_id: 260917-s7i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - turtle/quarry.lua
  - turtle/README.md
autonomous: true
requirements: []

estimate:
  tokens: 30000
  tasks: 1
  confidence: high
---

<objective>
Replace `quarry.lua`'s wedge territories with quadrants (a square with the chest at its
corner per turtle; four pinwheel into one square), and add a `solo` layout where a single
turtle sweeps full square rings centred on the chest. The user, running one turtle, saw the
wedge's 45° edges as "a circle, not a square". Executed inline by the orchestrator (user has
declined subagent spawns this session).
</objective>

<threat_model>
Same scope as the previous turtle quick tasks: a Lua script in the ComputerCraft sandbox,
fetched from this public repo's `main` via `wget`. No network I/O, no credentials. ASVS L1:
nothing applicable; no blocking findings.
</threat_model>

<tasks>

<task id="1" name="Quadrant shells, solo rings, layout-aware state">
<files>
turtle/quarry.lua, turtle/README.md
</files>
<action>
1. `inTerritory(x,z)`: never the chest; `solo` → everything; `quadrant` → x ≥ 1, z ≥ 0.
2. `shellCells(r)` (forward-declared for the Home section's fuel estimate): quadrant shell r
   = L-shape max(x, z+1) == r, odd shells column-out/row-back, even shells row-out/column-
   back; solo shell r = full ring max(|x|,|z|) == r walked from (r, -(r-1)) to (r, -r).
3. State: `layout`, `ring`, `idx` replace `dir`/`z_next`; `advanceTarget`, `runSweep`,
   `pendingRoundTrip` use `shellCells`/`currentTarget`.
4. Main: `quarry [solo]`, `quarry reset [solo]`; old-format state → clear message asking
   for `startup reset`; `solo` on a quadrant dig is ignored with a hint.
5. Header Territory section rewritten with an L-shell diagram; README layout bullet.
</action>
<verify>
luaparse OK; free globals are builtins; JS port of `shellCells` proves partition + walking
adjacency for both layouts (rings 1..8) and 0 overlaps / 0 gaps for the four-quadrant
pinwheel inside radius 7.
</verify>
<done>
Single commit with both files; proof output quoted in the SUMMARY.
</done>
</task>

</tasks>
